# Lane DL - dialog and overlay layer: enforce it, document it, settle the native-element question

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship one guard test that makes the modal-shell, portal and layering rules fail a
commit instead of merely being written down, plus the layering document those rules point at.

**Architecture:** A shared `node:fs` source-tree walker is extracted out of the existing
`responsive.guard.test.ts` into `web/src/test/sourceTree.ts`. A new Vitest guard file in the
dialog module imports it and asserts six properties over shipped source with comments stripped.
The layering scale is documented as a CSS comment block in `web/src/theme/tokens.css` in CSS
property terms, which emits nothing under Tailwind v4's prose scan, and the sixth assertion keeps
that block complete. Two header notes in `dialogStack.ts` and two short cross-link comments carry
the reader between them.

**Tech Stack:** TypeScript, Vitest 4, jsdom 29, Tailwind v4, Vite 8, `node:fs` / `node:url`.

**Spec:** `docs/superpowers/specs/2026-09-02-web2-dl-design.md`

---

## Slice independence declaration

**Single slice. Frontend only. Zero Go changes. Not a multi-stage plan.**

There is no backend slice, so the Phase 3 parallelism question does not arise: one
`relay-frontend-engineer` runs Tasks 0 through 11 in order, in one session, producing one PR.
Nothing here needs `make generate`, and no `.sql`, `.proto`, `*.sql.go` or `models.go` file is
touched. No `## Stage N` units: the whole lane is one PR, so nothing needs filing in
`docs/backlog/` as scheduled work.

**The conductor owns, and this plan does not do:** the four `/backlog close` runs, the PR, and the
merge. Task 11 lists exactly what the conductor must close and with what recorded reason.

---

## What the tree refuted in the spec

Read this before Task 0. Everything below was checked against the worktree; four spec claims are
adjusted here and the plan implements the adjusted version, not the spec's wording.

1. **The portal-shape hit count is right; its partition is off by one on each side.** Six hits
   total, confirmed. The spec says "two are in shipped source ... three are in test files". It is
   **three in shipped source** - `dialogStack.ts`'s `document.body.appendChild(l)` in `apply()`,
   and `DialogShell.tsx` **twice** (the `createPortal` import and the `createPortal(` call) - and
   **two in test files** (`dialogStack.test.ts`, `enrollmentTokenSecrecy.test.tsx`), plus the one
   comment in `UsersTab.test.tsx`. The conclusion is unaffected: every shipped hit is inside
   `web/src/components/dialog/`, so A5 is green on arrival and C5 matches both dialog-module files.
2. **`ResetPasswordDialog.tsx` carries three scrim tokens on one line, not a two-token fragment.**
   Its `error?` prop comment ends a line with the positioning, inset and stacking tokens
   together. The spec predicted "the near-miss probe needs three tokens on one line, so neither
   should register" - that reasoning does not hold for this file; only comment stripping keeps it
   out. This is an upgrade, not a problem: it turns that file into a **live positive control for
   the comment stripper against a real producer**, which Task 5 exercises deliberately.
   `SessionsTab.tsx` is as the spec describes - two tokens on one line, the third on the next.
3. **The spec's mutation table mis-attributes the C4 kill.** "Remove one file from the A4
   allowlist" reddens **A4** (the file is now found and unlisted), not C4. C4 - every allowlisted
   file is matched by the probe - is killed by the probe or the producer drifting apart, so Task 6
   kills it by rewriting one allowlisted registration through an aliased `document` reference,
   which is also the real-world drift C4 exists to catch.
4. **`web/e2e/` holds ten entries, not eight.** The spec enumerates the eight the argument cares
   about and silently omits `README.md` and `ensure-db.mjs`. The load-bearing claim is confirmed:
   the four `.spec.ts` files are `auth`, `header-nav`, `keyboard`, `layout`, and a
   case-insensitive search of the whole directory for dialog, modal or Confirm returns only three
   hits, all of them the word "confirmed" in prose. **No e2e dialog spec exists.**

Confirmed with nothing refuted: `SCRIM` and both attributes in `DialogShell.tsx`'s header comment;
`aria-modal` spelled in exactly four test files; three document `keydown` listeners and two
document `mousedown` listeners in shipped source; five stacking values in shipped source after
comment stripping (nothing from the table lane adds one); `web/src/test/renderWithQuery.tsx` as the
single `.tsx` the new exclusion removes; no ESLint anywhere in `web/`; `--color-popover` and its
comment as the current tail of the `@theme` block in `tokens.css`.

---

## Environment prerequisite

`npm ci` must have finished in `web/`. Every command below runs from
`D:/dev/relay/.claude/worktrees/web2-dl/web` unless it says otherwise, and none of them work
against an absent `node_modules`. Do not run `npm ci` yourself if it is already running; wait for
it. Work only under `D:/dev/relay/.claude/worktrees/web2-dl` - never `cd D:/dev/relay`.

## Conventions binding on every task

- **Never write a Tailwind utility class as a literal**, in a test file, in a comment, or in a
  commit message. Tailwind v4 scans every file under `web/` including prose, so a class-shaped
  literal emits CSS that outlives the component meant to own it. Build the fragment
  (`'z' + '-'`), derive it from the source at runtime, or name the CSS property instead. This plan
  obeys the same rule in its own listings, so that a copy-paste out of it is safe.
- **Cite a symbol or a distinctive phrase, never a file and a line number** (`web/CLAUDE.md`).
- **Comment policy** (root `CLAUDE.md`): no dates, no change history, no counts of other code, no
  uniqueness or completeness claims about other code, no measurement provenance. A comment states
  a hazard or constraint and may name the test that pins it.
- **Hyphens only.** No em dashes, no en dashes, ASCII only in every file you touch.

## File structure

| file | disposition | responsibility |
|---|---|---|
| `web/src/test/sourceTree.ts` | create | the shared walk, comment stripper, path normalizer, and `SRC_ROOT` |
| `web/src/components/holo/responsive.guard.test.ts` | modify | re-pointed at the shared walker; assertion bodies byte-identical |
| `web/src/components/dialog/dialogShellIsSole.guard.test.ts` | create | C0 and A1-A6 with C1-C6 |
| `web/src/theme/tokens.css` | modify | the LAYERING SCALE comment block, after the `@theme` block |
| `web/src/components/dialog/dialogStack.ts` | modify | two header notes: native-element constraint, and the marking-pass constraint |
| `web/src/components/dialog/DialogShell.tsx` | modify | one cross-link comment beside `SCRIM` |
| `web/src/components/holo/GlassPanel.tsx` | modify | one cross-link comment beside `BASE` |
| `web/src/shell/UserMenu.tsx` | modify | three stale line-number citations converted to phrase pointers |

---

### Task 0: Baseline measurements

No code change, no commit. Every number recorded here is a control that later tasks compare
against; the CSS asset hash is the A/B control for the `tokens.css` edit.

**Files:** none.

- [ ] **Step 1: Confirm a clean tree and the right worktree**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git status --short
git rev-parse --abbrev-ref HEAD
```

Expected: branch `claude/web2-dl-dialogs`. `git status --short` should list nothing under `web/src`.
If `web/dist/` shows as modified, run `git checkout -- web/dist/` now - it is tracked but not
maintained per-PR.

- [ ] **Step 2: Record the collected test counts at HEAD**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npm test 2>&1 | tail -20
```

Expected: a green run ending in `Test Files  <N> passed (<N>)` and `Tests  <M> passed (<M>)`.
**Write both numbers down.** The gate in Task 11 is `<N> + 1` test files (the one new guard file)
and `<M> + 7` tests (C0 plus A1-A6; each assertion and its positive control live in one `test(...)`
so the file contributes seven). If your count differs from `<M> + 7` at the end, a test was
silently skipped or an extra one was added - find out which before proceeding.

- [ ] **Step 3: Record a clean type-check**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npx tsc -b --force
```

Expected: no output, exit 0.

- [ ] **Step 4: Record the built asset names and sizes**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npm run build
```

Expected: vite prints an asset table. **Write down the full CSS asset name including its content
hash** (`dist/assets/index-<hash>.css`) and its size in kB, and the same for the JS asset.

The CSS name is the A/B control. The layering block is a CSS comment naming CSS properties, so
Lightning CSS strips it and the emitted stylesheet is unchanged **byte for byte**, which means the
content hash - and therefore the filename - must be **identical** at Task 11. A different CSS hash
means some string you wrote is class-shaped and emitted a rule.

- [ ] **Step 5: Restore `web/dist`**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git checkout -- web/dist/
git status --short
```

Expected: nothing listed. `web/dist` is tracked but stale by convention; a build must never leak
into the change set.

- [ ] **Step 6: Record the file-set sizes the walker will produce**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
node -e "const {readdirSync,statSync}=require('node:fs'),{join}=require('node:path');function w(d,o=[]){for(const e of readdirSync(d)){const f=join(d,e);if(statSync(f).isDirectory()){w(f,o);continue}o.push(f)}return o}const all=w('src');const tsx=all.filter(f=>f.endsWith('.tsx')&&!f.endsWith('.test.tsx'));const inTest=tsx.filter(f=>f.replace(/\\\\/g,'/').includes('/src/test/'));console.log('tsx non-test:',tsx.length);console.log('of those under src/test:',inTest.length,inTest.map(f=>f.replace(/\\\\/g,'/')));"
```

Expected: `tsx non-test: <K>` where `<K>` is comfortably above 51, and
`of those under src/test: 1 [ '.../src/test/renderWithQuery.tsx' ]`. This is the measurement the
spec asks for rather than assumes: the shared walker's `web/src/test/` exclusion shrinks the
existing file set by exactly one, and `<K> - 1` is still above the guard's "more than 50" control.

---

### Task 1: Extract the shared walker, re-point the responsive guard

A behaviour-preserving refactor, gated as one. This is deliberately first: the new guard file
imports the result, and doing it afterwards would mean writing the walker twice.

**Files:**
- Create: `web/src/test/sourceTree.ts`
- Modify: `web/src/components/holo/responsive.guard.test.ts` (everything above the
  `NUMERIC_SUFFIX` comment; nothing at or below it)

- [ ] **Step 1: Create the shared module**

Create `web/src/test/sourceTree.ts` with exactly this content. The `node:url` paragraph and the
comment-stripper paragraph move here verbatim from the responsive guard - they are the reasons
those two pieces have the shape they have, and they must travel with the code, not be left behind
in the file that no longer owns it.

```ts
import { readdirSync, statSync } from 'node:fs'
import { join } from 'node:path'
// Explicit node:url URL, not the global: the test environment is jsdom, which
// shadows the global URL constructor with its own (whatwg-url) implementation.
// That implementation cannot resolve a relative path against a file:// base that
// carries a Windows drive letter - it silently falls back to jsdom's default
// document location (http://localhost:3000/) instead of throwing, so the bug
// surfaces one line later as "The URL must be of scheme file" out of
// fileURLToPath. Node's own URL has no such bug.
import { fileURLToPath, URL as NodeURL } from 'node:url'

// web/src/ - this file lives at web/src/test/.
export const SRC_ROOT = fileURLToPath(new NodeURL('../', import.meta.url))

// Excluded from every walk. These are test harness modules, not shipped UI:
// renderWithQuery renders deliberately unrealistic markup for other tests to
// mount, so a rule about what the app ships must not be evaluated against it.
const TEST_DIR = join(SRC_ROOT, 'test')

// Shipped sources with the given extensions, e.g. ['.tsx'] or ['.ts', '.tsx'].
// Extensions are a parameter because one guard rules on JSX only while another
// must also reach hooks and plain modules. Declaration files are excluded: they
// carry no implementation for any rule to be about.
export function shippedSources(extensions: string[]): string[] {
  return walk(SRC_ROOT, extensions)
}

function walk(dir: string, extensions: string[]): string[] {
  const out: string[] = []
  for (const entry of readdirSync(dir)) {
    const full = join(dir, entry)
    if (statSync(full).isDirectory()) {
      if (full === TEST_DIR) continue
      out.push(...walk(full, extensions))
      continue
    }
    if (entry.endsWith('.d.ts')) continue
    if (!extensions.some((ext) => entry.endsWith(ext))) continue
    if (extensions.some((ext) => entry.endsWith(`.test${ext}`))) continue
    out.push(full)
  }
  return out
}

// web/src uses relative imports (Windows path separators), never a POSIX-style
// alias, so this is the one place that needs normalizing for a path to read the
// same in a received-array diff on Windows and Linux.
export function toPosix(path: string): string {
  return path.replace(/\\/g, '/')
}

// Strips comments before scanning, both kinds this repo writes. A previous
// version of this scan only stripped `//` line comments, and a real review lane
// broke it: inserting a prose comment into a fully compliant consumer made a
// tag-matching guard report it as a violation, because the regex stopped at the
// first `>` inside the comment's own prose. `{/* ... */}` JSX comments are
// `/* ... */` blocks underneath, and this repo writes them constantly, so a scan
// blind to them will eventually fire on someone documenting intent near the code
// a guard matches, and the fix people reach for under that pressure is to weaken
// the guard rather than the scan. Block comments are stripped first (they can
// span multiple lines), then `//` line comments on what remains.
//
// KNOWN LIMIT: a `//` comment that TRAILS code on the same line is not stripped,
// only a line whose first non-space characters are the slashes. No consumer
// writes one next to a class string today.
export function withoutComments(src: string): string {
  const noBlocks = src.replace(/\/\*[\s\S]*?\*\//g, '')
  return noBlocks
    .split('\n')
    .map((line) => (line.trim().startsWith('//') ? '' : line))
    .join('\n')
}
```

- [ ] **Step 2: Re-point the responsive guard**

In `web/src/components/holo/responsive.guard.test.ts`, replace **everything from the first line up
to but not including** the line that begins `// Matches a bare numeric Tailwind utility suffix`
with:

```ts
import { readFileSync } from 'node:fs'
import { relative } from 'node:path'
import { expect, test } from 'vitest'
import { SRC_ROOT, shippedSources, toPosix, withoutComments } from '../../test/sourceTree'

// The walk, the comment stripper and the path normalizer are shared with the
// dialog module's guard; sourceTree.ts carries the reason each one has the shape
// it has. The set this file rules over is JSX only.
const SRC = SRC_ROOT
const FILES = shippedSources(['.tsx'])
```

Touch nothing at or after the `NUMERIC_SUFFIX` comment. The identifiers the two `test(...)` bodies
read - `FILES`, `SRC`, `readFileSync`, `relative`, `toPosix`, `withoutComments`, `NUMERIC_SUFFIX` -
all still resolve, which is why the bodies can stay byte-identical.

- [ ] **Step 3: Run the byte-identical gate on the assertion bodies**

Write this script to your scratchpad directory as `tailcheck.mjs` and run it from the worktree
root.

```js
import { execFileSync } from 'node:child_process'
import { readFileSync } from 'node:fs'

const P = 'web/src/components/holo/responsive.guard.test.ts'
const ANCHOR = '// Matches a bare numeric Tailwind utility suffix'

// The blob comes out of git with LF; the working copy on this CRLF repo has
// CRLF. Normalizing both is required or the comparison is guaranteed to fail
// for a reason that has nothing to do with the refactor.
const norm = (s) => s.replace(/\r\n/g, '\n')
const tail = (s, label) => {
  const i = s.indexOf(ANCHOR)
  if (i < 0) throw new Error(`anchor missing in ${label}`)
  return norm(s.slice(i))
}

const before = tail(execFileSync('git', ['show', `HEAD:${P}`], { encoding: 'utf8' }), 'HEAD')
const after = tail(readFileSync(P, 'utf8'), 'worktree')
console.log('anchor..EOF bytes  before:', Buffer.byteLength(before), ' after:', Buffer.byteLength(after))
console.log('IDENTICAL:', before === after)
if (before !== after) process.exit(1)
```

Run: `node <scratchpad>/tailcheck.mjs` from `D:/dev/relay/.claude/worktrees/web2-dl`
Expected: two equal byte counts and `IDENTICAL: true`, exit 0.

This is **the** gate for "assertion bodies unchanged". The anchor sits above `NUMERIC_SUFFIX` and
therefore above both `test(...)` calls, so it covers the two assertion bodies and the constant they
share, and it cannot be satisfied by a diff that merely looks small.

- [ ] **Step 4: Inspect the diff shape by eye as a second check**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git diff --stat -- web/src/components/holo/responsive.guard.test.ts
git diff -- web/src/components/holo/responsive.guard.test.ts
```

Expected: one file changed, roughly 8 insertions and roughly 47 deletions - the exact counts are
not the gate, Step 3 is. **The expected hunk shape is a single contiguous hunk at the top of the
file**: the `node:fs`, `node:path` and `node:url` imports and the `node:url` paragraph removed, the
`SRC` definition removed, `sourceFiles` and its comment removed, the `FILES` line changed, and
`toPosix` and `withoutComments` and their comments removed, with the four-line import block and the
two-line `SRC`/`FILES` block added in their place. If any hunk header names a line range that
reaches the `test(` calls, stop: something below the anchor moved.

- [ ] **Step 5: Run both guards green**

Run: `cd web && npx vitest run src/components/holo/responsive.guard.test.ts`
Expected: `2 passed`. Both existing tests green, including the "more than 50 files" control (which
now sees one fewer file) and the `md:grid-cols-` prefixed-file list (which `renderWithQuery.tsx`
was never in, so it is unaffected).

- [ ] **Step 6: Prove the extraction did not blunt the guard**

Add a scratch mutation: in `web/src/components/holo/Chip.tsx`, append to the component's class
string a bare numeric grid-template-columns utility with count 3 and **no** breakpoint prefix.
Do not type the class into this plan or into any committed file; copy the prefix out of the guard's
own regex literal and append the digit.

Run: `cd web && npx vitest run src/components/holo/responsive.guard.test.ts`
Expected: **RED** on `every numeric grid column count carries a breakpoint prefix`, with the
received array containing one entry reading `components/holo/Chip.tsx: <the utility>`. That is the
guard finding a violation in a file it reaches through the new shared walker.

Revert by editing the class string back by hand. Do **not** use `git checkout --` here: the shared
module is not committed yet and a stray path could take it with it.

Run: `cd web && npx vitest run src/components/holo/responsive.guard.test.ts`
Expected: `2 passed`. Green re-run confirmed before moving on.

- [ ] **Step 7: Type-check and commit**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npx tsc -b --force
```

Expected: no output, exit 0.

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/test/sourceTree.ts web/src/components/holo/responsive.guard.test.ts
git commit -m "$(cat <<'EOF'
web: extract the guard-test source walker into web/src/test/sourceTree.ts

The walk, the comment stripper and the POSIX path normalizer were private to
responsive.guard.test.ts. The dialog module needs all three, and the field-error
wiring audit will need them again, so they move to a shared module rather than
being copied. Every feature of the walker exists because something broke without
it, and the reasons travel with the code.

Two behaviour changes, both deliberate. The walk now excludes web/src/test/,
which drops renderWithQuery.tsx - a harness component, not shipped UI - from the
existing guard's file set; the "more than 50 files" control still holds and both
rule assertions are unaffected. And the extensions are a parameter, because the
document-listener assertion this unblocks must reach plain modules as well as
JSX.

Behaviour-preserving for the existing guard, and gated as such: everything from
the NUMERIC_SUFFIX comment to EOF is byte-identical to HEAD after CRLF
normalization, so both assertion bodies are untouched. Re-proved with a bare
numeric grid column count added to Chip.tsx: RED on the grid-cols assertion with
that file named, green after revert.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: The guard file and its shared control C0

C0 first and alone. Every assertion in this file is an absence assertion, and an absence assertion
over an empty or wrongly-populated file list passes forever. C0 is what makes the other six mean
something, so it is committed before any of them exists.

**Files:**
- Create: `web/src/components/dialog/dialogShellIsSole.guard.test.ts`

- [ ] **Step 1: Write the file with C0 only**

```ts
import { readFileSync } from 'node:fs'
import { relative } from 'node:path'
import { expect, test } from 'vitest'
import { SRC_ROOT, shippedSources, toPosix, withoutComments } from '../../test/sourceTree'

// THE MODAL SHELL IS THE ONLY MODAL. Every rule here says the same thing from a
// different angle: modal semantics, the scrim, a document-level Escape, and a
// body-level portal all belong to this module, because each of them is a global
// side effect that two independent owners cannot both get right. The app reached
// three hand-rolled dialogs before DialogShell existed, and it reached them
// because each author had no reason to open the others' files - so the rule has
// to arrive on the commit that breaks it, not in a header note.
//
// Every assertion strips comments first and rules over shipped source only. Both
// exclusions are load-bearing and for different reasons: role and aria-modal are
// spelled in test files that assert them, and both are spelled a second time in
// DialogShell.tsx's own header comment.
//
// KNOWN GAPS, stated rather than implied: these are text scans and are only as
// good as their patterns. A role assembled from a variable, a body reference
// obtained by some other route, and a stacking order set through an inline style
// are all invisible here. The scans raise the cost of a silent violation; they do
// not make one impossible.
//
// NOTHING IN THIS FILE SPELLS A UTILITY CLASS. Tailwind v4 scans prose under
// web/, so a class-shaped literal in a test file emits CSS and keeps a rule alive
// independently of the component that owns it. Class-shaped probes are either
// built from fragments or extracted from the source at run time.

const TSX = shippedSources(['.tsx'])
const SOURCES = shippedSources(['.ts', '.tsx'])

const SHELL = 'components/dialog/DialogShell.tsx'
const STACK = 'components/dialog/dialogStack.ts'
const DIALOG_DIR = 'components/dialog/'

function rel(file: string): string {
  return toPosix(relative(SRC_ROOT, file))
}

function stripped(file: string): string {
  return withoutComments(readFileSync(file, 'utf8'))
}

// C0. Shared by every assertion below, and the reason they are not vacuous. A
// silent zero, or a walk that quietly started including test files, would make
// every absence assertion in this file pass forever while proving nothing.
test('the source walk reaches shipped sources and nothing else', () => {
  expect(TSX.length).toBeGreaterThan(50)
  expect(SOURCES.length).toBeGreaterThan(TSX.length)

  const paths = SOURCES.map(rel)
  expect(paths).toContain(SHELL)
  expect(paths).toContain(STACK)
  expect(paths).toContain('shell/HoloShell.tsx')
  expect(paths.filter((p) => p.startsWith('test/')), 'the walk reached a test harness module').toEqual([])
  expect(paths.filter((p) => /\.test\.tsx?$/.test(p)), 'the walk reached a test file').toEqual([])
})
```

- [ ] **Step 2: Run it green**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `1 passed`.

- [ ] **Step 3: Break C0 and observe it red**

Scratch edit in `web/src/test/sourceTree.ts`: change `SRC_ROOT`'s relative segment from `'../'` to
`'../nope/'`.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED**, an `ENOENT` out of `readdirSync` before any assertion runs. That is the failure
a walker pointed at nothing produces, and it is why C0 is not merely a length check.

Now the subtler break: revert `SRC_ROOT`, and instead make `walk` stop skipping `TEST_DIR` (delete
the `if (full === TEST_DIR) continue` line).

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED** on `the walk reached a test harness module`, received array containing
`test/msw.ts`, `test/renderWithQuery.tsx`, `test/secretLeaks.ts`, `test/setup.ts`,
`test/setup-helpers.ts`, `test/sseStream.ts`.

Restore the line by editing it back.

- [ ] **Step 4: Green re-run**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts src/components/holo/responsive.guard.test.ts`
Expected: `3 passed` across two files.

- [ ] **Step 5: Commit**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogShellIsSole.guard.test.ts
git commit -m "$(cat <<'EOF'
web: dialog guard file with the shared walk control

Every rule this file will carry is an absence assertion, and an absence
assertion over an empty file list passes forever. C0 lands first and alone: the
walk found more than fifty JSX sources, the combined set is strictly larger, it
contains the shell, the stack module and the app shell, and it contains nothing
under web/src/test/ and no test file.

Both breaks observed. Pointing SRC_ROOT at a directory that does not exist fails
in readdirSync before any assertion runs. Deleting the web/src/test/ skip
reddens the harness-module assertion with all six of that directory's modules
named. Green after each revert.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: A1 - the modal role is declared in one place

The guard file is now committed, which is the precondition for every mutation below: reverting a
mutated consumer with `git checkout --` can no longer discard the guard under test.

**Files:**
- Modify: `web/src/components/dialog/dialogShellIsSole.guard.test.ts`

- [ ] **Step 1: Write the failing test**

Append to the guard file:

```ts
// A1. The probe accepts the string form and the braced form, so a role written
// as an expression is caught too. A role assembled from a variable is not - see
// the header's known gaps.
const ROLE_PROBE = /role=\{?['"`]dialog['"`]/

const HAND_ROLLED_MODAL =
  'compose DialogShell instead of declaring modal semantics on a div. The shell owns the portal, ' +
  'the focus trap, the scoped Escape and registration in the dialog stack, and a hand-rolled modal ' +
  'gets none of them.'

test('the modal role is declared in the dialog shell only', () => {
  const found = TSX.filter((f) => ROLE_PROBE.test(stripped(f))).map(rel)
  // C1: the probe matches the one file that is supposed to carry it. Without
  // this, a probe that matches nothing satisfies the rule below vacuously.
  expect(found, 'the role probe no longer matches DialogShell').toContain(SHELL)
  expect(found.filter((p) => p !== SHELL), HAND_ROLLED_MODAL).toEqual([])
})
```

- [ ] **Step 2: Run it and confirm it passes at HEAD**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `2 passed`. This assertion is green on arrival - the tree already complies - so its RED
proof is the mutation in Step 4, not a pre-implementation failure.

- [ ] **Step 3: Commit before mutating**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogShellIsSole.guard.test.ts
git commit -m "$(cat <<'EOF'
web: guard that the modal role is declared in DialogShell only

Green on arrival: the tree complies. The RED proof is the mutation recorded in
the next commit, run against this committed guard so a checkout cannot discard
it.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Mutate, observe RED, revert, re-run green**

Scratch edit: in `web/src/components/holo/Panel.tsx`, add `role="dialog"` to the outermost element
it renders.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on `the modal role is declared in the dialog shell only`**, received
`[ 'components/holo/Panel.tsx' ]`, and the failure message beginning `compose DialogShell instead
of declaring modal semantics on a div`. Confirm the failing assertion is A1's and not C1's - a
mutation that reddens a different assertion has not pinned the guard it was named for. **Record
the message verbatim; it goes in the next commit body.**

Revert: `git checkout -- web/src/components/holo/Panel.tsx`

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `2 passed`. Green re-run taken before the next mutation, so a silently unapplied mutation
cannot later report as "survived".

---

### Task 4: A2 - `aria-modal`

**Files:**
- Modify: `web/src/components/dialog/dialogShellIsSole.guard.test.ts`

- [ ] **Step 1: Write the test**

Append:

```ts
// A2. The test-file exclusion, not any argument about query helpers, is what
// makes this work: four test files spell this attribute literally in
// toHaveAttribute assertions. It is also spelled a second time inside
// DialogShell.tsx's own header comment, which is why comments are stripped even
// for the file that legitimately carries it.
const MODAL_ATTR_PROBE = /aria-modal\s*=/

test('the modal attribute is declared in the dialog shell only', () => {
  const found = TSX.filter((f) => MODAL_ATTR_PROBE.test(stripped(f))).map(rel)
  // C2.
  expect(found, 'the aria-modal probe no longer matches DialogShell').toContain(SHELL)
  expect(found.filter((p) => p !== SHELL), HAND_ROLLED_MODAL).toEqual([])
})
```

- [ ] **Step 2: Run it**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `3 passed`.

- [ ] **Step 3: Prove the comment stripping is load-bearing here**

Scratch edit in `web/src/test/sourceTree.ts`: make `withoutComments` return `src` unchanged
(`return src`).

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: still `3 passed` for A2 specifically - `DialogShell.tsx`'s header spells the attribute
without an `=`, so the probe does not match it there. **This is the point:** the probe is written
with the `=` precisely so that a count-shaped formulation is not needed, and this observation is
what licenses that. Record it. Restore `withoutComments`.

- [ ] **Step 4: Commit**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogShellIsSole.guard.test.ts
git commit -m "$(cat <<'EOF'
web: guard the modal role and aria-modal to the dialog shell

A1's RED proof: role="dialog" added to Panel.tsx's outer element reddens the
modal-role assertion with that file named and the remedy message
"compose DialogShell instead of declaring modal semantics on a div ...". Green
after git checkout -- of that file.

A2 added. The test-file exclusion is what makes it work at all - four test files
spell aria-modal in toHaveAttribute assertions - and the probe requires the
attribute's equals sign, so the second spelling inside DialogShell.tsx's own
header comment is not a match either way. Verified by disabling the comment
stripper and observing A2 still green.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 5: Mutate A2, observe RED, revert, re-run green**

Scratch edit: in `web/src/components/holo/Panel.tsx`, add the `aria-modal` attribute set to the
string `true` on the outermost element.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on `the modal attribute is declared in the dialog shell only`**, received
`[ 'components/holo/Panel.tsx' ]`. Confirm it is A2's assertion, not C2's. Record the message.

Revert: `git checkout -- web/src/components/holo/Panel.tsx`
Run the file again. Expected: `3 passed`.

---

### Task 5: A3 - the scrim, without spelling it

**Files:**
- Modify: `web/src/components/dialog/dialogShellIsSole.guard.test.ts`

- [ ] **Step 1: Write the test**

Append. Note how the probe is obtained: the identifier `SCRIM` is not class-shaped, so anchoring on
it lets the test read the class string out of the component at run time and never hold a literal.
The probe follows a change to the value automatically.

```ts
// A3. The scrim string is never spelled here. It is extracted from the shell's
// own source at run time, anchored on the SCRIM identifier - an identifier is not
// class-shaped, so nothing in this file emits CSS, and the probe follows the
// value if it changes.
const SHELL_SRC = readFileSync(join(SRC_ROOT, 'components', 'dialog', 'DialogShell.tsx'), 'utf8')
const SCRIM_MATCH = SHELL_SRC.match(/\bSCRIM\s*=\s*'([^']*)'/)
const SCRIM_VALUE = SCRIM_MATCH ? SCRIM_MATCH[1] : ''
const SCRIM_TOKENS = SCRIM_VALUE.split(/\s+/).filter(Boolean)
// The three tokens a near miss has to reproduce: the two the value leads with,
// plus the stacking-order token wherever it sits. Requiring the stacking token
// specifically is what keeps a line carrying two common layout tokens from
// registering.
const STACK_PREFIX = 'z' + '-'
const SCRIM_DISTINCTIVE = [
  SCRIM_TOKENS[0],
  SCRIM_TOKENS[1],
  SCRIM_TOKENS.find((t) => t.startsWith(STACK_PREFIX)) ?? '',
]

const HAND_ROLLED_SCRIM =
  'this is the modal scrim. Compose DialogShell rather than painting one: the scrim is half of a ' +
  'modal and the stack that owns the scroll lock, the inert background and the Escape scoping is ' +
  'the other half.'

test('the modal scrim is painted by the dialog shell only', () => {
  // C3: the extraction actually produced a value with enough tokens to be
  // discriminating, and both sub-probes match the file that owns it.
  expect(SCRIM_TOKENS.length, 'the SCRIM extraction found nothing').toBeGreaterThanOrEqual(5)
  expect(SCRIM_DISTINCTIVE.every(Boolean), 'no stacking-order token in the scrim value').toBe(true)
  const shellStripped = stripped(join(SRC_ROOT, 'components', 'dialog', 'DialogShell.tsx'))
  expect(shellStripped).toContain(SCRIM_VALUE)
  expect(
    shellStripped.split('\n').some((line) => SCRIM_DISTINCTIVE.every((t) => line.includes(t))),
  ).toBe(true)

  const others = SOURCES.filter((f) => rel(f) !== SHELL)

  // Exact: nobody else holds the whole string.
  const exact = others.filter((f) => stripped(f).includes(SCRIM_VALUE)).map(rel)
  expect(exact, HAND_ROLLED_SCRIM).toEqual([])

  // Near miss: nobody else has a line carrying all three distinctive tokens.
  // This is what catches a scrim rebuilt with the tokens reordered or with one
  // added, which the exact probe cannot see.
  const near = others
    .filter((f) =>
      stripped(f)
        .split('\n')
        .some((line) => SCRIM_DISTINCTIVE.every((t) => line.includes(t))),
    )
    .map(rel)
  expect(near, HAND_ROLLED_SCRIM).toEqual([])
})
```

Add `join` to the `node:path` import at the top of the file: `import { join, relative } from 'node:path'`.

- [ ] **Step 2: Run it**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `4 passed`.

If the near-miss probe reports `admin/users/ResetPasswordDialog.tsx`, the comment stripper is not
running - fix the probe or the stripper wiring, never the comment. That file's prose legitimately
carries all three distinctive tokens on one line.

- [ ] **Step 3: Prove the stripper against that real producer**

This is a positive control on the comment stripper using a producer that already exists, rather
than a synthetic one.

Scratch edit in `web/src/test/sourceTree.ts`: make `withoutComments` return `src` unchanged.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on `the modal scrim is painted by the dialog shell only`**, near-miss received array
containing `admin/users/ResetPasswordDialog.tsx`. Confirm `profile/SessionsTab.tsx` is **not** in
it: that file splits the same three tokens across two lines, and the probe is per line.

Restore `withoutComments` by editing it back. Re-run: `4 passed`.

- [ ] **Step 4: Commit before mutating**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogShellIsSole.guard.test.ts
git commit -m "$(cat <<'EOF'
web: guard the modal scrim to the dialog shell, without spelling it

A2's RED proof: aria-modal="true" added to Panel.tsx reddens the modal-attribute
assertion with that file named. Green after revert.

A3 extracts the scrim string from DialogShell.tsx at run time, anchored on the
SCRIM identifier, so this test holds no class-shaped literal and cannot keep a
Tailwind rule alive on its own. Two sub-probes: the whole string appearing
elsewhere, and a line elsewhere carrying the value's first two tokens plus its
stacking-order token, which catches a scrim rebuilt with the order changed.

The comment stripper has a real producer to prove it against:
ResetPasswordDialog.tsx's prose carries all three distinctive tokens on one
line. With the stripper disabled the near-miss probe reddens naming that file,
and SessionsTab.tsx does not register because its two-token fragment and its
third token sit on different lines.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 5: Mutate the exact probe, observe RED, revert, re-run green**

Scratch edit: open `web/src/components/dialog/DialogShell.tsx`, copy the value assigned to `SCRIM`,
and paste it as the whole `className` of the outermost element in
`web/src/components/holo/Panel.tsx`. Do not retype it and do not write it into any commit message.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on the scrim assertion**, and specifically on the **exact** sub-probe (the first of
the two `toEqual([])` calls), received `[ 'components/holo/Panel.tsx' ]`, message beginning `this is
the modal scrim`.

Revert: `git checkout -- web/src/components/holo/Panel.tsx`
Run the file. Expected: `4 passed`.

- [ ] **Step 6: Mutate the near-miss probe, observe RED, revert, re-run green**

Scratch edit: paste the same value into `Panel.tsx` again, but reorder the tokens (move the last
token to the front) and append one more layout utility of your choice.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on the scrim assertion**, and specifically on the **near-miss** sub-probe (the
second `toEqual([])`), received `[ 'components/holo/Panel.tsx' ]`. Confirm the exact sub-probe
passed - if it also fired, the tokens were not actually reordered and the mutation did not test
what it was named for.

Revert: `git checkout -- web/src/components/holo/Panel.tsx`
Run the file. Expected: `4 passed`.

---

### Task 6: A4 - the document keydown allowlist

The rule is not "at most N listeners". It is: every document-level keydown listener in shipped
source appears in an allowlist that records, per entry, why that surface is not a modal. Writing
the reason is the control.

**Files:**
- Modify: `web/src/components/dialog/dialogShellIsSole.guard.test.ts`

- [ ] **Step 1: Write the test with the allowlist table**

Append:

```ts
// A4. Anchored on `document.` so a listener registered on an AbortSignal is not
// a false positive - lib/api.ts and the SSE test helper each register one.
// Widened to plain modules as well as JSX: no shipped module adds a document
// listener today, and that is a fence, not a fix - a future hook that did would
// otherwise be invisible to a JSX-only walk.
const DOC_KEYDOWN_PROBE = /document\s*\.\s*addEventListener\s*\(\s*['"`]keydown['"`]/

// THE ALLOWLIST. An entry may only be added with the reason that surface is not
// a modal, because the reason is the actual control - a bare count would grow
// silently.
//
//  components/dialog/DialogShell.tsx - the modal shell itself. Escape must fire
//    when focus has left the panel, which a panel-scoped handler cannot see.
//  shell/UserMenu.tsx - a disclosure, not a modal: no scrim, no scroll lock, no
//    inert background, no focus trap, and Tab out is a dismiss route.
//  shell/HoloShell.tsx - the collapsed navigation panel, the sibling disclosure
//    of the above, sharing its handler set.
const DOC_KEYDOWN_ALLOWED = [SHELL, 'shell/UserMenu.tsx', 'shell/HoloShell.tsx']

const UNLISTED_KEYDOWN =
  'a document-level Escape handler. For a modal, compose DialogShell, which already owns a scoped ' +
  'document Escape gated on being topmost. For a non-modal disclosure, follow the two existing ' +
  'ones and add an allowlist entry stating why this surface is not a modal.'

test('every document keydown listener is allowlisted with a reason', () => {
  const found = SOURCES.filter((f) => DOC_KEYDOWN_PROBE.test(stripped(f))).map(rel)

  // C4, and the strongest control in this file: every allowlisted file is
  // matched by the probe. If the probe drifts, an entry stops matching and this
  // goes red - which a count-shaped control could never detect.
  expect(
    DOC_KEYDOWN_ALLOWED.filter((p) => !found.includes(p)),
    'the keydown probe no longer matches a file the allowlist says registers one',
  ).toEqual([])

  expect(found.filter((p) => !DOC_KEYDOWN_ALLOWED.includes(p)), UNLISTED_KEYDOWN).toEqual([])
})

// CONSIDERED AND DELIBERATELY NOT ASSERTED: both disclosures also register a
// document mousedown listener. A fourth of those is not the hazard this file is
// about, and asserting it would make the allowlist grow for a reason unrelated
// to modal semantics.
```

- [ ] **Step 2: Run it**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `5 passed`. Green on arrival with all three entries matched. If it is red on C4 naming
`shell/HoloShell.tsx`, the probe is wrong - that file's collapsed navigation panel registers a
document keydown listener for Escape and is a legitimate third entry.

- [ ] **Step 3: Commit before mutating**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogShellIsSole.guard.test.ts
git commit -m "$(cat <<'EOF'
web: guard document keydown listeners behind an allowlist with reasons

A3's two RED proofs: the SCRIM value pasted whole onto Panel.tsx reddens the
exact sub-probe naming that file; pasted with the tokens reordered and one added
it reddens the near-miss sub-probe instead, with the exact sub-probe passing.
Green after each revert.

A4 is not "at most N listeners" - that shape decays, and would have shipped red
here, because a legitimate third listener exists in HoloShell.tsx for the
collapsed navigation panel. The assertion is that every document-level keydown
listener appears in an allowlist carrying, per entry, the reason that surface is
not a modal. Adding an entry requires writing the reason, which is the control.

The probe is anchored on document. so an AbortSignal listener is not a false
positive, and it scans plain modules as well as JSX - a fence for a future hook,
not a fix for anything present.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Mutate A4, observe RED, revert, re-run green**

Scratch edit: in `web/src/components/holo/Chip.tsx`, add a `useEffect` that registers a document
keydown listener and removes it on cleanup.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on `every document keydown listener is allowlisted with a reason`**, on the second
assertion, received `[ 'components/holo/Chip.tsx' ]`, message beginning `a document-level Escape
handler`. Confirm C4's assertion passed.

Revert: `git checkout -- web/src/components/holo/Chip.tsx`
Run the file. Expected: `5 passed`.

- [ ] **Step 5: Mutate C4, observe RED, revert, re-run green**

This is the drift C4 exists to catch, not an allowlist edit. Scratch edit in
`web/src/shell/UserMenu.tsx`: inside the `useEffect` that registers `onDown` and `onKey`, introduce
a local alias (`const doc = document`) and register and remove both listeners through it.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on the same test but on the FIRST assertion**, received `[ 'shell/UserMenu.tsx' ]`,
message `the keydown probe no longer matches a file the allowlist says registers one`. The
behaviour is unchanged and the probe has gone blind - exactly the case a count-shaped control
cannot see.

Revert: `git checkout -- web/src/shell/UserMenu.tsx`
Run the file. Expected: `5 passed`.

---

### Task 7: A5 - body-level portals

**Files:**
- Modify: `web/src/components/dialog/dialogShellIsSole.guard.test.ts`

- [ ] **Step 1: Write the test**

Append:

```ts
// A5. Route 1 of the body-level portal question is a constraint written in
// dialogStack.ts's header; this is what delivers it to the person who needs it,
// on the commit that needs it. KNOWN GAP: a body reference obtained some other
// way is not caught.
const BODY_INSERTIONS = ['appendChild', 'append', 'prepend', 'insertBefore']
const PORTAL_PROBE = new RegExp(
  `createPortal|document\\s*\\.\\s*body\\s*\\.\\s*(?:${BODY_INSERTIONS.join('|')})\\b`,
)

const BODY_PORTAL =
  'a node appended to the body after a dialog opens is never marked inert or aria-hidden: the ' +
  'dialog stack marks the background on register and unregister only. It stays keyboard-reachable ' +
  'from behind a modal, is announced as though the modal were not there, and paints above the ' +
  'scrim as a later sibling. Portal into a container this component owns, or add the entry here ' +
  'and decide at the same time whether the new layer sits above or below a modal.'

test('body-level portals belong to the dialog module', () => {
  const found = SOURCES.filter((f) => PORTAL_PROBE.test(stripped(f))).map(rel)
  // C5.
  expect(found, 'the portal probe no longer matches the shell').toContain(SHELL)
  expect(found, 'the portal probe no longer matches the stack').toContain(STACK)
  expect(found.filter((p) => !p.startsWith(DIALOG_DIR)), BODY_PORTAL).toEqual([])
})
```

- [ ] **Step 2: Run it**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `6 passed`.

- [ ] **Step 3: Commit before mutating**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogShellIsSole.guard.test.ts
git commit -m "$(cat <<'EOF'
web: guard body-level portals to the dialog module

A4's two RED proofs: a document keydown listener added to Chip.tsx reddens the
unlisted-listener assertion naming that file; re-registering UserMenu's existing
listeners through a local document alias reddens the allowlist-coverage
assertion instead, with behaviour unchanged - the drift a count-shaped control
could not see. Green after each revert.

A5 makes the body-level portal constraint fire. A MutationObserver on
document.body was the alternative and is an always-on watcher for a producer
that does not exist; the same shape was already rejected in writing twice on
this component. The sweep going red is now that route's trigger, and it stops
the author who knows whether their layer belongs above or below a modal.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Mutate, observe RED, revert, re-run green**

Scratch edit: in `web/src/components/holo/Chip.tsx`, import `createPortal` from `react-dom` and
wrap the returned element in a `createPortal(..., document.body)` call.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on `body-level portals belong to the dialog module`**, on the third assertion,
received `[ 'components/holo/Chip.tsx' ]`, message beginning `a node appended to the body after a
dialog opens`. Confirm both C5 assertions passed.

Revert: `git checkout -- web/src/components/holo/Chip.tsx`
Run the file. Expected: `6 passed`.

---

### Task 8: The two header notes in `dialogStack.ts`

Both are comment-only. The native-element note is rewritten because it points at a backlog item
that has closed and because its current form is evidence-shaped rather than constraint-shaped. The
portal note is new and cites the guard from Task 7 by name, which is the form the comment policy
allows.

**Files:**
- Modify: `web/src/components/dialog/dialogStack.ts` (the header block only - the `WHY NOT native
  <dialog>` paragraph, and a new paragraph after it)

- [ ] **Step 1: Rewrite the native-element note**

Replace the whole existing paragraph that begins `// WHY NOT native <dialog> + showModal(). jsdom
29's` and ends with the `docs/backlog/idea-2026-06-03-web-e2e-harness.md` pointer, with:

```ts
// WHY NOT native <dialog> + showModal(). jsdom's HTMLDialogElement
// implementation is an empty subclass of the base element implementation - no
// showModal, no close, no open reflection - so a dialog built on it throws in
// every test that mounts one here, and the only workaround is a hand-rolled
// polyfill, which makes the suite exercise the polyfill rather than the platform
// and leaves the focus trap, the whole point of the route, as the one thing
// never verified. Any migration is partial by construction in any case:
// showModal() does not lock document scroll, so the scroll lock below stays
// here. The full argument and the conditions for revisiting are in the closed
// backlog item idea-2026-08-09-native-dialog-element-reconsideration.
```

No dates, no history, no "revisit when" that names a moment - the conditions live in the closed
item, which is a record of a moment and cannot drift.

- [ ] **Step 2: Add the marking-pass note**

Immediately after that paragraph, add:

```ts
// THE MARKING PASS RUNS ON REGISTER AND UNREGISTER ONLY. A node appended to
// document.body while a dialog is open is therefore neither marked inert nor
// aria-hidden, and is not covered by the scrim - it stays keyboard-reachable
// from behind the modal, is announced as though the modal were not there, and
// paints above the scrim as a later sibling. A body-level portal author must
// decide whether their layer sits above or below a modal;
// dialogShellIsSole.guard.test.ts is what stops them to ask.
```

- [ ] **Step 3: Run the dialog suite**

Run: `cd web && npx vitest run src/components/dialog/`
Expected: all green. These are comments; nothing should move. If `dialogStack.test.ts` or
`DialogShell.test.tsx` reddens, you edited code.

- [ ] **Step 4: Commit**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogStack.ts
git commit -m "$(cat <<'EOF'
web: state the native-element and marking-pass constraints in dialogStack

A5's RED proof: a createPortal into document.body added to Chip.tsx reddens the
portal assertion naming that file, with both dialog-module controls passing.
Green after revert.

The native-element note pointed at the e2e-harness backlog item as a future
trigger and that item has closed, so the note was quietly stale in the one place
a reader would look. Rewritten to the constraint - the jsdom implementation is
an empty subclass, so the trap would be verified only against a polyfill - plus
the fact that makes any migration partial by construction: showModal() does not
lock document scroll. The argument and the replacement trigger live in the
closed reconsideration item.

The marking-pass note is new and is route 1 of the body-level portal question.
It cites the guard test by name, which is what actually delivers the constraint
to the author who needs it.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: The layering block in `tokens.css`, and the cross-links

**Files:**
- Modify: `web/src/theme/tokens.css` (a block immediately after the `@theme` block closes, before
  the `body` rule)
- Modify: `web/src/components/holo/GlassPanel.tsx` (the comment above `BASE`)
- Modify: `web/src/components/dialog/DialogShell.tsx` (a comment immediately above `SCRIM`)
- Modify: `web/src/shell/UserMenu.tsx` (three stale line-number citations in existing comments)

- [ ] **Step 1: Add the layering block**

In `web/src/theme/tokens.css`, insert this immediately after the closing `}` of the `@theme` block
and before the `body {` rule. The `ENTRY` marker and the `z-index <number> <path>` ordering on those
five lines are **load-bearing**: assertion A6 parses them.

```css
/* LAYERING SCALE - begin
   How overlays stack in this app. --color-popover above is the other half of
   "how do I build an overlay here": this block is where it sits, that token is
   what fills it.

   Written in CSS property terms and never as utility class names, deliberately.
   The class scanner reads this file as source, so a class-shaped name here would
   emit a rule that outlives the component supposed to own it.

   Each ENTRY line is: the z-index value, the file that owns it, the symbol or
   element carrying it, and the stacking context it lives in.

   ENTRY  z-index 50  components/dialog/DialogShell.tsx  SCRIM  root, the portal layer is appended to the body
   ENTRY  z-index 10  shell/HoloShell.tsx  the header element  root
   ENTRY  z-index 0  shell/HoloShell.tsx  the main element  root
   ENTRY  z-index 50  shell/UserMenu.tsx  the dropdown panel  inside the header
   ENTRY  z-index 50  shell/HoloShell.tsx  navPanelClass, below the md breakpoint  inside the header

   RULES

   1. A backdrop filter on an ancestor establishes a stacking context, so a
      z-index declared inside it cannot compete with anything outside it. A glass
      surface carries one; so does the header. The measurement behind this sits
      in shell/HoloShell.tsx, in the comment above the header element - search for
      "275 hit-test points".
   2. The main element is positioned and carries a zero z-index on purpose, so
      page-level stacking is contained and cannot climb over the header. A new
      page-level overlay stays inside that context rather than raising itself past
      it. Same measurement.
   3. A value is comparable only to another value in the same stacking context.
      Repeated values on this scale do not compete unless the context column says
      they share one, and that is what a reader cannot infer from the numbers.
   4. Paint order is only half of it. A new overlay must also decide whether it
      stays reachable and announced while a modal is open. The header note in
      components/dialog/dialogStack.ts is the other half.

   THE DIALOG SCRIM ENTRY DESCRIBES TODAY'S MECHANISM. The scrim is a painted
   element, which is why it is on this scale at all. The alternative is the
   browser's top layer via a native dialog element, which would take dialogs off
   the scale entirely; the argument and the conditions for revisiting it are in
   the closed backlog item
   idea-2026-08-09-native-dialog-element-reconsideration. Adopting it later edits
   this entry rather than invalidating the block.

   Completeness of the ENTRY lines is enforced by
   components/dialog/dialogShellIsSole.guard.test.ts.
   LAYERING SCALE - end */
```

Check as you write: no dates, no counts of other code, no completeness claim about other code
(rule 1 says "a glass surface carries one", not "every glass surface"), no line numbers, no utility
class names, ASCII only, hyphens only.

- [ ] **Step 2: Add the two cross-links**

In `web/src/components/holo/GlassPanel.tsx`, append to the existing comment above `BASE`:

```ts
// This surface's backdrop filter establishes a stacking context, so a z-index
// declared inside it cannot compete with anything outside. The scale and the
// rules for reading it are the LAYERING SCALE block in theme/tokens.css.
```

In `web/src/components/dialog/DialogShell.tsx`, immediately above the `SCRIM` declaration:

```ts
// The stacking value inside SCRIM is on the LAYERING SCALE block in
// theme/tokens.css, together with the rule that decides what it competes with.
```

- [ ] **Step 3: Repair the three pointers your insertion just moved**

The `DialogShell.tsx` insertion shifts every line below it, and `UserMenu.tsx` cites that file by
line three times. `web/CLAUDE.md` requires a symbol or a phrase precisely because nothing in this
repo checks a line citation, so it becomes a pointer at the wrong code silently. In
`web/src/shell/UserMenu.tsx`:

- Replace `(HoloShell.tsx:29-49, measured over 275 hit-test points)` with
  `(the comment above HoloShell.tsx's header element, "275 hit-test points")`. This one is already
  stale at HEAD - those lines are nowhere near the header element - which is the defect the rule
  describes, arriving on its own.
- Replace `DialogShell.tsx:227-240 is the real instance of that detachment shape` with
  `DialogShell.tsx's registration cleanup, where focusWasInside is read, is the real instance of
  that detachment shape`.
- Replace `Same REASONING as DialogShell.tsx:234-239,276-282` with
  `Same REASONING as DialogShell.tsx's focusWasInside read and its isEmpty() trigger-restore
  branch`.

- [ ] **Step 4: Run the affected suites**

Run: `cd web && npx vitest run src/components/dialog/ src/components/holo/ src/shell/`
Expected: all green. Comment-only edits.

- [ ] **Step 5: Run the CSS A/B control now, while the block is the only new thing**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npm run build
```

Expected: the CSS asset name printed by vite is **character-for-character the same** as the one
recorded in Task 0, Step 4. The block is a CSS comment naming CSS properties, Lightning CSS strips
it, and the emitted stylesheet is byte-identical - so its content hash, and therefore its filename,
must be unchanged.

If the hash differs, some string you wrote is class-shaped. Find it:

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
git stash
npm run build && cp dist/assets/*.css /tmp/css-before.css
git stash pop
npm run build && cp dist/assets/*.css /tmp/css-after.css
node -e "const fs=require('node:fs');const sel=p=>new Set(fs.readFileSync(p,'utf8').split('}').map(s=>s.split('{')[0]).filter(Boolean));const a=sel('/tmp/css-before.css'),b=sel('/tmp/css-after.css');console.log('added:',[...b].filter(x=>!a.has(x)));console.log('removed:',[...a].filter(x=>!b.has(x)));"
```

Expected in the failure case: `added:` names the rule, and the word that produced it is in your
block. Reword it to a CSS property phrase; do not weaken the rule that put it there.

**Note before you panic about ordinary English:** words like `relative` are Tailwind utilities and
do appear in the block, but the app already emits that rule, so no delta arises. The check is a
delta check, not an absence-of-English check.

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git checkout -- web/dist/
```

- [ ] **Step 6: Commit**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/theme/tokens.css web/src/components/holo/GlassPanel.tsx web/src/components/dialog/DialogShell.tsx web/src/shell/UserMenu.tsx
git commit -m "$(cat <<'EOF'
web: document the layering scale in tokens.css and cross-link it

Five surfaces declare a stacking order and the numbers cannot be compared
without knowing which stacking context each sits in - the reason two of them
read the same value and do not compete. The scale, the rule that explains it and
the pointer to the inert half now sit in one place a human adding an overlay can
read.

tokens.css and not a TypeScript constants module: Tailwind v4 scans source for
literal class-shaped substrings and never reads the emitted bundle, so a
component building its class from an imported constant emits no CSS at all. A
CSS comment can state the scale as property names and numbers, which is not
class-shaped and emits nothing - measured by the built stylesheet's content hash
being unchanged, so the emitted CSS is byte-identical.

UserMenu.tsx's three citations into other files by line are converted to symbol
and phrase pointers. One of them was already pointing at the wrong lines, and
the DialogShell cross-link added here would have moved the other two.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: A6 - the layering document is complete

Both sides are derived, so nothing class-shaped is spelled: the tree side builds its pattern from
fragments, the document side parses the block.

**Files:**
- Modify: `web/src/components/dialog/dialogShellIsSole.guard.test.ts`

- [ ] **Step 1: Write the test**

Append:

```ts
// A6. A document with nothing keeping it honest is the same defect as an
// invariant with nothing enforcing it, one level up - which is the defect this
// file exists to fix. Both sides are derived: the tree side builds its pattern
// from fragments, and the document side parses the ENTRY lines out of the block,
// so neither spells a utility class. KNOWN GAP: a stacking order set through an
// inline style rather than a utility is invisible to this.
const Z = 'z' + '-'
const Z_INDEX = Z + 'index'
const Z_NUMERIC = new RegExp(`(?<![\\w-])${Z}(\\d+)(?![\\w-])`, 'g')
const Z_ARBITRARY = new RegExp(`(?<![\\w-])${Z}\\[`)
const TOKENS_CSS = join(SRC_ROOT, 'theme', 'tokens.css')
const ENTRY_LINE = new RegExp(`^\\s*ENTRY\\s+${Z_INDEX}\\s+(-?\\d+)\\s+(\\S+\\.tsx?)\\b`)

function scannedPairs(): string[] {
  const out = new Set<string>()
  for (const file of SOURCES) {
    for (const m of stripped(file).matchAll(Z_NUMERIC)) out.add(`${rel(file)} ${m[1]}`)
  }
  return [...out].sort()
}

function documentedPairs(): string[] {
  const css = readFileSync(TOKENS_CSS, 'utf8')
  const start = css.indexOf('LAYERING SCALE - begin')
  const end = css.indexOf('LAYERING SCALE - end')
  if (start < 0 || end < 0) return []
  const out = new Set<string>()
  for (const line of css.slice(start, end).split('\n')) {
    const m = line.match(ENTRY_LINE)
    if (m) out.add(`${m[2]} ${m[1]}`)
  }
  return [...out].sort()
}

const DOCUMENT_IT =
  'a stacking order that the LAYERING SCALE block in theme/tokens.css does not name. Add an ENTRY ' +
  'line for it - the surface, the value, the owning file, the symbol carrying it, and which ' +
  'stacking context it lives in - and read rule 3 while you are there: a value is comparable only ' +
  'to another value in the same context.'

test('the layering block names every stacking order in shipped source', () => {
  const scanned = scannedPairs()
  const documented = documentedPairs()

  // C6.
  expect(scanned.length, 'the stacking-order scan found nothing').toBeGreaterThan(0)
  expect(documented.length, 'no ENTRY line parsed out of the layering block').toBeGreaterThan(0)
  expect(documented.some((p) => p.startsWith(SHELL))).toBe(true)
  expect(documented.some((p) => p.startsWith('shell/HoloShell.tsx'))).toBe(true)

  expect(scanned.filter((p) => !documented.includes(p)), DOCUMENT_IT).toEqual([])
  expect(
    documented.filter((p) => !scanned.includes(p)),
    'the layering block names a stacking order the tree no longer has. Delete the ENTRY line.',
  ).toEqual([])

  // An arbitrary bracketed value cannot be expressed as an ENTRY number, so it
  // is refused outright rather than slipping past the pair comparison.
  expect(
    SOURCES.filter((f) => Z_ARBITRARY.test(stripped(f))).map(rel),
    'an arbitrary stacking-order value. Give it a plain number, add its ENTRY line, and widen this ' +
      'guard deliberately if the arbitrary form is really needed.',
  ).toEqual([])
})
```

- [ ] **Step 2: Run it**

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: `7 passed`.

If the first pair assertion is red, print both arrays and compare. The scanned set should hold
exactly five pairs: the shell's scrim value, the header, the main element, the dropdown panel, and
the navigation panel's below-breakpoint value (the variant prefix does not block the match, because
the character before the utility is the variant separator).

- [ ] **Step 3: Commit before mutating**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git add web/src/components/dialog/dialogShellIsSole.guard.test.ts
git commit -m "$(cat <<'EOF'
web: keep the layering block complete

A6 compares file-and-value pairs in both directions, so a new stacking order
anywhere in shipped source goes red, and a stale ENTRY line for one that was
deleted goes red too. Both sides are derived - the tree pattern is built from
fragments, the document side parses the ENTRY lines - so the test file holds no
class-shaped literal and keeps no Tailwind rule alive on its own.

An arbitrary bracketed stacking value is refused outright: it cannot be written
as an ENTRY number, so allowing it would let the pair comparison pass while the
document said nothing about it.

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: Mutate the unlisted-value direction, observe RED, revert, re-run green**

Scratch edit: in `web/src/components/holo/Chip.tsx`, append a stacking-order utility to the class
string, formed as the letter z, a hyphen and the number 20.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on `the layering block names every stacking order in shipped source`**, received
`[ 'components/holo/Chip.tsx 20' ]`, message beginning `a stacking order that the LAYERING SCALE
block`. Confirm all four C6 assertions passed.

Revert: `git checkout -- web/src/components/holo/Chip.tsx`
Run the file. Expected: `7 passed`.

- [ ] **Step 5: Mutate the sixth-value-in-a-named-file case**

The file-level formulation would miss this one, which is why the comparison is over pairs. Scratch
edit: in `web/src/shell/HoloShell.tsx` - a file the block already names three times - append a
stacking-order utility formed as the letter z, a hyphen and the number 30 to the `main` element's
class string.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED**, received containing `shell/HoloShell.tsx 30`.

Revert: `git checkout -- web/src/shell/HoloShell.tsx`
Run: `cd web && npx vitest run src/components/dialog/ src/shell/`
Expected: all green - `HoloShell.test.tsx` asserts the main element's classes, so a survived
mutation would show up there too.

- [ ] **Step 6: Mutate the arbitrary-value case**

Scratch edit: in `web/src/components/holo/Chip.tsx`, append a stacking-order utility in the
bracketed arbitrary form with the value 999.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on the arbitrary-value assertion**, received `[ 'components/holo/Chip.tsx' ]`.

Revert: `git checkout -- web/src/components/holo/Chip.tsx`
Run the file. Expected: `7 passed`. **This revert matters more than the others**: an arbitrary
bracketed value emits a real rule, so leaving it behind would change the built stylesheet and
break the CSS control in Task 11.

- [ ] **Step 7: Mutate C6, observe RED, revert, re-run green**

Scratch edit in `web/src/theme/tokens.css`: change the string `ENTRY` to `Entry` on all five scale
lines - a plausible reformatting, and the one that would otherwise make A6 pass vacuously.

Run: `cd web && npx vitest run src/components/dialog/dialogShellIsSole.guard.test.ts`
Expected: **RED on `no ENTRY line parsed out of the layering block`**, and on the stale-document
assertion is acceptable only if the parsed-count assertion fired first - check the output orders
them that way. This is C6 earning its place: without it, a reformatted block would silently mean
"nothing is documented, so nothing is undocumented".

Revert: `git checkout -- web/src/theme/tokens.css`
Run the file. Expected: `7 passed`.

- [ ] **Step 8: Amend nothing; record the mutations in a follow-up commit body**

The mutation observations from Steps 4 to 7 go into the Task 11 commit body if there is one, or
into the PR description the conductor writes. Do not amend the committed guard.

---

### Task 11: Gates, hygiene, and the handoff

**Files:** none changed unless a gate fails.

- [ ] **Step 1: Confirm no mutation survived**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git status --short
git diff
```

Expected: `git status --short` lists nothing (every scratch edit reverted, every intended edit
committed) and `git diff` is empty. If any file under `web/src` still shows a scratch mutation,
revert it and re-run the full suite before continuing. Read `git diff --stat` against the size of
the change you intended - a diffstat far larger than expected means a programmatic edit rewrote
line endings.

- [ ] **Step 2: Full unit suite with the count delta**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npm test 2>&1 | tail -20
```

Expected: green, `Test Files  <N + 1> passed`, `Tests  <M + 7> passed`, against the `<N>` and `<M>`
recorded in Task 0. **One new test file** (`dialogShellIsSole.guard.test.ts`) and **seven new
tests** (C0 plus A1 through A6; each control lives inside the test it guards). No existing count
moves: `responsive.guard.test.ts` still contributes two.

If the file count moved by more than one, vitest is collecting something new - check that
`sourceTree.ts` was not named with a test suffix.

- [ ] **Step 3: Type-check**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npx tsc -b --force
```

Expected: no output, exit 0.

- [ ] **Step 4: Build, and the CSS A/B**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl/web
npm run build
```

Expected: clean build, and the CSS asset filename **identical** to the one recorded in Task 0,
Step 4. That is the concrete evidence that the layering block names CSS properties rather than
utility classes, and that none of the four comment edits introduced a class-shaped substring
either. If it differs, run the selector diff from Task 9, Step 5 to name the added rule.

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git checkout -- web/dist/
git status --short
```

Expected: nothing listed. `web/dist` is tracked but not maintained per-PR; a build artifact must
never enter this change set.

- [ ] **Step 5: Line endings**

```bash
cd D:/dev/relay/.claude/worktrees/web2-dl
git ls-files --eol web/src/test/sourceTree.ts web/src/components/dialog/dialogShellIsSole.guard.test.ts web/src/components/dialog/DialogShell.tsx web/src/components/dialog/dialogStack.ts web/src/components/holo/GlassPanel.tsx web/src/components/holo/responsive.guard.test.ts web/src/shell/UserMenu.tsx web/src/theme/tokens.css docs/superpowers/plans/2026-09-02-web2-dl.md
```

Expected: every line reads `i/lf`. A `w/crlf` in the working-tree column is normal on this repo;
`i/lf` in the index column is the requirement. Note that `git diff` normalizes LF churn away while
`git status` does not, so never conclude "nothing to revert" from `git diff` alone.

- [ ] **Step 6: Encoding**

Write this to your scratchpad as `asciicheck.mjs` and run it from the worktree root.

```js
import { readFileSync } from 'node:fs'

const files = process.argv.slice(2)
let bad = 0
for (const f of files) {
  const buf = readFileSync(f)
  // Still valid UTF-8? A round trip through the decoder replaces an invalid
  // byte, so a length change is the tell.
  if (Buffer.compare(Buffer.from(buf.toString('utf8'), 'utf8'), buf) !== 0) {
    console.log(`${f}: NOT valid UTF-8`)
    bad++
  }
  for (let i = 0; i < buf.length; i++) {
    if (buf[i] > 0x7f) {
      console.log(`${f}: non-ASCII byte 0x${buf[i].toString(16)} at offset ${i}`)
      bad++
      break
    }
  }
}
console.log(bad === 0 ? 'clean: valid UTF-8, ASCII only' : `${bad} problem(s)`)
process.exitCode = bad === 0 ? 0 : 1
```

Run it over the same nine paths as Step 5.
Expected: `clean: valid UTF-8, ASCII only`. This catches an em dash, an en dash, and a raw Latin-1
byte written where a UTF-8 sequence was intended - none of which `git ls-files --eol`, `gofmt` or
any test in this repo can see.

- [ ] **Step 7: The e2e question, answered rather than skipped**

`make test-e2e` is **not required for this change set**, and the reason is a property of the diff:
every source edit is a comment, a CSS comment, a new test file or a new test-helper module. No
component's rendered output changes, and the built stylesheet is byte-identical (Step 4).

**The condition that flips this:** if any RED proof left a consumer edit behind (Step 1 catches
that), or if a reviewer takes any of the spec's four decisions the other way and a dialog behaviour
moves, then `make test-e2e` runs. Confirm Step 1 was clean before relying on this.

- [ ] **Step 8: Hand off to the conductor**

Nothing further to commit. Report to the conductor:

**Backlog closes the conductor owns** (all four via `/backlog close <fragment>`, which does the
`git mv` into `docs/backlog/closed/` and stamps the frontmatter - a status flip in place is not a
close):

1. `idea-2026-08-09-native-dialog-element-reconsideration` - **closed, not implemented.** Recorded
   reason: jsdom's `HTMLDialogElement` implementation is an empty subclass, so the focus trap - the
   whole point of the route - would be verified only against a hand-rolled polyfill; and the
   browser lane that could verify it landed with **no dialog spec at all**, so it supplies a
   capability and zero inherited coverage. Migration would delete assertions that run on every
   commit before it could claim parity. Any migration is partial by construction, because
   `showModal()` does not lock document scroll. **Replacement trigger, a conjunction and not a
   disjunction:** (1) jsdom's `HTMLDialogElementImpl` stops being an empty subclass, **and**
   (2) `web/e2e/` carries a dialog spec that opens a modal and asserts the trap, focus restoration
   and Escape scoping in both engines. Either alone is insufficient. This plan implements nothing
   for this item; it records the constraint in `dialogStack.ts`'s header (Task 8) and points at the
   closed item from there and from the layering block.
2. `idea-2026-08-09-dialog-shell-sweep-test` - closed by the new guard file.
3. `idea-2026-08-12-document-z-index-layering-scale` - closed by the `tokens.css` block, the
   cross-links, and A6.
4. `idea-2026-08-09-body-level-portal-inert-marking` - **the conductor decides** whether A5 plus
   the header note closes it, or whether it stays open carrying route 2 (the `MutationObserver`)
   with A5 going red as its newly-real trigger. **If it is closed, the closing note must record
   that A5 is the trigger for route 2** - that sentence is the conductor's step, not this plan's,
   because it can only be written once the disposition is decided.

**Follow-ups to file** (proposals; the human accepts or declines each):

- **An e2e dialog spec** - open a modal in both engines and assert the trap, focus restoration and
  Escape scoping. It is half of item 1's replacement trigger, and filing it is what makes that
  trigger findable rather than latent. Highest value of the three.
- **`idea-2026-08-13-field-error-wiring-audit`** should record that its part 3 mechanism question is
  settled: a Vitest guard test reusing `web/src/test/sourceTree.ts`, not ESLint, because ESLint does
  not exist in this repository at all. A note on the existing item, not a new item.
- **Route 2 for body-level portals** now has a trigger for the first time (A5 going red). Fold into
  item 4's disposition above.

---

## Self-review

**Spec coverage.** Section 1 (Decision 1) - Task 8 and the Task 11 handoff; nothing is implemented
for the migration, as decided. Section 2.1 (shared walker) - Task 1. Section 3 (portal constraint,
route 1 plus A5) - Tasks 7 and 8. Section 4 and 6 (layering block in `tokens.css`) - Task 9.
Section 5 A1-A6 and C0-C6 - Tasks 2 through 7 and 10. Section 7 (both header notes) - Task 8.
Section 9 (sequencing) - followed, with one deliberate departure: the spec puts Decision 1's header
rewrite first, and this plan puts it in Task 8 so the new portal note can cite the guard test by
name truthfully rather than citing a file that does not exist yet. Section 10 (acceptance criteria,
mutation battery, both procedural requirements on the RED proof, emitted-CSS control, e2e flip
condition) - Task 11 and the per-task mutation steps; the guard file is committed before every
mutation and a green re-run is taken between mutations. Section 11 escalation 3 (cutting A6) is
recorded as cuttable wholesale: Task 10 is a single self-contained commit.

**Type consistency.** `SRC_ROOT`, `shippedSources`, `toPosix`, `withoutComments` are defined in
Task 1 and used under those exact names in Tasks 1, 2 and onward. `TSX`, `SOURCES`, `SHELL`,
`STACK`, `DIALOG_DIR`, `rel`, `stripped` are defined in Task 2 and reused unchanged. `join` is added
to the `node:path` import in Task 5, where it is first needed. `HAND_ROLLED_MODAL` is defined in
Task 3 and reused in Task 4.

**Known gaps carried forward deliberately**, each stated at its assertion rather than left implied:
a role assembled from a variable (A1), a body reference obtained by another route (A5), a stacking
order set through an inline style (A6), and a `//` comment trailing code on the same line (the
stripper). These raise the cost of a silent violation; they do not make one impossible. The
alternative that could close them is an AST rule, and Decision 2 rejects that on the ground that
ESLint would be a from-zero adoption for two rules.
