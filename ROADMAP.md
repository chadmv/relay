# Roadmap

> Generated by /roadmap on 2026-06-25 (refresh - wait-for-job adaptive poll shipped;
> mcp-resources and p4d-checksum promoted; SSE wait follow-up filed).
> Source of truth is docs/backlog/ - edit items there, then re-run /roadmap. Manual edits may be
> overwritten.

Relay is a distributed task/render-farm orchestrator: three Go binaries (relay-server =
HTTP + gRPC + scheduler with an embedded React/Vite SPA; relay-agent worker that holds one
gRPC stream and runs each task as a subprocess; the relay CLI), plus an MCP server and a
Perforce source-provider. The current phase is hardening. A 2026-06-10 internal codebase
review surfaced a class of concurrency/correctness bugs that sidestep the cross-cutting
invariants now documented in CLAUDE.md - epoch fencing on every task-status write, one
bounded sender per gRPC stream, identity-checked teardown, the single job-spec pipeline,
no interior pointers across locks, and the single JSON entry point. The team has now closed
that review's agent/runner concurrency findings one at a time; recent sessions shipped the
requeue/retry epoch bump, the stale-stream-teardown fix, the nil-provider source guard, cron
source-spec persistence, the Perforce registry race fix, the runner pipe-drain hang fix, the
job status recompute race fix, the forced-cancel send-backpressure fix, the archived-user
offboarding fix, the background-sweeper Prepare TOCTOU fix, the MCP structured-error delivery
fix, the CLI admin/profile stdout fix, the cancel/disable concurrent-fan-out fix, the
default-cancel/Abandon unbounded-hang fix, the default-cancel sendInventory wedge-escape, the MCP
discovery-time role filtering for admin-only tools, the MCP token mid-session reload, the
per-eviction `p4 client -d` timeout (`RELAY_EVICTION_TIMEOUT`), and the `relay_wait_for_job`
adaptive poll, with priority on correctness of the thing being operated - task dispatch,
scheduling, and source workspaces - before broadening features. With the agent/runner
concurrency-bug stream drained, the MCP auth bugs closed, and the source-workspace eviction
hardened, attention is on the last MCP fidelity items and the embedded web SPA build-out (auth,
Workers, Jobs, and Schedules lists shipped; detail pages, list filters, and Admin/Profile still
pending). CLI and store/schema work fill in behind those.

## Now / Next / Later

### Now
- [MCP resources have no caching and no resource templates](docs/backlog/bug-2026-05-09-mcp-resources-caching-templates.md) - **bug**; the last open MCP bug. No `relay://jobs/{id}` templates and every read refetches.
- [Pin SHA-256 of p4d/p4 binaries downloaded by test container](docs/backlog/idea-2026-05-01-p4d-binary-checksum-verification.md) - idea; the test image curls p4d/p4 with no checksum verification; bounded (test-only) supply-chain risk.
- [Job detail page and row-click navigation](docs/backlog/idea-2026-06-05-job-detail-page-row-click.md) - idea; frontend-only, the backend already returns the full job with DAG, and it unblocks the last-job-link feature.

### Next
- [Jobs stats 24h buckets rely on updated_at finish proxy](docs/backlog/bug-2026-06-05-jobs-stats-24h-updated-at-proxy.md) - bug, low; correct today, a latent watch that only becomes inaccurate if a job-retry endpoint reopens terminal jobs.
- [Audit log for archive/unarchive actions](docs/backlog/idea-2026-05-06-audit-log-archive-unarchive.md) - idea; archive/unarchive are recorded nowhere; wire them into an audit table if one is added.
- [Worker detail running-tasks and activity stats panel](docs/backlog/feature-2026-06-05-worker-detail-activity-panel.md) - feature, medium; needs a new per-worker task endpoint before the UI can be built - the next substantive web-frontend slice.

### Later
- [Worker detail page admin mutation actions](docs/backlog/feature-2026-06-05-worker-detail-admin-mutations.md) - feature, medium; the first write operations in the web frontend.
- [Wire relay_wait_for_job to SSE job events with a poll fallback](docs/backlog/idea-2026-06-25-mcp-wait-sse-subscribe.md) - idea, low; the adaptive poll already captured most of the win, so this near-instant-completion enhancement is a low-priority follow-on.

## What moved

- **Shipped + closed: [wait-for-job-poll-interval](docs/backlog/closed/bug-2026-05-09-wait-for-job-poll-interval.md).** The prior Now #1 / mcp #1 bug landed via Option A (adaptive client-side poll): `relay_wait_for_job` now uses a pure `nextWaitInterval(attempt)` schedule (500ms x4 then 2s steady), so sub-2s jobs return within ~500ms of completion while long-job GET load stays within ~10% of before. First GET immediate; clamp/cancel/terminal unchanged. LISTEN/NOTIFY ruled out (the MCP server holds only a relayclient, no DB); review caught and the engineer fixed a vacuous fast-job test (now wall-clock-bounded, proven non-vacuous). Unit + integration green.
- **New item filed: [mcp-wait-sse-subscribe](docs/backlog/idea-2026-06-25-mcp-wait-sse-subscribe.md) (idea, low).** Phase 1 grounding found an existing auth'd SSE endpoint (`GET /v1/events?job_id=`) + `relayclient.StreamEvents`; wiring `relay_wait_for_job` to subscribe (with a post-subscribe terminal re-check and the adaptive poll as fallback) would make completion near-instant. Deferred to low because the adaptive poll already captured most of the win. Placed in Later / mcp.
- **Reordered: mcp-resources and p4d-checksum promoted.** With wait-for-job closed, `mcp-resources-caching-templates` (the last open MCP bug) and `p4d-binary-checksum` move up into Now, joined by the frontend-only `job-detail-page`; `jobs-stats-24h-proxy`, `audit-log`, and `worker-detail-activity-panel` fill Next.

## Suggested backlog actions

- Consider filing a feature item for the real prerequisite behind
  [remove-synthetic-step-marker](docs/backlog/idea-2026-04-26-remove-synthetic-step-marker.md): persist
  `step_index`/`step_total` onto `task_logs` and expose them on the logs API, so step boundaries have a
  structured form and the text marker can later be retired. Until then the step-marker deletion stays
  blocked.

## Agent runtime & Perforce workspaces (agent-perforce)

1. [Remove synthetic step marker text line once consumers use step_index/step_total](docs/backlog/idea-2026-04-26-remove-synthetic-step-marker.md) (idea) - **BLOCKED**: the structured fields are dropped at persist time and never exposed by the logs API, so the text marker is the only per-step signal consumers get; deleting it now is a regression. Needs the persist+expose prerequisite first.
2. [Pin SHA-256 of p4d/p4 binaries downloaded by test container](docs/backlog/idea-2026-05-01-p4d-binary-checksum-verification.md) (idea) - the test image curls p4d/p4 with no checksum verification; bounded (test-only) supply-chain risk.

## API authorization, auth & account lifecycle (api-auth)

1. [Audit log for archive/unarchive actions](docs/backlog/idea-2026-05-06-audit-log-archive-unarchive.md) (idea) - archive/unarchive are recorded nowhere; wire them into an audit table if one is added.

## Store schema integrity & query performance (store-schema)

1. [Jobs stats 24h buckets rely on updated_at finish proxy](docs/backlog/bug-2026-06-05-jobs-stats-24h-updated-at-proxy.md) (bug, low) - correct today; a conditional watch that becomes inaccurate only if a job-retry endpoint that re-opens terminal jobs is added.

## MCP server fidelity (mcp)

1. [MCP resources have no caching and no resource templates](docs/backlog/bug-2026-05-09-mcp-resources-caching-templates.md) (bug) - no `relay://jobs/{id}` templates and every read refetches.
2. [MCP live task-log streaming via resources or streaming tool calls](docs/backlog/idea-2026-05-09-mcp-live-task-log-streaming.md) (idea) - open question on whether stdio transport can surface live logs without polling.
3. [Wire relay_wait_for_job to SSE job events with a poll fallback](docs/backlog/idea-2026-06-25-mcp-wait-sse-subscribe.md) (idea, low) - subscribe to `GET /v1/events?job_id=` with a post-subscribe terminal re-check, keeping the adaptive poll as fallback; near-instant completion, but the adaptive poll already captured most of the win.

## Web SPA correctness & feature build-out (web-frontend)

1. [UserMenu dropdown panel lacks menu/menuitem roles and keyboard navigation](docs/backlog/feature-2026-06-05-usermenu-panel-menu-roles.md) (feature, low) - the toggle advertises `aria-haspopup=menu` but the panel has no menu semantics or arrow-key nav.
2. [Adopt role-layering pattern or extract a shared accessible-table primitive](docs/backlog/idea-2026-06-05-shared-accessible-table-primitive.md) (idea, low) - decide once a second pseudo-table appears whether to share the WorkersTable ARIA pattern.
3. [Upgrade vite 5 to 8 and vitest 2 to 4 to clear dev-tooling audit advisories](docs/backlog/feature-2026-06-05-upgrade-vite-vitest.md) (feature, low) - 5 npm audit advisories (1 critical) in build/test tooling; do it as a deliberate, separately-tested migration.
4. [Add an end-to-end (Playwright) test harness for the web UI](docs/backlog/idea-2026-06-03-web-e2e-harness.md) (idea, medium) - the auth slice shipped two integration bugs past thorough unit tests; a browser-driven harness catches that class.
5. [Return the user object from login/register to skip the /users/me round-trip](docs/backlog/idea-2026-06-03-login-return-user-object.md) (idea, low) - a small backend change that lets the web client populate the current user in one round-trip.
6. [Job detail page and row-click navigation](docs/backlog/idea-2026-06-05-job-detail-page-row-click.md) (idea) - frontend-only; the backend `GET /v1/jobs/{id}` already returns the full job with DAG.
   blocks [last-job-link-status](docs/backlog/idea-2026-06-05-last-job-link-status.md)
7. [Worker detail running-tasks and activity stats panel](docs/backlog/feature-2026-06-05-worker-detail-activity-panel.md) (feature, medium) - needs a new per-worker task endpoint before the UI can be built.
8. [Worker detail page admin mutation actions](docs/backlog/feature-2026-06-05-worker-detail-admin-mutations.md) (feature, medium) - the first write operations in the web frontend.
9. [Worker detail reservations panel](docs/backlog/feature-2026-06-05-worker-detail-reservations-panel.md) (feature, low) - needs a per-worker reservations lookup or a documented client-side filter.
10. [Schedule detail page and Edit action](docs/backlog/idea-2026-06-05-schedule-detail-page.md) (idea) - editable cron/tz/overlap, read-only spec, next-fires preview, recent runs.
11. [Fleet-wide schedules stats endpoint for the summary strip](docs/backlog/idea-2026-06-05-schedules-stats-endpoint.md) (idea) - makes the ENABLED/PAUSED strip fleet-wide instead of page-scoped.
   blocks [failed-24h-stat](docs/backlog/idea-2026-06-05-failed-24h-stat.md)
12. [FAILED 24h summary stat for the Schedules page](docs/backlog/idea-2026-06-05-failed-24h-stat.md) (idea) - needs the failed-runs aggregate from the schedules stats endpoint.
   depends on [schedules-stats-endpoint](docs/backlog/idea-2026-06-05-schedules-stats-endpoint.md)
13. [Server-side filter and search for the Schedules list](docs/backlog/idea-2026-06-05-schedules-filter-search.md) (idea) - enabled/disabled chips and name/owner/cron search need server-side query params.
14. [Advanced filter params for list endpoints](docs/backlog/idea-2026-05-06-list-endpoint-filters.md) (idea, low) - the generic backend filtering capability behind the per-page search/filter features below.
15. [Job search box (server ?q= filter)](docs/backlog/idea-2026-06-05-job-search-box-q-filter.md) (idea) - a real list-query filter (client-side search is misleading under pagination).
16. [My jobs toggle (server ?mine= filter)](docs/backlog/idea-2026-06-05-my-jobs-toggle-mine-filter.md) (idea) - a server-side `?mine=true` WHERE clause across the jobs list queries.
17. [LAST JOB column as a link with run-status dot](docs/backlog/idea-2026-06-05-last-job-link-status.md) (idea) - needs the job detail page plus a last-job-status field on the response.
   depends on [job-detail-page-row-click](docs/backlog/idea-2026-06-05-job-detail-page-row-click.md)
18. [Jobs Lanes (swimlanes-by-status) view](docs/backlog/idea-2026-06-05-jobs-lanes-swimlanes-view.md) (idea) - per-status capped list calls with a "+N more" overflow.
19. [Jobs Timeline view (6h/24h/7d)](docs/backlog/idea-2026-06-05-jobs-timeline-view.md) (idea) - needs a backend time-window list query the API does not expose yet.

## Recently shipped

- [relay_wait_for_job poll interval too coarse for sub-2s jobs](docs/backlog/closed/bug-2026-05-09-wait-for-job-poll-interval.md) - closed 2026-06-25; `relay_wait_for_job` now drives its inter-poll sleep from a pure `nextWaitInterval(attempt)` helper (500ms for the first 4 attempts, then 2s steady), so sub-2s jobs return within ~500ms of completion while long-job GET load stays within ~10% of before. First GET immediate; deadline clamp / ctx-cancel / terminal behavior unchanged; `s.waitPoll` preserved as a flat-interval test override. LISTEN/NOTIFY ruled out (MCP server holds only a relayclient, no DB); the SSE alternative filed as a low follow-up. Review caught a vacuous fast-job test; the engineer added a wall-clock bound proven non-vacuous (fails at 2.0s under a forced-flat regression). Unit + integration green Windows/Docker; review otherwise clean.
- [Per-eviction timeout for EvictWorkspace?](docs/backlog/closed/idea-2026-04-25-eviction-timeout.md) - closed 2026-06-25; `Sweeper.evict` now wraps the `p4 client -d` (`DeleteClient`) call in `context.WithTimeout`, so a wedged Perforce call can no longer permanently stall the serialized sweeper (which would disable disk reclamation for the agent's lifetime). Operator-tunable via `RELAY_EVICTION_TIMEOUT` (Go duration, mirrors `RELAY_WORKER_GRACE_WINDOW`), default 30m, per maintainer input that production workspaces can be 1 TB+. Only the remote subprocess is bounded - `os.RemoveAll` of the tree stays unbounded (uncancelable local I/O), and a p4 timeout returns before it so no DirtyDelete marker is set and the next sweep retries cleanly. Best-effort semantics unchanged; documented in README.md; tests green Windows + Docker; review found no issues.
- [MCP token read once at startup - mid-session expiry requires client restart](docs/backlog/closed/bug-2026-05-09-mcp-token-mid-session-expiry.md) - closed 2026-06-25; `relayclient.Client`'s token is now `sync.RWMutex`-guarded with `SetToken`/`Token` accessors; a single `Server.do` chokepoint (all ~21 MCP tool calls route through it) reloads the token from config on a 401 and retries exactly once when the reloaded token is non-empty and differs from the token the call used, so `relay login` mid-session takes effect without a client restart. A second 401 still surfaces `auth_expired`; steady-state calls do zero extra I/O. A concurrency fix (compare against the token the call used, not the live one) was found under `-race`. Unit + on-disk reload + negative-case tests, `-race` clean in Docker, integration green; review found no issues.
- [MCP admin-only tools have no discovery-time role filtering](docs/backlog/closed/bug-2026-05-09-mcp-admin-tools-role-filtering.md) - closed 2026-06-25; `NewServer` resolves caller identity once at startup via `callWhoami` and registers admin-only `relay_list_reservations` only when `is_admin == true` (fail-closed comma-ok assertion); whoami failure at startup returns the error from `NewServer` (clean exit, `cli/mcp.go` untouched). Discovery filtering is cosmetic - the server-side `auth(admin(...))` gate plus `forbidden` ToolError fallback stay authoritative and unchanged. New unit tests over the real SDK `ListTools` surface + integration assertions; ~16 MCP test files repaired via a shared `whoamiHandler`/`newWhoamiBackend` helper. Unit + Docker + integration green; review found no issues.
- [sendInventory in the default-cancel defer can park unbounded under a wedged sendCh](docs/backlog/closed/bug-2026-06-21-sendinventory-blocking-send-under-wedge.md) - closed 2026-06-25; `sendInventory` now uses a room-first bounded best-effort try-send (`select { case r.sendCh <- msg: default: }`, mirroring `sendFinalStatus`) when `r.cancelled.Load() || r.abandoned.Load()`, so the deferred cleanup goroutine no longer parks until agent shutdown under a wedged `sendCh`; normal completion stays blocking. Three `//go:build !windows` tests RED-vs-GREEN on Linux/Docker; `internal/agent` suite + `go vet` clean; review found no high/medium/low issues. Closes residual #3 of the 2026-06-21 default-cancel/Abandon hang design.
- [Default cancel / Abandon() hang unbounded under a wedged sendCh](docs/backlog/closed/bug-2026-06-20-default-cancel-abandon-backpressure-bound-test.md) - closed 2026-06-21 (PR #83); per-runner `cancelledCh` gives the default `Cancel(false)` / `Abandon()` log-write and terminal-status paths a per-task escape so a wedged `sendCh` no longer hangs `cmd.Wait()` unbounded.
- [Through-the-stack re-enrollment integration test](docs/backlog/closed/idea-2026-06-05-reenrollment-integration-test.md) - closed 2026-06-21; `TestConnect_EnrollmentTokenRevivesRevokedWorker` drives a revoked worker back through the gRPC enrollment-token revive path.
- [Schedules pagination footer lacks absolute X-Y range](docs/backlog/closed/bug-2026-06-21-schedules-pagination-footer-absolute-range.md) - closed 2026-06-21; `SchedulesPage` now shows an absolute `X-Y of total` range via a `startOffset`/`offsets[]` accumulator gated on `isPlaceholderData`.
- [Add cwd assertions to the three perforce unit tests](docs/backlog/closed/idea-2026-05-01-add-cwd-assertions-perforce.md) - closed 2026-06-21; a shared `assertCwdContract` helper pins the cwd half of the client-selection contract across every recorded call.

## Deferred

- [CIDR allowlist for auto-enroll defense-in-depth](docs/backlog/idea-2026-06-04-cidr-allowlist-auto-enroll.md) - explicitly a Non-Goal in the auto-enroll design; the accepted trust boundary is network reachability to the gRPC server.
- [Should the warm-preference scoring be configurable?](docs/backlog/idea-2026-04-25-warm-preference-scoring-configurable.md) - speculative tuning knob with no current need; CLAUDE.md discourages unrequested configurability.
- [Is last_used_at accurate enough for the sweeper's age policy?](docs/backlog/idea-2026-04-25-last-used-at-accuracy-sweeper.md) - an open question with no confirmed defect; revisit if a misjudged-age eviction is actually observed.
