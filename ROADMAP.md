# Roadmap

> Generated by /roadmap on 2026-09-10 (light, after the third six-item Now batch shipped end
> to end). Seven items closed and fifteen filed; open count 198 -> 206. Theme set unchanged. **This is the project's first use of the `parent` schema** - the hostname pair is now one umbrella with two children, so the per-child `READY` tokens below are live.
> **All six Now items were addressed and merged, as PRs #206 to #211, and none stays open** -
> the first batch in three where every Now item closed outright. One extra item closed as a
> consequence rather than as scope: the null-timestamp inventory freeze, which is the same
> mechanism as the source_key bound on a different column. No open item carries a `parent` or
> `blocked_by` edge. This light run does no re-verification; the 2026-08-23 deep-run verdicts
> stand for every item they covered.
> Source of truth is docs/backlog/ - edit items there, then re-run /roadmap. Manual edits may
> be overwritten.

Relay is a distributed task/render-farm orchestrator: three Go binaries (relay-server =
HTTP + gRPC + scheduler with an embedded React/Vite SPA; relay-agent worker that holds one gRPC
stream and runs each task as a subprocess; the relay CLI), plus an MCP server and a Perforce
source-provider. Hardening is complete - the 2026-06-10 review's findings are closed and the
seven cross-cutting invariants in CLAUDE.md are enforced and well-tested. They are written in
backend terms but hold across the stack: the generation-ordering rule was rediscovered as a
frontend bug, so `web/` changes are in scope for them too.

The active phase is the embedded web SPA build-out, governed by the `design_handoff_relay_holo`
"Holo" direction (the hi-fi `hifi3-holo-pages.jsx` layer is authoritative; `reference/` is
structure-only). The relayout is done and every surface sits on the shared
`web/src/components/holo` primitives, so what remains is net-new surfaces and backend depth, not
styling. Two rules govern the work: omit data the backend cannot supply rather than fabricating
it, and file the enabler instead; and extract a shared primitive before its third consumer.
The web-frontend theme's top twenty-two items shipped in the 2026-09-02 batch (the Jobs search,
My jobs toggle and Timeline; the Schedules strip, search and LAST JOB link; the reservations panel;
the resizable job-detail split; the structured job builder; the dialog guard and layering scale;
table accessibility; the vite/vitest upgrade), with their server filters and stats endpoints.
What remains in web is accessibility and shared-primitive debt plus the follow-ups those reviews
filed. The backend enablers in front of it are now bounds on agent-supplied input: how many
distinct workspace keys a job author can mint, the size of the key itself, and the two unpaged
reads that still run before the HTTP listener. MCP and store/schema items fill in behind.

## Now / Next / Later

### Now

- **[Settle the worker hostname unique index in one pass](docs/backlog/feature-2026-09-10-settle-the-worker-hostname-index-in-one-pass.md)** `0/2 done` `next: reg.Hostname is unvalidated and reaches a unique btree` - **it leads Now because the answer its validation half was waiting on now exists, and because scoping the pair as one piece of work is this refresh's own suggested action, performed.** Refresh 47 paired the validation child with `worker_workspaces.source_key` and said the two should settle on one answer; that slice shipped and settled it - bound in BYTES at the boundary where the value arrives, refuse rather than transform because the value is an identity, drop the row rather than failing the batch, one constructor as the sole route, and `CHECK ... NOT VALID` so planted data cannot deny the control's own deployment. The one axis that precedent does NOT cover is why this needs its own thinking: `source_key` arrives from an authenticated agent, `hostname` arrives PRE-AUTHENTICATION, so a refusal there must join `msgAuthFailed` and must not distinguish malformed from over-long from already-claimed.
  - `READY`   [reg.Hostname is unvalidated and reaches a unique btree](docs/backlog/bug-2026-08-25-hostname-is-unvalidated-and-reaches-a-unique-index.md)
  - `READY`   [Deleting a worker frees its hostname to whoever claims it first](docs/backlog/bug-2026-08-26-deleting-a-worker-frees-its-hostname-to-a-race.md)
- [`SyncStream` builds one un-batched argv element per include, with no byte bound](docs/backlog/bug-2026-09-10-syncstream-argv-length-is-unbounded.md) - bug, medium; **new this refresh, and the one new item with its measurement already in hand.** Starting the process at increasing entry counts: 442 entries gives a 32,777-character command line and starts, 450 fails with "the filename or extension is too long". So the tightest documented platform limit falls INSIDE the 512-entry cap that shipped this batch. It is a refusal rather than a truncation and there is no injection surface, but it lands after the per-entry round trips have been paid and repeats on every retry. **A tighter count bound is the wrong instrument** - path length is unbounded, so one long path blows the limit at one entry.
- [The boot admits and dispatches agent work for the whole pre-listener window while HTTP is down](docs/backlog/idea-2026-09-10-grpc-admits-work-before-the-http-listener.md) - idea, medium; **new this refresh, and it corrects a premise three other items share.** Every pre-listener item says "before the server accepts a request", which is true of HTTP and false of gRPC: `grpcSrv.Serve` and `dispatcher.Run` start ABOVE the two blocking passes and `ListenAndServe` below them. Consequences no item states - a readiness probe on `:8080` fails while `:9090` accepts, the passes contend with in-flight registrations for one 25-connection pool, and a cross-replica PATCH is reachable in that window, which is what makes the reconcile clobber hazard real rather than theoretical.
- [CLAUDE.md's canonical commands are all `make`, and `make` is not runnable on the dev machine](docs/backlog/idea-2026-09-10-make-is-not-on-path-and-msys-git-corrupts-status.md) - idea, medium; **new this refresh and a `quick win`.** Three agents in one batch each rediscovered that no `make` is on PATH and each worked out the same workaround. The reason it is worth an item rather than a footnote is that the obvious workaround is worse than the problem: putting msys64 on PATH brings an msys `git` that reports **every tracked file in the repo as modified** under `core.autocrlf` - the never-conclude-nothing-to-revert trap arriving from a direction CLAUDE.md does not cover.
- [Nothing records a workspace's size or counts evictions, so sweeper churn is unobservable](docs/backlog/idea-2026-09-04-no-workspace-size-or-eviction-instrumentation.md) - idea, medium; **promoted from Next, and two slices in this batch are why.** The exclusion feature's disk trade is explicitly negative for a mixed-exclusion stream, the p4 ceiling multiplies workspaces per stream, and that ceiling now **evicts inline** - because `Sweeper.Run` returns immediately when both its knobs are zero, so on a default agent nothing reclaims a workspace ever. A third mechanism creates churn that still cannot be measured.
- [`rawJSON` passes a stored JSON `null` through where `rawObject` normalises it](docs/backlog/bug-2026-08-27-api-rawjson-passes-null-where-rawobject-normalises.md) - bug, medium; **promoted from Next into the slot the hostname pair freed by becoming one item.** Three clients now defend themselves separately, so "every client does this" is measured rather than predicted. Fixing it at the source retires the class for all three.

### Next

- [Re-enrolling a deleted worker escapes every reservation that fenced it](docs/backlog/bug-2026-08-26-re-enrolling-a-deleted-worker-escapes-its-reservations.md) - bug, medium; reservations are an exclusion control, and re-enrollment mints a new uuid, so a deliberately fenced machine returns to the general pool. Unlike the empty-array case README already covers, a multi-worker reservation still looks healthy.
- [`rawJSON` passes a stored JSON `null` through where `rawObject` normalises it](docs/backlog/bug-2026-08-27-api-rawjson-passes-null-where-rawobject-normalises.md) - bug, medium; three clients now defend themselves separately, so "every client does this" is measured rather than predicted. Fixing it at the source retires the class for all three.
- [A validation sweep resumed after the listener would stamp stale verdicts over fresh clears](docs/backlog/idea-2026-09-10-resumed-validation-pass-can-clobber-a-fresh-clear.md) - idea, medium; **new this refresh**, and it is the remainder the deadline slice created rather than a pre-existing gap. `AdvanceScheduledJob` clears `last_error` while touching none of the three columns `RecordScheduledJobFailure` fences on, so a continuation running beside the runner can stamp a stale verdict over a fresh clear. **Do not prescribe the fence** - the naive choice trades a mistimed fire for a spurious one.
- [`store.Migrate` has no duration bound and no knob, and it gates the HTTP listener](docs/backlog/bug-2026-09-10-store-migrate-has-no-duration-bound.md) - bug, medium; **new this refresh**, and it corrects a sibling spec that called this step bounded. True of its row count, false of its duration. The exclusion is deliberate and documented, so the tension is real: the property protecting a long migration from a statement timeout is the same one leaving an operator no bound on boot and no way to tell a slow migration from a hung one.
- [Cursor values are not validated against the sort key's kind](docs/backlog/bug-2026-08-13-cursor-value-kind-not-validated.md) - bug, medium; the deep review found a sibling defect in the same slice boundary - `encodeCursorV2` omits `v` for an empty text sort value, emitting a cursor `decodeCursor` itself rejects, and an agent can force it by registering an empty hostname.
- [A revoked agent credential survives on a held connection](docs/backlog/idea-2026-08-21-revoked-agent-credential-survives-on-a-held-connection.md) - idea, medium; nothing re-checks a credential after registration and `MaxConnectionAge` is deliberately unset. Sibling of sse-revoked-token, and the two must settle on ONE staleness tolerance - deciding them apart guarantees two numbers.
- [`InventoryRowRejections` exists but no operator can read it](docs/backlog/idea-2026-09-10-publish-inventory-row-rejection-counter.md) - idea, medium; **new this batch, filed because a comment deferred to an item that did not exist.** Sibling of the enrollment-refusal counters, deferred the same way and for the same reason, so the two should settle on one answer about sections. Carry the accessor's own warning into wherever the number is read: an authenticated agent moves it at will, and the remedy is to find that agent, never to raise the bound.
- [A first boot that commits the token but fails to persist it locks the hostname out permanently](docs/backlog/idea-2026-08-25-first-boot-lockout-window.md) - idea, medium; a window the create-only rule opened. The trade is still right, but the cost was disclosed rather than closed. Note the trap: "never confirmed" is also the shape of an attacker's junk row.
- [Go CI never RUNS Windows code, only compiles it](docs/backlog/idea-2026-09-01-go-ci-never-compiles-or-runs-windows-code.md) - idea, medium; **half 1 shipped last batch and the item stays open for half 2.** `GOOS=windows` build and vet now run on the existing runner. Nothing executes those files: a `windows-latest` job running `go test ./...` is the judgement call, and its two open decisions are whether `-race` is included and the constraint that `-tags integration` stays Linux because the Windows runner has no useful Docker.
- [`UserRateLimit` and `RateLimit` panic on the first request when the limit is zero](docs/backlog/bug-2026-09-04-userratelimit-panics-on-a-zero-limit.md) - bug, low; both are exported with an unstated precondition, enforced by convention at four call sites. Not reachable from the environment - `ParseRateLimit` refuses a zero count and main is fatal on it - so this is about the exported signature. The failure is not a 500: `net/http` recovers and closes the connection, so a client sees `EOF` with no status code at all.

### Later

- [The Python SDK's `_CLIENT_TEMPLATE_RE` has drifted from Go's `clientTmplRe`](docs/backlog/bug-2026-09-04-python-client-template-regex-has-drifted-from-go.md) - bug, low; Go was tightened to refuse a leading hyphen because the value lands straight after `-t`; the SDK pattern still allows one. The server is the validator of record, so this is drift rather than a hole. The exclusion slice added a third field to the same Go/Python pair, so the unguarded-copy question is now bigger than this one regex.
- [`GET /v1/scheduled-jobs/stats`' `failing` key is documented "Not windowed" and now under-reports for a second reason](docs/backlog/idea-2026-09-10-scheduled-jobs-stats-failing-is-not-windowed.md) - idea, low; **new this refresh.** It is a live `count(*)`, so it is accurate about the table and not about the world. The sweep only ever ADDS records, so a set `last_error` stays trustworthy and only its ABSENCE lost meaning - which is the sentence that belongs beside the key.
- [PublicOnlyRoute renders the auth screens while auth status is still loading](docs/backlog/bug-2026-09-02-publiconlyroute-renders-auth-screens-during-loading.md) - bug, low; one guard in `PublicOnlyRoute`, the mirror of what `ProtectedRoute` already does.
- [TaskDag's horizontal scroller has no tab stop, role or accessible name](docs/backlog/bug-2026-09-02-taskdag-scroller-has-no-tab-stop-or-name.md) - bug, low; the same shape that put a tab stop on `Table`'s wrapper, on a route the harness already visits at 320.
- [Nothing enforces one `Handler()` call per `api.Server`](docs/backlog/idea-2026-09-04-nothing-enforces-one-handler-call-per-server.md) - idea, low; every limiter built there is a fresh bucket plus an unstoppable gc goroutine; only the search bucket is structurally unique, via `sync.Once`. Production is immune today because `buildHTTPServer` returns `*http.Server` and main never holds the `api.Server`, which is a property of the call site rather than of the type.
- [`go-ci.yml`'s `Race unit tests` comment carries change history current policy forbids](docs/backlog/idea-2026-09-04-go-ci-race-step-comment-carries-change-history.md) - idea, low; it records what a previous version of the comment said and the date it was corrected. Worth doing as a sweep of that file rather than alone; the three job-timeout comments reason about a real hazard and stay.
- [No endpoint can promote or demote an existing user](docs/backlog/feature-2026-08-09-user-role-change-endpoint.md) - feature, medium; the only open item with an admin-lockout failure mode - a deployment whose sole admin leaves has no in-product handover path.

## What moved

Refresh 48:

- **All six Now items addressed end to end and ALL SIX CLOSED.** Merged as PRs #206 to #211.
  The first batch in three where nothing stayed open by design. A seventh item closed as a
  consequence rather than as scope: the null-timestamp inventory freeze is the same mechanism as
  the `source_key` bound on a different column, so the slice that fixed one fixed both. Open
  count 198 -> 205; fourteen items filed.
- **The user asked for a dependency tree over Now and parallel execution where safe**, so the six
  ran as four lanes: two independent items, and two pairs that share a file and therefore ran
  sequentially inside their own lane. The conflict prediction held for both pairs. It was wrong
  about one thing, and the correction is now a rule: two lanes that own different ROWS of one
  README table still conflict, because partitioning by section is not partitioning by line.
- **Every one of the six specs refuted its own backlog item, and three refutations changed the
  design rather than the prose.** Read literally, the p4-ceiling item's sketch put the BASE
  workspace inside the ceiling, which would let an author with junk strings evict the shared warm
  workspace every non-exclusion task uses - the attacker's best outcome from the control. The
  `source_key` item's harm model was backwards: a bad row already failed the batch, and that IS
  the defect, because the rollback undoes the inventory DELETE too. And the deadline item's
  second acceptance criterion was not achievable as worded, since per-row disclosure means writing
  to every row the deadline declined to check.
- **A gating premise was measured against a real server before any code was written**, with an
  explicit instruction to stop if it came back the other way. The p4-ceiling item's whole framing
  is "a bound moved, not removed", which holds only if `p4 client -o -S` refuses a non-existent
  stream. Reframed away from exit status first, on the precedent that p4 exits zero on conditions
  it also reports on stderr: the question is whether the spec can be PERSISTED. It cannot - the
  refusal is at the first of the two calls, so nothing is minted.
- **Two items named a mitigation that does not exist.** The p4-ceiling item says the sweeper's
  pressure pass is "off by default"; `Sweeper.Run` returns immediately when both knobs are zero
  and the sweeper is only constructed when one is set, so on a default agent nothing reclaims a
  workspace ever. The ceiling therefore evicts inline. And a comment deferred to "its own item"
  that had never been filed, which is now filed.
- **Wrong prose about correct code remained the dominant defect, in two new shapes.** A reduction
  factor computed against the wrong denominator: an "82x" claim that is 1.5% once the per-task
  multiplier is counted, which is the same trap the file states in capitals two constants above.
  And a claim that contradicted its own header three paragraphs up - "two replicas write the same
  value" for a value computed from each replica's own clock.
- **Two reviewers disagreed on one finding and the tie was broken by arithmetic, not by vote.**
  The security lens defended the "73x" figure as honest; the invariants lens refuted it. The
  security lens had made the same error the comment made, forgetting that tasks multiply.
- **One spec assertion was impossible to write**, and only reading the call site showed it: it
  asked for an assertion that a client name does not appear in `client -i`'s argv, and that argv
  is two elements with the name on stdin.
- **`go test -overlay` cannot mutate a file a test reads with `go/parser`**, and the failure is
  silent - a mutation of `main.go`'s argument order reported `ok` with the mutant plainly present
  in the overlay file. Promoted to CLAUDE.md along with two other measurement traps that also look
  like a passing test: `strings.Repeat` of one character is TOAST-compressible, and a matcher
  discriminating on SQL text must be tested against the GENERATED statement.
- **A session limit killed six agents at once with SendMessage disabled**, so resumption was
  impossible. The recovery was clean because of the inspection, not the resume: one agent was
  mid-mutation-battery and had committed everything first, so nothing was stranded. A task
  notification cannot distinguish that from a left-applied mutation.
- **Retention cap:** the Refresh 43 block was trimmed. Retros, one per item:
  `docs/retros/2026-09-10-pgdsn-root-guard.md`, `-sync-entry-count-bound.md`,
  `-reconcile-on-startup-paging.md`, `-worker-workspace-inventory-bounds.md`,
  `-startup-validation-deadline.md`, `-workspace-exclusion-set-ceiling.md`.

Refresh 47:

- **All six Now items addressed end to end, five closed and one left open by design.** Merged as
  PRs #200 to #205. Closed: the agent-subprocess-to-`task_logs` harness, the change-password bcrypt
  bound, the sync-spec exclusion paths, the integration-guard CI gap (after eleven appended
  instances), and the per-owner schedule cap. Left open deliberately: the Windows-CI item, whose
  `windows-latest` half the item itself scopes out as the judgement call. Open count 194 -> 196.
- **Three of the six items' own prescriptions were refuted by measurement, not by argument.** The
  schedule cap's item said a single conditional `INSERT` closes the race; two sessions at cap-1 both
  committed, 3 rows against a cap of 2, so exactness needs a lock in an earlier statement. The
  bcrypt item's repro was the cheaper attack - `DeleteOtherTokensForUser` spares the caller's own
  token, so a caller can loop SUCCESSFUL changes at two cost-12 operations each, which is what
  killed its proposed skip-the-compare remedy. And the e2e item's proposal was not executable:
  `newRunner` is unexported, so the harness routes through the exported `agent.Agent`.
- **A security defect shipped in the exclusion slice and was caught by review, then reproduced
  against live p4d.** The refusal for an inert exclusion matched one phrase, `no such file`. p4 says
  it three ways and all exit zero; on a REMAPPED stream - one of the two routes the design exists
  for - it says `file(s) not in client view`, so the subtree transferred in full and the task
  reported success while README asserted the opposite. Replaced with `p4 files -m1`, which asks for
  the state instead of parsing the prose.
- **A second control was found failing OPEN on a legal server setting.** The schedule cap's query
  header named READ COMMITTED as a premise and `pool.Begin` inherited whatever the server had.
  Measured: at `repeatable read` the count never sees the competitor's row - 3 rows at a cap of 2,
  no error and no log line. The transaction now pins the level where it is relied on.
- **The dominant defect across every lane was false prose about correct code**, and in four cases a
  CORRECTION regenerated a fresh false claim. The sharpest: the slice whose whole purpose was
  "make CI run these tests" shipped five comments asserting CI does not run things it now runs,
  including a banner above a paragraph describing that same PR's work as future work. All deleted
  rather than rewritten.
- **Two claims about the complement were computed against a stale base.** The integration-guard
  census that closed its item was taken before a sibling lane of the same batch merged and added a
  tagged file; re-run after a rebase it is 139 files rather than 137, and one package had four where
  the table said three and the closing sentence said two. In a concurrent batch, rebase immediately
  before counting.
- **Nine items filed.** Seven came from the batch's own reviews: the p4-client multiplication now
  leading Now, the second unpaged pre-listener read, the boot-sweep wall-clock deadline, the `pgdsn`
  run-as-root guard (hit independently by two lanes), the zero-limit limiter panic, the
  one-`Handler()`-per-`Server` question, and the `go-ci.yml` comment history. Two more were filed
  from this refresh's own Suggested backlog actions, at the user's request immediately afterwards -
  the exclusion capability flag and the Go/Python job-spec type pair.
- **Retention cap:** the Refresh 42 block was trimmed. Retros:
  `docs/retros/2026-09-04-windows-crosscompile-ci.md`,
  `-agent-subprocess-to-task-logs-e2e.md`, `-change-password-rate-limit.md`,
  `-sync-spec-exclusion-paths-impl.md`, `-integration-guards-remaining-gaps.md`,
  `-per-owner-schedule-cap.md`.

Refresh 46:

- **All six Now items addressed end to end, four closed and two left open by design.** Merged as
  PRs #194 to #199. Closed: the react-router advisories, the sync-spec exclusion design, the
  `POST /v1/jobs` rate limit and the `?q=` cost bound. Left open deliberately: the boot-sweep item,
  whose memory half shipped and whose surviving half is a per-owner cap, and the integration-guards
  item, which shed several instances and kept the rest. Open count 191 -> 194.
- **The fork-upstreaming batch is complete**, and with its last item closed **the project carries no
  `blocked_by` edge at all.** Nothing in the backlog is blocked; every open item is ready.
- **The batch's own reviews were where the value was, and three findings could not have come from
  reading.** Mutating `main.go`'s deps literal to zero left the entire `cmd/relay-server` lane green,
  so the new rate limit could be silently off in production - the named-field trade removed the
  transposition hazard at the `api.New` boundary and not inside main's own literal. Setting
  `Retry-After` to zero survived every rate-limit test, because the assertions were `NotEmpty` or
  `secs >= 1` and `0+1 == 1` satisfies both. And the new `pg-integration` lane failed about a third
  of the time on `SQLSTATE 55006`, found by a reviewer running it seven times where the implementer
  had run it once.
- **A predicted mechanism was falsified, and the prescribed remedy went with it.** The flake was
  assumed to be concurrent `CREATE DATABASE` pressure from dropping `-p 1`; 200 create/drop cycles at
  concurrency 8 produced zero errors. The trigger is any session attached to `template1`, so `-p 1` -
  which the spec named as its own fallback - would have lowered the RATE of CREATE statements without
  reducing their count, and rate was not the variable. `TEMPLATE template0` has `datallowconn = f`, so
  the class is structurally eliminated rather than made rarer.
- **One false claim reached three documents before three independent lenses caught it**, and a fourth
  copy was found afterwards in a backlog item written during the same batch. "One `@every 1s` schedule
  is an uncapped job engine" is refused by `minScheduleInterval`, enforced on both create and PATCH,
  and README already said so 800 lines further down. Deleted rather than corrected in all four places:
  the tempting correction was wrong a second way, because the unbounded axis is the NUMBER of
  schedules.
- **Seven items filed**, five from the exclusion design (the implementation, the `source.sync` entry
  bound, the workspace-size instrumentation, the Python regex drift, the unbounded `source_key`), one
  from the rate-limit security lens (the unbudgeted cost-12 bcrypt route, which now leads Now), and
  one carrying the boot-sweep item's surviving half.
- **Retention cap:** the Refresh 41 block was trimmed. Retros:
  `docs/retros/2026-09-04-react-router-advisory-bump.md`,
  `-sync-spec-exclusion-paths-design.md`, `-pg-integration-ci-lane.md`,
  `-authenticated-route-rate-limiting.md`, `-boot-sweep-keyset-paging.md`,
  `-text-search-cost-bound.md`, and the measurement `-q-cost-measurement.md`.

Refresh 45:

- **One item closed: the fork batch's `clobber` workspace option, implemented end to end** on
  `claude/pr-merging-session-65b658` (22 packages default and under a Linux-container `-race` run;
  both p4d E2E tests real PASS). `RELAY_WORKSPACE_CLOBBER` is opt-in and default off; off is a
  byte-identical no-op, including for an operator template that already carries `Options: clobber`.
  Open count 191 -> 191 (one closed, one filed).
- **The fork-upstreaming batch is down to its last item.** Seven of eight have shipped, six of them
  today. What remains is the sync-spec exclusion paths design, which is unblocked and now leads Now.
- **The observation that paid for itself.** No fixture in the repository had ever recorded a real
  p4 `Options:` line, so the slice captured one before asserting anything. Two surprises: seven
  tokens rather than the six every document assumed (`noaltsync` is named nowhere in the tree), and
  a comment header `#  Options:     Client options:` that sits BEFORE the field - so an unanchored
  reader takes the comment, fails the token check, and silently no-ops the knob on every workspace
  in the fleet. The predicted hazard (`SubmitOptions:` contains `Options:`) was real but weaker, and
  its own fixture did not even kill the mutant until the decoy moved above the target.
- **One item filed:** `setSpecField` writes through `regexp.ReplaceAll`, so a `$` in the value
  expands rather than being written literally, and every duplicate field line is rewritten while the
  reader takes only the first. Both halves measured; neither reachable today.
- **The review shape this batch settles on.** A low-priority slice got ONE combined lens instead of
  a four-way fan-out, and it still produced a 26-mutant battery and a HIGH: a README sentence
  asserting no job spec could request clobber, which the branch's own fixture refuted, because
  `client_template` is job-spec-supplied and `p4 -t` copies the template's option field.
- **Retention cap:** the Refresh 40 block was trimmed. Retro:
  `docs/retros/2026-09-04-workspace-clobber-option.md`.

Refresh 44:

- **One item closed: the fork batch's p4 sync progress heartbeat, implemented end to end** on
  `claude/pr-merging-session-65b658` (23 files, +3914/-45; 22 packages default and under a
  Linux-container `-race` run, both p4d E2E tests real PASS with zero skips). `p4 sync` loses `-q`;
  its per-file lines are counted and never forwarded, and a five-field summary is emitted every
  `RELAY_SYNC_HEARTBEAT_INTERVAL`. Open count 190 -> 191.
- **The item's architecture was refuted before any code was written.** It proposed a heartbeat
  goroutine calling the provider's `progress` callback; that callback holds a mutex across a send
  bounded only by the agent context, so a second caller is a mutual-exclusion point and `Prepare`
  would never return - and the item was filed directly beside the open bug about that very callback
  parking. What shipped inverts it: the sync runs on a goroutine, the heartbeat loop stays on
  `Prepare`'s own.
- **Two items filed, three amended.** A `Sweeper.Run` that guards a zero interval but not a
  negative one, on an unsupervised goroutine (now unreachable through the parser this slice fixed,
  which is why it is low and why it is worth writing down); and abandoned free-disk probe goroutines
  that are bounded per sync but not per agent lifetime. Amended with what this slice measured: the
  task-logs volume cap gained a producer whose rate is a constant rather than a consequence of what
  a task prints; the wiring-guard idea gained `cmd/relay-agent` as a second unguarded package; the
  progress-parking bug gained the raised park count and the fact that an uninterruptible `statfs`
  now sits inside the handle-held window.
- **The pattern this slice adds.** Review found four defects in the slice's own new code, the
  sharpest being that a byte-offset clip of a user-chosen filename produces invalid UTF-8, which
  Postgres rejects for a `TEXT` column - dropping the whole batched chunk, stickily, so the task log
  goes silent for the rest of the sync. The repo already had that transform written correctly twenty
  lines long in another package, with its own comment naming the defect. Three of the conductor's
  own fix prescriptions were refuted with measurements. The lessons the retro promotes: read what a
  callback DOES when its consumer is gone, and grep for the sanitizer before writing one.
- **Retention cap:** the Refresh 39 block was trimmed. Retro:
  `docs/retros/2026-09-04-p4-sync-progress-heartbeat.md`.

Older refreshes: `git log --follow ROADMAP.md` and `docs/retros/`.

## Suggested backlog actions

Three of the four actions this refresh first surfaced have been performed, at the user's direction,
and are recorded here as done rather than removed - so the next reader can see what changed and why.

- **DONE - `Recently shipped` now follows the template.** It had grown to 127 entries going back
  months, roughly a third of the file, while the template scopes it to "items closed since the
  previous run". It now carries this refresh's seven, with a pointer to the durable record: each
  closed item's own `## Resolution` note, the per-item retros, and `git log --follow ROADMAP.md`. The
  file dropped from about 313 KB to 170 KB.
- **DONE - the hostname pair is scoped as one piece of work.** Filed
  [`feature-2026-09-10-settle-the-worker-hostname-index-in-one-pass`](docs/backlog/feature-2026-09-10-settle-the-worker-hostname-index-in-one-pass.md)
  as the umbrella, with `parent:` on both bugs. This is the project's first use of the `parent`
  schema, so the nesting markers now render and the roadmap's per-child `READY` tokens are live -
  `grep "READY" ROADMAP.md` answers what can be started right now. **No `blocked_by` edge was added
  between them**: either is shippable alone, and inventing an edge would make one read as waiting on
  the other. What makes them one body of work is that they must settle on the same answer.
- **DONE - an item that prescribed its own remedy in its filename was renamed.**
  `idea-2026-09-10-resumed-validation-pass-needs-a-fence` became
  [`...-can-clobber-a-fresh-clear`](docs/backlog/idea-2026-09-10-resumed-validation-pass-can-clobber-a-fresh-clear.md).
  Its body already said the fence must not be prescribed - a fenced non-match skips the advance and
  produces the spurious fire never-catch-up exists to prevent - while the identifier a reader sees
  first named that remedy. The caution now sits in the Summary rather than only in the Proposal.
  Its sibling
  [`idea-2026-09-10-reconcile-patch-clobber-window`](docs/backlog/idea-2026-09-10-reconcile-patch-clobber-window.md)
  needed no rename; its title already describes the defect.
- **DONE - the unshelves item was amended rather than closed.**
  [`bug-2026-08-29-source-unshelves-is-one-subprocess-per-entry-and-unbounded`](docs/backlog/bug-2026-08-29-source-unshelves-is-one-subprocess-per-entry-and-unbounded.md)
  now records that a fourth count bound landed, so its "if a fourth count bound is ever added" reads
  as a fifth; that the sync item's claim of being "the only one of the three with no bound at all" was
  false precisely because this item exists; and that the sync cap's per-spec-concentration property
  transfers, so any bound proposed here must not claim an aggregate reduction it cannot deliver. It is
  **sharper now, not resolved** - the remaining unbounded per-entry axis on the source spec.

Nothing to close: every item the batch addressed is in `docs/backlog/closed/` with a `## Resolution`
note, and the open directory holds no `status: closed` file.

## Agent runtime & Perforce workspaces (agent-perforce)

1. [`SyncStream` builds one un-batched argv element per include, with no byte bound](docs/backlog/bug-2026-09-10-syncstream-argv-length-is-unbounded.md) (bug, medium) - measured by starting the process: 442 entries gives a 32,777-character command line and starts, 450 fails, so the tightest documented platform limit is reached INSIDE the 512-entry cap just shipped. A refusal rather than a truncation, but it lands after the per-entry round trips have been paid and repeats on every retry. A tighter count bound is the wrong instrument.
2. [Key reconcile's task maps on the raw 16 bytes](docs/backlog/idea-2026-08-20-key-reconcile-task-maps-on-raw-uuid-bytes.md) (idea, low) - the reconcile fix re-encodes around the spelling-mismatch bug class; keying both maps on the UUID's raw byte array (which is comparable, so it works as a map key) deletes the class, drops `uuidStr`'s `fmt.Sprintf` from a caller-driven loop, and lets the requeue arm build its value directly instead of re-parsing a string it just rendered. Needs an explicit validity guard, since the current code fails closed on an invalid UUID only by accident.
3. [A late dispatch send can start an orphaned subprocess](docs/backlog/bug-2026-08-20-a-late-dispatch-send-can-start-an-orphaned-subprocess.md) (bug, low) - **new 2026-08-20**, the residue the requeue fences cannot reach. They close the window on the WRITE side; if a wedged `registry.Send` blocks and then **succeeds** after the assignment has already been ended, the agent starts the subprocess anyway, and a cancel sent ahead of it is dropped because no runner is registered yet. Bounded - the coordinator's own state stays consistent and the next reconnect's reconcile cancels it on the stale-epoch arm - but no requeue predicate can fix it. The fix is an ordering guarantee or a per-assignment fencing token the agent checks, which `tasks.sql` already names as an explicit non-goal.
4. [Remove synthetic step marker text line once consumers use step_index/step_total](docs/backlog/idea-2026-04-26-remove-synthetic-step-marker.md) (idea) - **BLOCKED**: the structured fields are dropped at persist time and never exposed by the logs API, so the text marker is the only per-step signal consumers get; deleting it now is a regression. Its persist+expose prerequisite is now a tracked item.
   depends on [persist-expose-step-index-total](docs/backlog/feature-2026-06-26-persist-expose-step-index-total.md)
5. [Pin SHA-256 of p4d/p4 binaries downloaded by test container](docs/backlog/idea-2026-05-01-p4d-binary-checksum-verification.md) (idea) - the test image curls p4d/p4 with no checksum verification; bounded (test-only) supply-chain risk.
6. [`source.unshelves` is one `p4 unshelve` subprocess per entry, unbounded](docs/backlog/bug-2026-08-29-source-unshelves-is-one-subprocess-per-entry-and-unbounded.md) (bug, medium) - **new 2026-08-29.** `1,` is two bytes, so ~524,000 entries fit a 1 MiB body - **4.5x the density of the command axis the 2026-08-29 bounds capped** - and it runs in the workspace-prepare phase, which none of those three bounds reach. Filed with its caveats intact rather than talked up: the loop returns on first failure, so the changelists must really unshelve, and it needs a Perforce-capable agent. `len(Unshelves) > 0` forces `ModeExclusive`, which is the availability half.
7. [Perforce workspace admission is quadratic in sync-path count, under the mutex](docs/backlog/bug-2026-08-29-perforce-workspace-admission-is-quadratic-under-the-mutex.md) (bug, medium) - **new 2026-08-29.** No `break` when `disjoint` goes false, so the full cross-product is paid even in the common case that decides on the first hit; ~30,273 sync entries fit 1 MiB and two such holders extrapolate to ~6.0s under `w.mu`. **The durable half is that the held set never shrinks** - `release` writes every path into `syncedPaths`, so admission cost grows with every path the agent has ever seen. The `break` is free and carries no retroactivity; a count bound is the last resort, not the first.

8. [Strip inherited RELAY_* identity variables from the agent's own process environment](docs/backlog/feature-2026-09-01-strip-inherited-relay-identity-vars.md) (feature, low) - **new 2026-09-01**, the half the identity-vars slice deliberately left. `Runner.Run` now strips the four reserved names from the job spec's `env` and the workspace provider's env, so those two principals are closed; `os.Environ()` is the only merge input still passed through unfiltered, so a value exported into `relay-agent`'s own shell is inherited by every task wherever relay has none of its own. **Not a security fix** - the agent operator already chooses the binary and owns the machine - but a stale debugging export silently poisons every link relay posts, and nothing reports it. Cheap: the predicate already exists (`isReservedIdentityNameFor`, which folds case on Windows only to match `os/exec`), so this is a third call site, not new logic. The current behaviour is PINNED by `TestRunner_AnAgentProcessEnvValueSurvivesWhenTheCoordinatorHasNone`, so the change of contract must be a visible RED rather than a silent edit.
9. [Agent-reported task progress, the enabler for the hi-fi's per-task progress bar](docs/backlog/idea-2026-09-02-agent-reported-task-progress.md) (idea, low) - **new 2026-09-02.** Relay computes no task progress (no column, no proto field, nothing on the agent), so the worker current-tasks panel omits the bar rather than fabricate it. A protocol change plus a decision about what a fraction means for an arbitrary subprocess; pairs with the per-task timing item in api-backend.
10. [README's source-workspaces table omits client_template, which validateSourceSpec enforces](docs/backlog/bug-2026-09-03-readme-source-workspaces-table-omits-client-template.md) (bug, low) - the validator enforces a field the document does not list; docs/code disagreement on the source block
11. [The provider's progress callback parks while holding the workspace](docs/backlog/bug-2026-09-03-provider-progress-parks-while-holding-the-workspace.md) (bug, medium) - **new this refresh, and the sharpest of the batch's four follow-ups.** `makePrepareProgressFn`'s callback flushes through `Runner.send`, which selects only on `sendCh` and the AGENT context - not the task's - so a wedged coordinator parks it until process shutdown while a workspace handle is held, and every later task for that stream blocks in `Acquire`. The batch closed one branch by moving `Release()` above the failure line; `[sync] starting` and the `[recover]` line still park under the hold and **cannot** be reordered away, because the sync runs under it. The fix is `sendOrAbort`, which the sibling call site already uses.
12. [sendStepMarker writes the full argv into task_logs](docs/backlog/bug-2026-09-03-sendstepmarker-writes-full-argv-to-task-logs.md) (bug, medium) - **new this refresh**, and it is the decision the lifecycle-logging slice deferred rather than a newly found hole: that slice's `argv[0]` narrowing bounds the new HOST-log surface and closes nothing here. A secret passed as a command argument is stored in `task_logs` and readable by every authenticated user - the logs endpoint is `auth`-only, with no owner scope and no admin gate. Three shapes and they are not exclusive: redact at the agent, refuse at ingest, or scope the read audience.
13. [classifyP4Error still misclassifies when p4 echoes the offending path into its own stderr](docs/backlog/bug-2026-09-03-classify-p4-error-matches-p4-echoed-path-in-stderr.md) (bug, medium) - **new this refresh**, the residual of the out-of-disk slice. Excluding the command args closed every route where the phrase arrives via argv; stderr is classified by design and p4 routinely quotes the failing path back. Severity is reduced rather than closed because the destructive remedy is gone - the message no longer sends an operator to evict other tenants' warm workspaces. The structural fix is exit codes or `-ztag`, not another substring rule.
14. [sendFinalStatus carries no ErrorMessage](docs/backlog/feature-2026-09-03-sendfinalstatus-carries-no-error-message.md) (feature, medium) - **new this refresh.** A `cmd.Start()` failure lands as a `failed` task with an empty log: the exact shape the prepare-failure batch was filed against, one branch to the left of where it was fixed. The receiving side is already built and bounds the message; the cost is that every `sendFinalStatus` call site must then decide what it carries, including the nil-argv and empty-commands paths nobody has reasoned about.
15. [An unshelve-bearing spec always re-syncs](docs/backlog/idea-2026-09-03-unshelve-spec-always-resyncs-a-baseline-workspace.md) (idea, low) - **new this refresh**, found because new progress brackets made a previously silent re-sync audible. `needsExclusive` is set by `len(Unshelves) > 0`, and an exclusive admission makes `needsSync` unconditionally true, so the baseline-match fast path is unreachable for those specs. One flag answers two questions; decide whether that coupling is intended.
16. [p4 sync reports "file(s) not in client view" on stderr and exits zero](docs/backlog/bug-2026-09-04-p4-sync-reports-not-in-client-view-and-exits-zero.md) (bug, high) - **new this refresh, and the sharpest thing the client-path review found.** `execRunner.Stream` captures stderr into a buffer read only inside `newP4CommandError`, i.e. only on a non-nil `Wait()`. `p4 sync` exits ZERO when a path matches nothing, so the whole "nothing matched" family is discarded: the progress log reads `starting` then `complete`, the baseline is recorded as satisfied, and the task runs in an empty workspace and reports SUCCESS. Measured against a live p4d. `#head` fails closed by a different route, so the silent path needs `@CL`, `@label` or `#N` - all three of which `validateSourceSpec` accepts. For a render farm that is a green task that built nothing.
17. [A subpath of a renaming remap does not resolve](docs/backlog/bug-2026-09-04-a-subpath-of-a-renaming-remap-does-not-resolve.md) (bug, medium) - **new this refresh.** `toClientPath` is a string rewrite, so it assumes the client namespace mirrors the stream namespace - true for a mainline stream and for an `import+` remap, false for a `Remapped:` view that RENAMES a subtree, which is the case the client-path slice exists to serve. Proven against the branch's own p4d fixture. **Not a regression**, since the depot form failed identically, and README states the mechanism rather than claiming coverage. The remedy is probably `p4 where`, at one round trip per sync entry; the slice's spec rejected "resolve the remap ourselves" but `toClientPath` is already such a reimplementation, just an identity one.
18. [A repaired p4 client loses its template fields](docs/backlog/bug-2026-09-04-a-repaired-p4-client-loses-its-template-fields.md) (bug, low) - **new this refresh.** `CreateStreamClient` runs on every Prepare, which is what repairs a client spec deleted out from under a surviving registry row - but `client_template` is applied cold-only, so the repair rebuilds from p4 defaults and drops every template-contributed field. Cold-only is deliberate and should stay (two `ModeShared` peers with different templates hash identically and would flip the spec against each other), so the fix is a keying question, and `BaselineHash` is a cross-process contract that costs a fleet-wide re-sync to change.
19. [A warm workspace in active use ages out](docs/backlog/bug-2026-09-04-a-warm-workspace-in-active-use-ages-out.md) (bug, low) - **new this refresh**, pre-existing but surfaced by the slice that touched the code that would fix it. `LastUsedAt` is written by the cold-path Upsert and the post-sync Mutate; a warm workspace already at its baseline skips the sync, so neither runs and the entry keeps ageing until the sweeper evicts it between two tasks that are using it. The next task pays a full re-sync. Note both `TestSweeperClaim_*` tests now structurally depend on the omission, so fixing it means re-seeding them.
20. [Sweeper.Run panics on a negative interval](docs/backlog/bug-2026-09-04-sweeper-run-panics-on-a-negative-interval.md) (bug, low) - **new this refresh, and currently unreachable, which is the point.** `Run` guards an interval of exactly zero and nothing guards a negative one, so the value reaches `time.NewTicker`, which panics - on a bare `go sw.Run(ctx)`, so the agent dies at startup. The route that reached it was the shared duration parser discarding `strconv.Atoi`'s error and multiplying without an overflow check, which the heartbeat slice fixed on the operand side. The precondition is still unguarded, and the zero case being handled is what makes the omission look like validation.
21. [Abandoned free-disk probes accumulate on a wedged volume](docs/backlog/idea-2026-09-04-abandoned-free-disk-probes-accumulate-on-a-wedged-volume.md) (idea, low) - **new this refresh.** A `statfs` has no cancellation, so the heartbeat's bounded probe renders `-` on timeout and abandons the goroutine. The latch keeps that to one per `Prepare` rather than one per tick, which was the design's point - but on a permanently wedged volume every task that prepares against it leaves one more. Strictly better than the unbounded block it replaced; the reason to write it down is that the bound is stated as closed and is closed on one axis only.
22. [setSpecField uses regex replacement semantics](docs/backlog/bug-2026-09-04-setspecfield-uses-regex-replacement-semantics.md) (bug, low) - **new this refresh, both halves measured.** The helper edits a p4 client spec through `regexp.ReplaceAll`, so its *replacement template* expands `$1` and `$0` rather than writing the value literally (`RELAY_WORKSPACE_ROOT=/mnt/$1data` silently writes `/mnt/data`), and it rewrites EVERY matching line while its readers take only the first. Neither is reachable today - the one operator-controlled value is the workspace root, and real p4 emits one `Options:` field with tab-indented continuation lines - so this is filed for the invisibility rather than the impact: both behaviours are undetectable at the call site, and the next caller passing a `$`, or reading one field while writing another, inherits them silently.
23. [Nothing records a workspace's size or counts evictions, so sweeper churn is unobservable](docs/backlog/idea-2026-09-04-no-workspace-size-or-eviction-instrumentation.md) (idea, medium) - **new this refresh**, and in Next. `InventoryEntry` carries `SourceKey`, `ShortID`, `BaselineHash`, `LastUsedAt` and no bytes; `SweepOnce` returns the ids it evicted to a caller that discards them. Neither half of the disk story is visible, which is why the exclusion design had to record its central trade as unquantified rather than estimate it.
24. [An agent capability flag for sync exclusions](docs/backlog/feature-2026-09-04-sync-exclusions-agent-capability-flag.md) (feature, medium) - **new this refresh**, Stage 2 of the exclusion design; Stage 1 shipped in [#203](https://github.com/chadmv/relay/pull/203) and left this as a documented limitation rather than a tracked one. Protobuf drops a field an agent's build does not know, so an older agent syncs the excluded path in full and reports success - no error, no warning, no log line - and on a mixed-version fleet whether a task's exclusions take effect depends on which agent it lands on. There is a second cost beyond the disk: the coordinator computes the composite `SourceKey` while an old agent registers the bare stream, so the warm bias never matches and every excluded task on that agent is a full cold sync of the whole stream, permanently. Copies the `supports_workspaces` shape with one deliberate difference - that column persists COALESCE-to-previous, and this one must COALESCE to FALSE, because an agent that stops reporting has been downgraded to a build that cannot honour exclusions and preserving a stale TRUE is exactly wrong.
25. [`BaselineHashFromAPISpec` is recomputed inside `selectWorker`'s worker loop](docs/backlog/idea-2026-09-10-baselinehash-recomputed-inside-the-worker-loop.md) (idea, low) - invariant in the worker being scored, and `warmKey` one branch above is already hoisted with a comment saying why. Needs a guard, since the result is identical either way and no behavioural test can see it.
26. [The exclusion-ceiling eviction attempt cap can refuse while an evictable candidate remains untried](docs/backlog/idea-2026-09-10-eviction-attempt-cap-can-refuse-with-a-free-slot.md) (idea, low) - deliberate bounded-work tradeoff, documented as a work bound rather than as convergence after a draft claiming it converges in one prepare was refuted by simulation. Measure how often it fires before changing it.
27. [Probing exclusion paths through an existing sibling client would close the warm case without minting](docs/backlog/idea-2026-09-10-probe-through-a-sibling-client-for-the-same-stream.md) (idea, low) - deferred with its premise named: it rests on an untested assumption about p4 view equivalence. Measure that first, and close the item rather than designing around it if the assumption does not hold.

## API authorization, auth & account lifecycle (api-auth)

1. **[Settle the worker hostname unique index in one pass](docs/backlog/feature-2026-09-10-settle-the-worker-hostname-index-in-one-pass.md)** (feature, medium) `0/2 done` `next: reg.Hostname is unvalidated and reaches a unique btree` - two bugs on one index, from the two directions a value reaches it. Neither blocks the other, so no edge between them; what makes them one body of work is that they must settle on the SAME answer and the cost of disagreeing is paid twice - two constraint changes and two passes over an index on the pre-auth registration path.
   - `READY`   [reg.Hostname is unvalidated and reaches a unique btree, making a pre-auth log site reachable](docs/backlog/bug-2026-08-25-hostname-is-unvalidated-and-reaches-a-unique-index.md) - the `source_key` slice settled the mechanism (bytes, refuse-not-transform, one constructor, `CHECK ... NOT VALID`); the axis it does not cover is that this value arrives PRE-AUTH, so the refusal must join `msgAuthFailed` and must not become an enumeration oracle.
   - `READY`   [Deleting a worker frees its hostname to whoever claims it first, at an attacker-observable instant](docs/backlog/bug-2026-08-26-deleting-a-worker-frees-its-hostname-to-a-race.md) - `autoEnrollAndRegister` takes no row lock, so its `ON CONFLICT DO NOTHING` insert succeeds the instant the delete commits. Documentation-first is a legitimate outcome, but decide it AFTER the validation answer, since a constraint change moves this race's shape.
2. [A schedule fires under its owner's identity with no credential check at fire time](docs/backlog/bug-2026-08-29-a-schedule-outlives-its-owners-credentials.md) (bug, medium) - **new 2026-08-29.** `ListEligibleScheduledJobs` filters on `enabled` and `next_run_at` only, and `fireOne` creates the job under `row.OwnerID` with nothing authenticated at fire time. Filed despite being ~300x slower than `POST /v1/jobs`, because rate is not the axis: **the load survives the remedy an operator reaches for first.** Revoking a token does not stop it and there is no user-disable flag, so the only working rungs are deleting each schedule or deleting the user.
3. [Re-enrolling a deleted worker mints a new id, so every reservation that fenced it silently stops](docs/backlog/bug-2026-08-26-re-enrolling-a-deleted-worker-escapes-its-reservations.md) (bug, medium) - **new 2026-08-26.** `reservedIDs` is consulted only as an exclusion set over live rows, so a reservation naming W keeps W out of general dispatch. Delete scrubs the id correctly; the gap is one step later, when the machine returns under a new uuid and is no longer fenced. README covers only the sub-case where the array empties - the multi-worker case is worse because it looks healthy.
4. [The delete response's counts are unpinned across the API/CLI boundary](docs/backlog/bug-2026-08-26-attribution-cleared-is-unpinned-across-the-api-cli-boundary.md) (bug, medium) - **new 2026-08-26.** Two hand-written structs with independently spelled json tags and a hand-written CLI fixture, so a rename or an added fifth count leaves both packages green while the shipped command prints `0`. `attribution_cleared` is the sharp case: it counts the run-attribution history the delete destroys, and the handler's own comment calls the body the ONLY record of that. The guard must fail on an ADDED field, not only a renamed one.
5. [The counters payload cannot say "not measured"](docs/backlog/idea-2026-08-21-counters-payload-cannot-say-not-measured.md) (idea, low) - **new 2026-08-21**, and it is `GET /v1/server/counters`'s own subject one layer down. With both gRPC caps disabled, `Accept` returns connections unwrapped and does no accounting, so every `levels` field reads 0 however many are live - and because the section is PRESENT, the payload affirmatively asserts "this control ran and stopped nothing" where the truth is "this control measured nothing". A false statement rather than an ambiguous one. Two wrong closures are named and forbidden up front: making the section absent (which would weaken `absent` for all four sections) and accounting on the disabled path (which `TestLimitListener_ZeroDisables` reddens). **Cheapest during slice 2**, because it is a contract expansion across every section and expensive once four exist.
6. [rejected_total is forgeable and its remedy helps the forger](docs/backlog/idea-2026-08-21-rejected-total-is-forgeable-and-its-remedy-helps-the-forger.md) (idea, medium) - **new 2026-08-21.** Only `worker_id` is authenticated on an incoming chunk, and nothing rate-limits task-log CHUNKS - the ingest budget bounds LINES - so any enrolled agent can drive `task_log_fence.counts.rejected_total` up smoothly and indefinitely by naming task ids it does not own. That produces exactly the "climbs steadily on a healthy fleet" signature README attributes to a too-small `RELAY_TASKLOG_TRAILING_WINDOW`, **whose documented remedy is to RAISE the window - widening the hole the window exists to bound.** No disclosure problem (an agent cannot read the endpoint and the payload carries no identifiers); the exposure is griefing and misdirection of the one signal. Merged with the priced LEFT JOIN reason-split rather than filed separately, because the split is what would resolve it.
7. [A revoked agent credential survives on a held connection](docs/backlog/idea-2026-08-21-revoked-agent-credential-survives-on-a-held-connection.md) (idea, medium) - **new 2026-08-21**, in Next. Sibling of item 9 below rather than an absorption, because the two transports have different registries, revocation paths and costs - but a pair describing one defect must not disagree about the tolerance, so agreeing on ONE is an acceptance criterion. Proposed fix is a `Registry.CloseIfPresent(workerID)` after the DB write; `Registry` already has `UnregisterIf` and `workerSender.Close`. **Scope correction from the slice**: new dispatches stop at once (revocation sets `workers.status` to `revoked` and the dispatcher selects only `online`/`stale`); what survives is in-flight execution and its `task_logs`/`tasks.status` writes, which fence on epoch and `worker_id` and never on worker status.
8. [The CLI prints an unvalidated worker hostname unescaped](docs/backlog/bug-2026-08-15-cli-prints-unvalidated-worker-hostname-unescaped.md) (bug, medium) - **new 2026-08-15.** `Hostname` is never validated at registration and is stored verbatim; `internal/cli/workers.go` then prints it (and `Name`, `Os`, `GpuModel`) with `%s`, so terminal-escape and newline injection survive from the wire all the way to an operator's terminal. The server-side log sink was fixed in the ingest-budget slice, which is what established the value is attacker-controlled at rest. The SPA and the JSON API are **not** affected - recorded so nobody re-derives it. Server-side validation at registration is the other half.
9. [Registration-time log sites sit outside the connection budget](docs/backlog/bug-2026-08-15-registration-log-sites-are-outside-the-connection-budget.md) (bug, low) - **new 2026-08-15.** `lim` is allocated after `authenticateAndRegister` returns, so the auto-enroll audit line and `finishRegister`'s inventory line are unbudgeted - both caller-forceable, both capped at one per connection only by coincidence of where the allocation happens. The clean option is to allocate at the top of `Connect` and thread it into `finishRegister`. Open question the item records rather than settles: whether the audit line should be *deliberately* exempt, since suppressing an enrollment record is its own problem.
10. [Per-stream log budget renewal is unpriced](docs/backlog/idea-2026-08-21-per-stream-log-budget-renewal-is-unpriced.md) (idea, low) - **new 2026-08-21**, and the honest residual of `MaxConcurrentStreams(1)`. That option caps CONCURRENT streams, not streams over a connection's lifetime, and `ingestLogLimiter` is allocated per `Connect` call - so open/burn/close/reopen yields a fresh 16-token burst every cycle without a second connection. What prices it is `authenticateAndRegister` running first: a valid credential plus several Postgres round trips per cycle. **The deciding half is that under `RELAY_ALLOW_AUTO_ENROLL` the credential is free**, so the price is real but it is a cost, not a bound. Sits directly beside item 4 below.
11. [A first boot that commits the token but fails to persist it locks the hostname out permanently](docs/backlog/idea-2026-08-25-first-boot-lockout-window.md) (idea, medium) - **new 2026-08-25**, and a window the create-only rule opened: `autoEnrollAndRegister` commits the row and its token hash before the agent persists the token, so a failed persist on first boot leaves the hostname claimed with a credential the agent never received - refused thereafter by **both** paths. Before the guard, the retry self-healed through `DO UPDATE`. The trade is right; the cost was disclosed rather than closed, and it is a different case from the spec's "lost its state directory". Whatever ships must state whether it optimises for stranded operators or against hostname recycling, because "never confirmed" is both.
12. [task_logs has no per-task volume cap](docs/backlog/bug-2026-08-14-task-logs-have-no-per-task-volume-cap.md) (bug, medium) - **new 2026-08-14.** The trailing-window bound that shipped 2026-08-14 closed the *duration* half of this exposure; this is the *rate* half, still fully open. A token-holding agent that keeps one task `running` can insert unbounded rows at up to 4 MB a chunk: no per-task row or byte cap, no rate limit on the gRPC ingest path (`internal/api/ratelimit.go` is HTTP middleware only), no `MaxRecvMsgSize` override, and nothing prunes the table. Needs a count subquery or a counter column plus a migration on a path that is deliberately one round trip - a different design question from the time bound, not a bigger version of it.
13. [handleTaskLog narrows the wire-supplied int64 epoch to int32](docs/backlog/bug-2026-08-12-tasklog-epoch-int32-truncation.md) (bug, low) - `int32(chunk.Epoch)` means `2^32 + E` matches a stored epoch of `E`. Latent rather than live: the assignee predicate now has to match too, and wrapping into a task you already own gains nothing. `handleTaskStatus` compares at int64 width and has no equivalent wrap. One-line guard whenever the path is next touched.
14. [A revoked token keeps receiving SSE events for the life of the held connection](docs/backlog/idea-2026-08-09-sse-revoked-token-keeps-streaming.md) (idea, low) - `/v1/events` authenticates once at connect, so archive and admin-password-reset (which delete a user's tokens expecting that to end their reach) do not terminate a held stream. More consequential now that the stream carries raw subprocess output rather than only status payloads.
15. [No endpoint can promote or demote an existing user](docs/backlog/feature-2026-08-09-user-role-change-endpoint.md) (feature, medium) - `is_admin` is settable only at `POST /v1/users`; `updateUserRequest` is name-only and nothing else mutates the role, so a deployment whose sole admin leaves has no in-product way to hand over administrative access. Needs a last-active-admin guard mirroring archive's.
16. [POST /v1/reservations returns 500 for a nonexistent user_id](docs/backlog/bug-2026-08-09-create-reservation-500-on-client-error.md) (bug, medium) - every `CreateReservation` error funnels to a single 500, so a client mistake surfaces as a server error via the users FK; the handler also skips worker-existence and inverted-window checks. `internal/api/agent_enrollments.go` has the same funnel shape, so check whether this is one handler or a shared habit.
17. [Audit log for archive/unarchive actions](docs/backlog/idea-2026-05-06-audit-log-archive-unarchive.md) (idea) - archive/unarchive are recorded nowhere; the narrow first slice of an audit trail, which the broad sibling below would subsume once an audit table lands.
18. [Audit log for privileged admin-console actions](docs/backlog/feature-2026-06-26-audit-log-admin-console-actions.md) (feature, low) - the broad audit trail (invite creation, admin password reset, worker disable/enable/revoke, plus archive/unarchive); introduces the audit table the narrow item also needs. Now that the admin console has real privileged actions shipping, this has concrete subjects to record.
19. [A token expiry check runs on two different clocks](docs/backlog/bug-2026-08-13-token-expiry-two-clocks.md) (bug, low) - **new 2026-08-13.** `GET /v1/auth/tokens` filters on Postgres `NOW()` while `BearerAuth` filters on Go `time.Now()` (`internal/api/middleware.go:32`), and `issueToken` stamps expiry from the Go clock too - so the SQL predicate is the odd one out. Under app/DB skew a caller's own token can authenticate and still be excluded from the list, making `is_current` true for zero rows.
20. [Reap expired invites and API tokens](docs/backlog/idea-2026-08-13-reap-expired-invites-and-tokens.md) (idea, low) - **new 2026-08-13.** `agent_enrollments` has a janitor (`cmd/relay-server/main.go:245-253`); neither `invites` nor `api_tokens` does, so both grow monotonically. The trap is the same NULL semantics as the read side: a reaper guarded on `expires_at IS NOT NULL` is right, and one without that guard deletes every permanent credential in the system.
21. [A failed login can leave a live token in localStorage](docs/backlog/bug-2026-08-13-failed-login-leaves-a-live-token-in-localstorage.md) (bug, low) - **new 2026-08-13**, surfaced by the cross-generation-401 review as pre-existing. `applyAuth` calls `setToken` and then awaits `GET /users/me` with no `catch`, so a login whose follow-up request fails leaves the credential written while the user is told the login failed and the status stays `anonymous`. The token is real and the next page load bootstraps into a session the user believes they never got. One-line ordering fix; the risk is a shared or public machine.
22. [Two workspace handlers reflect parseUUID's error text to the client](docs/backlog/idea-2026-08-20-workspaces-handlers-reflect-parseuuid-error-text.md) (idea, low) - **new 2026-08-20**, and it exists because the reconcile slice's own `internal/api` sweep said no caller does this and Phase 4 found two that do. Both workspace handlers pass the parser's error text to `writeError`, where the other 26 non-test `parseUUID` call sites write a fixed message. **Not a vulnerability** - `writeJSON` encodes via `json.NewEncoder`, so the reflected path segment is escaped, and its length is bounded by `MaxHeaderBytes`; the same class was already dismissed for the reservations handler. A consistency cleanup, recorded so nobody re-derives the analysis.
23. [The netlimit conn wrapper drops TCP_USER_TIMEOUT on Linux](docs/backlog/bug-2026-08-21-netlimit-conn-wrapper-drops-tcp-user-timeout.md) (bug, low) - **new 2026-08-21**, mandated by the admission plan by name and currently tracked only in a package doc comment. `Accept` must return a WRAPPING `net.Conn` because that `Close` is the only hook that can release a slot, and two grpc-go call sites type-assert past an interface: `SetTCPUserTimeout` needs the concrete `*net.TCPConn` and silently returns nil, and `channelz.GetSocketOption` needs `syscall.Conn`. Bounded - the application-layer probe still reaps a dead peer at ~40s, and both caps at zero now return the conn unwrapped - but restoring it means a build-tagged file duplicating a grpc-go internal. The item prices the do-not-wrap-at-all alternative so it is rejected on the record.
24. [The HTTP listener has none of the admission bounds the gRPC listener got](docs/backlog/bug-2026-08-23-http-listener-has-no-admission-bounds.md) (bug, medium) - **new 2026-08-23.** `buildHTTPServer` returns a bare `&http.Server{}` - no `ReadHeaderTimeout`, `IdleTimeout`, or connection cap - while `:9090` got five controls in #139. Slowloris holds a goroutine and fd per connection; nothing bounds concurrent SSE subscriptions. The `WriteTimeout`-vs-SSE interaction is the design question to record, not omit.
25. [The agent gRPC channel is plaintext and mDNS discovery trusts the first advertiser](docs/backlog/bug-2026-08-23-agent-grpc-plaintext-mdns-first-advertiser.md) (bug, medium) - **new 2026-08-23.** The agent dials with `insecure.NewCredentials()` unconditionally and `discovery.Browse` returns the first `_relay._tcp` responder, so any LAN host can advertise and harvest agent tokens, and a passive observer reads them off the wire. The whole epoch-fence edifice assumes that token is secret; README's transport-security section covers only HTTP. Distinct from the deferred CIDR-allowlist item, which is about who may become a worker, not who may impersonate the server.
26. [PATCH /v1/workers/{id} accepts max_slots 0 or negative silently](docs/backlog/bug-2026-08-23-patch-worker-max-slots-unvalidated.md) (bug, low) - **new 2026-08-23.** No bounds check and no CHECK constraint, so a typo'd `0` silently stops dispatch to the worker while its status stays `online` - the disable endpoint's effect with none of its visibility. Same validates-almost-nothing shape as the reservation-500 item, on a surface the SPA's client-side form validation was masking.
27. [After an ungraceful restart, every worker that was online stays online forever](docs/backlog/bug-2026-08-24-ungraceful-restart-leaves-workers-online-forever.md) (bug, medium) - **new 2026-08-24**, and verified by enumerating every production writer of `workers.status`: only `MarkWorkerOfflineIfEpoch` (needs a live teardown at a matching epoch) and the metrics sweeper's `SetWorkerStatus` (which skips untracked workers). After a SIGKILL neither can fire - the new process's Metrics map is empty - and `seedGraceTimersFromActiveTasks` writes no status at all, so `GET /v1/workers` reports a fleet of healthy agents connected to nothing, unbounded. The tasks ARE requeued by the seeding; the worker row is not. Same end state the finishRegister strand produced, by a different route.
28. [An agent that re-registers and fails faster than the grace window defers its own requeue indefinitely](docs/backlog/bug-2026-08-24-crash-looping-agent-defers-requeue-indefinitely.md) (bug, medium) - **new 2026-08-24.** `GraceRegistry.Start` resets to the full window on every call, so a crash-looping agent gets Cancel-then-Start each cycle and the requeue never fires; the tasks wait out the 24h watchdog, which fails them rather than re-running them. Not a regression - the pre-fix outcome was identical and worse - but it is the limit of what the finishRegister fix bought, and the shipped comment says so. The shape is [[reference_recovery_bound_must_be_time_based]]: a bound expressed per-event caps nothing when the event re-arms it.
29. [A panic on any gRPC handler goroutine terminates relay-server for the whole fleet](docs/backlog/idea-2026-08-24-panic-on-a-grpc-handler-goroutine-kills-the-server.md) (idea, low) - **new 2026-08-24.** No `recover()` and no interceptor anywhere in production code, verified; `net/http` protects the HTTP half and nothing protects the gRPC half, so one misbehaving connection is a fleet-wide outage. **No reachable panic is claimed** - filed low deliberately, as a blast-radius item. Its honest resolution may be `wontfix` in favour of the restart-reconcile item above, and the item says so.
30. [conflicting_total is forgeable by the task's own assignee, and the documented remedy helps the forger](docs/backlog/idea-2026-08-24-conflicting-total-is-forgeable-by-the-assignee.md) (idea, medium) - **new 2026-08-24**, and the second instance of a shape already filed for `task_log_fence`, with a disjoint mechanism (self-task, so the identity gate does not stop it) and a disjoint remedy space. Measured: 10,000 forged reports give `Conflicting: 10000` with everything else flat. The advertisement is now honest - README carries the caveat and a cross-check instruction - but the counter is not bounded. **Accepting and closing as wontfix is a legitimate outcome**; the item says so.
31. [A wire-keyed dedupe key lets a peer drain the shared budget and suppress its own diagnostics](docs/backlog/bug-2026-08-24-wire-keyed-dedupe-lets-a-peer-suppress-its-own-diagnostics.md) (bug, medium) - **new 2026-08-24**, reproduced twice against real Postgres. A NUL byte in chunk content makes Postgres reject at Bind BEFORE the fence's `WHERE`, so neither task id nor epoch constrains the key; 16 such chunks drain the 16-token bucket shared by every kind. Per-connection only and itself counted, so it is detectable - but the slice that closed the budget-bypass bug is what made it load-bearing, since those three lines previously fired unconditionally.
32. [Should the coordinator's own fence rejections have a section?](docs/backlog/idea-2026-08-24-failclaimedtask-is-a-ready-uncounted-fence-site.md) (idea, low) - **new 2026-08-24.** `task_status_fence` is the agent-reported path only; `Dispatcher.failClaimedTask` and `Watchdog.SweepOnce` are refused by the same statements and counted nowhere. Declined on merit rather than cost - `failClaimedTask`'s target is `dispatched` by construction, so its three-way partition degenerates to one value. **Closing this as a written decision is an acceptable outcome** and the item is framed as the question rather than the remedy.
33. [`POST /v1/jobs` echoes a raw internal error in its 500 body](docs/backlog/idea-2026-08-28-create-job-echoes-a-raw-internal-error-in-its-500-body.md) (idea, low) - **new 2026-08-28, and filed with its own motivating repro already refuted.** `handleCreateJob` answers a `CreateJobFromSpec` failure with `err.Error()` while every other 500 in the file writes a fixed string, so a pgx error would carry a constraint name, a table and a SQLSTATE to the caller. The Phase 4 security lens reported it as live via duplicate `depends_on`; `CreateTaskDependency` carries `ON CONFLICT DO NOTHING`, so that path returns no error at all. **No reachable trigger is currently known**, which is why this is `idea`/low rather than a security bug - the surviving finding is the asymmetry, and the acceptable outcome is either a fixed string or an enumeration recorded at the call site.

34. [Settle one rune set for operator-controlled text rendered to a human](docs/backlog/idea-2026-08-28-settle-one-rune-set-operator-identifiers.md) (idea, medium) - **new 2026-08-28, filed from this refresh's own suggested action, and it is the DECISION the three items around it each have to make before they can be fixed.** When relay prints text a user chose into somewhere a human reads, which characters are dangerous, what does it do with them, and at which boundary? Every one of those fixes has to answer that, so fixing them separately means answering it separately - and the answers will be nearly identical, which is worse than obviously different because nobody notices. **The divergence is measured rather than predicted**: the schedule-failure slice wrote one rune set into three packages in a single PR, because they cannot cleanly share a helper, and the best mitigation available was to make each copy's comment name the other two. Scope is four surfaces - the two hostname items and the schedrunner log sites below, plus [relay-logs-treats-a-chunk-as-a-line](docs/backlog/bug-2026-08-26-relay-logs-treats-a-chunk-as-a-line-and-forges-lines.md), which is adjacent rather than identical. **Schedule this before whichever of the four is picked up first**; it blocks none of them formally, and taking one first without it re-opens the question that item cannot avoid answering.

35. [schedrunner logs operator-controlled schedule names raw at 13 sites](docs/backlog/bug-2026-08-28-schedrunner-logs-operator-controlled-schedule-names-raw.md) (bug, low) - **new 2026-08-28, and it lands here to sit beside the two items it must be decided with.** The schedule-failure slice closed this class for every RENDERED surface - it widened two sanitizers to cover C1 controls and bidi overrides and added a third in `relayclient.Do` - and closed none of it for values the server LOGS, which reach an operator's terminal by a different route and are read with the same eyes. 13 sites interpolate `row.Name` raw; `handleCreateScheduledJob` checks only that a name is non-empty and the PATCH handler does not check it at all. One of the 13 was added by that slice and is internally incoherent, logging the sanitized failure text beside the unsanitized name - deliberately left that way, because making one line the exception among 11 siblings raises a question the file cannot answer. **Decide it together with** [cli-prints-unvalidated-worker-hostname](docs/backlog/bug-2026-08-15-cli-prints-unvalidated-worker-hostname-unescaped.md) and [hostname-is-unvalidated](docs/backlog/bug-2026-08-25-hostname-is-unvalidated-and-reaches-a-unique-index.md): three items, one rune set, one argument - deciding them apart guarantees three spellings of the same predicate. The durable fix is a property-level test (no log site interpolates a name without the helper), not 13 call-site edits.
36. [`UserRateLimit` and `RateLimit` panic on the first request when the limit is zero](docs/backlog/bug-2026-09-04-userratelimit-panics-on-a-zero-limit.md) (bug, low) - **new this refresh.** `allow` takes its over-limit branch on `len(hits) >= rl.limit`, which is `0 >= 0` on the first request of an empty window, and then indexes `hits[0]`. Both constructors are exported with that precondition unstated, enforced by convention at four call sites. Not reachable from the environment, so this is about the signature rather than a live hole. Worth knowing how it presents: `net/http` recovers the panic and closes the connection without writing, so the client sees `EOF` with no status code - worse than a 500 for anyone diagnosing from the client side, and it repeats per request rather than crashing the process.
37. [Nothing enforces one `Handler()` call per `api.Server`](docs/backlog/idea-2026-09-04-nothing-enforces-one-handler-call-per-server.md) (idea, low) - **new this refresh.** `Handler()` builds the job-submit, password-change, login and register limiters inline, so a second call doubles every one of those budgets and leaks four never-stopped gc goroutines. Measured: eleven calls gave a goroutine delta of exactly +10 while the memoized search limiter returned the identical pointer every time. Production is immune because `buildHTTPServer` returns `*http.Server` and main never holds the `api.Server` - a property of the call site, not of the type. Filed rather than commented, because a note on any one limiter would advertise a property three of its neighbours share and none of them state.

## Store schema integrity & query performance (store-schema)

1. [`CreateJobFromSpec` issues one round trip per `depends_on` entry](docs/backlog/bug-2026-08-29-createjobfromspec-inserts-one-dependency-edge-per-round-trip.md) (bug, medium) - **new 2026-08-29, and it takes this slot from the count-bounds item that closed out of it**, which had cleared `depends_on` as "no algorithmic blowup". That was right about the validator and silent about the writer: `detectCycle` is linear, and `CreateJobFromSpec` then issues one `CreateTaskDependency` round trip per ENTRY, sequentially, inside the caller's transaction. **The worst case is a TWO-task spec** - `Validate` checks each name for membership only and never dedupes, `detectCycle` handles repeats correctly and so ACCEPTS them, and the INSERT is `ON CONFLICT DO NOTHING`, so one name repeated to the body limit is ~209,700 round trips producing exactly one row. `maxTasksPerJob` does not reach it, and batching must dedupe first or a `CopyFrom` just relocates the cost.
2. [There is no backoff between a failed task and its redispatch](docs/backlog/bug-2026-08-27-no-backoff-between-a-failed-task-and-its-redispatch.md) (bug, medium) - **new 2026-08-27**, scoped out of the retry-bounds slice and the reason its cap came out low. `IncrementTaskRetryCount` returns the task to `pending`, `handleTaskStatus` immediately calls `NotifyTaskSubmitted`, the dispatcher wakes: nothing waits. So a millisecond-failing command burns its whole budget in under a second, and the retry feature's own use cases - a flaky mount, a contended license server - are exactly the faults a wait would clear and an immediate retry will not. The natural shape is a `not_before` column plus a `GetEligibleTasks` predicate, with one non-obvious consequence: a time-gated task needs something to wake an idle dispatcher when its wait expires.
3. [A database CHECK on `tasks.retries` / `tasks.timeout_seconds`](docs/backlog/idea-2026-08-27-db-check-constraint-on-retries-and-timeout-seconds.md) (idea, low) - **new 2026-08-27**, the column-level guarantee the retry-bounds slice declined. Migrations run on startup, so a plain `ADD CONSTRAINT` refuses to boot the binary on exactly the population that has the bug, and `NOT VALID` converts that loud failure into a silently stuck task. The pre-existing-row decision (clamp, fail to boot, or two-release `VALIDATE`) IS the item; the SQL is four lines. A caller-side AST guard stands in for it today, which protects the call graph rather than the column.
4. [task_logs has no retention or pruning](docs/backlog/idea-2026-08-14-task-logs-retention-and-pruning.md) (idea, medium) - **new 2026-08-14**, filed alongside the trailing-window bound. `task_logs` has existed since migration 000001 and the only deletion path is an `ON DELETE CASCADE` from `DeleteJob`, which has **no production caller** - so the table grows monotonically for the life of a deployment. Pairs with [reap-expired-invites-and-tokens](docs/backlog/idea-2026-08-13-reap-expired-invites-and-tokens.md), which is the same shape one table over. Two traps recorded up front: a reaper keyed on `created_at` eats a long-running task's early output, and a server-side writer that stamps terminal status on old rows would break exactly what the retry-resurrect slice closed.
5. [Cursor values are not validated against the sort key's kind](docs/backlog/bug-2026-08-13-cursor-value-kind-not-validated.md) (bug, medium) - **new 2026-08-13**, found independently by two review lanes and probed. `parsePage` compares a cursor's sort field to the resolved sort but never its **value kind** against `SortKind` (`internal/api/pagination.go`), so a null- or text-valued cursor on a timestamp key decodes to a zero time: the DESC arm returns an empty page while `total` still reports N - the exact "footer states a number the caller cannot page to" failure the SQL comments guard against, reached by a route SQL cannot defend. The inverse is worse: a timestamp cursor on a text key leaves the string empty, every row compares `> ''`, and the endpoint returns page one again - a paging **loop**. Affects every paginated endpoint identically.
6. [Jobs stats 24h buckets rely on updated_at finish proxy](docs/backlog/bug-2026-06-05-jobs-stats-24h-updated-at-proxy.md) (bug, low) - **its trigger has now fired without firing.** The item predicted the proxy would break once a retry endpoint re-opened terminal jobs; that endpoint shipped 2026-08-13 and the prediction was measured rather than assumed - a retried job leaves both 24h buckets while re-running and re-enters with a correct new `updated_at`, so the only effect is a transient undercount that self-corrects. Explicitly accepted in writing at the statement. The item also asserted `UpdateJobStatus` is the only writer of `updated_at`; `RecomputeJobStatus` writes it too, so the proxy survives on a narrower invariant than the one recorded. Both corrections are now in the item. Stays open on its original merits - still a proxy, still no `jobs.finished_at` - but it is no longer anyone's scheduling dependency.
7. [Persist and expose step_index/step_total on task_logs](docs/backlog/feature-2026-06-26-persist-expose-step-index-total.md) (feature, low) - add nullable step columns and thread them through AppendTaskLog (under the epoch fence) and the logs API; the prerequisite that unblocks retiring the synthetic step-marker line.
   blocks [remove-synthetic-step-marker](docs/backlog/idea-2026-04-26-remove-synthetic-step-marker.md)
8. [Task list ordering is unspecified and shifts](docs/backlog/bug-2026-08-09-task-list-ordering-has-no-tiebreaker.md) (bug, medium) - a job's tasks all share one transaction-constant `created_at` and `ORDER BY created_at` has no tiebreaker, so the order is undefined and visibly shifts as rows are updated. Blocked index-based task routing outright. The cheap tiebreaker and a meaningful declaration-order column are separable.
9. [Remove dead 'queued'/'dispatched' status vocabulary](docs/backlog/idea-2026-07-01-dead-status-vocabulary.md) (idea, low) - `CancelJobTasks` and `CountActiveJobsForSchedule` filter on statuses the schema's CHECK constraints make unreachable for those tables; cosmetic, contradicts the schema.
10. [The startup validation sweep lists every enabled schedule ahead of the HTTP listener](docs/backlog/bug-2026-08-28-boot-sweep-lists-every-schedule-ahead-of-the-listener.md) (bug, medium) - **new 2026-08-28, and it REPLACES the un-fireable-schedule item that occupied this slot for five refreshes** (closed, [#159](https://github.com/chadmv/relay/pull/159)). `ListEnabledScheduledJobs` is the statement that slice added to give the boot sweep every enabled schedule, and it is the first statement in the tree to materialize the whole enabled set with `job_spec` attached: no LIMIT, `SELECT *`, consumed into one slice synchronously before `ListenAndServe`. Every other read of that column is bounded - the list handler is paged, `ListEligibleScheduledJobs` has `LIMIT $1`, and the catch-up scan is filtered to overdue rows that a freshly created schedule cannot satisfy. Because it sits ahead of the listener the failure is a boot that never completes, which a readiness probe turns into a crash loop with no HTTP surface left to repair it from. The sweep's own comment claims it cannot stop the server booting; that is true of the per-row error path it was written for and not of the read in front of it, and correcting the comment is worth doing even if the paging is not. Two halves: keyset-page the sweep, and the quota/rate-limit decision it shares with [task-and-command-counts](docs/backlog/closed/bug-2026-08-28-task-and-command-counts-are-unbounded-multipliers.md).
11. [The dispatcher never reads job priority](docs/backlog/feature-2026-08-23-dispatcher-never-reads-job-priority.md) (feature, medium) - **new 2026-08-23.** Priority is validated, stored, sorted, and rendered as a chip, but `GetEligibleTasks` orders by `created_at` alone and `internal/scheduler/` has zero priority references - so a `high` job queues behind every earlier `normal` task. Under the no-fabrication rule the chip warrants an enabler. Intersects the task-list-ordering item (same ORDER BY family), so decide the two together, including the starvation semantics.
12. [The dispatch cycle has no LIMIT and rebuilds per-task state each tick](docs/backlog/bug-2026-08-23-dispatch-cycle-unbounded-per-tick.md) (bug, medium) - **new 2026-08-23.** `GetEligibleTasks` loads every eligible task each cycle and `selectWorker` rebuilds the reservation set and re-parses source JSON per task - O(tasks x workers) on the single dispatcher goroutine, a multi-second stall at render-farm queue depths. Any LIMIT must apply after priority ordering once item 9 lands, or high-priority work beyond the window starves.

13. [Nothing reaps auto-enrolled rows that never came back, and the schema cannot tell which they are](docs/backlog/idea-2026-08-25-ttl-reaper-for-never-reconnected-workers.md) (idea, medium) - **new 2026-08-25**, the ceiling's complement rather than its alternative. The 2026-08-25 slice bounds how many non-revoked workers can exist and removes none of the ones already there, so a deployment already hit gets no relief - and the documented remedy trades a bounded count for an unbounded table, since revoking frees ceiling budget without freeing the row or the hostname. **Lead with the schema gap**: nothing records which path created a row, so a reaper keyed on `last_seen_at` alone would delete legitimate machines. Note the trap - "never had a successful reconnect" describes an attacker's junk row and a first-boot agent whose token persist failed, and reaping the second is a recovery while reaping the first is a hostname recycler.
14. [pg_trgm index for the ?q= substring search, behind a measurement](docs/backlog/idea-2026-09-03-pg-trgm-index-for-text-search.md) (idea, low) - strpos dominates the text-search plan at 50k rows; a trigram index is the remedy, behind the same probe, since it costs every insert
15. [GET /v1/jobs?mine=true needs a dedicated owner statement before an owner index pays](docs/backlog/idea-2026-09-03-dedicated-owner-statement-before-an-owner-index.md) (idea, low) - the proposed owner-index migration was dropped because the shared filtered-list statement never used it; an owner-scoped statement first, then the index
16. [A rolling multi-replica deploy tells an agent to cancel its in-flight workspace sync](docs/backlog/bug-2026-09-04-rolling-deploy-cancels-an-in-flight-sync.md) (bug, medium) - **new this refresh, and REPRODUCED against a real Postgres rather than predicted.** Migrations run at server startup, so the first new replica widens the schema while old replicas still serve. A `preparing` row is then invisible to every old replica's copy of the assignment partition, and the loudest consequence is not the silent log loss anyone would guess at: `reconcileRunningTasks` misses the task in `serverSet` and tells the agent to CANCEL a healthy multi-hour sync. Three quieter effects share the cause - the row is not requeued on disconnect, the worker is over-issued slots, and no old replica sweeps it. An agent can extend the window but cannot open it. The generalisable half is the valuable one: `prepare_failed` and a task-level `cancelled` will both hit the same shape, and nothing in the tree currently says so.
17. [`ListGraceCandidates` is a `SELECT DISTINCT` with no LIMIT on the pre-listener boot path](docs/backlog/bug-2026-09-10-listgracecandidates-is-unbounded-before-the-listener.md) (bug, low) - the one pre-listener read no prior item had named, found by enumerating the gating path. Three narrow columns over a fleet-sized population, so far smaller than either schedule statement, and it seeds the grace timers so it cannot simply move.
18. [`store.Migrate` has no duration bound and no knob, and it gates the HTTP listener](docs/backlog/bug-2026-09-10-store-migrate-has-no-duration-bound.md) (bug, medium) - the exclusion is deliberate and documented, so the tension is real: the property protecting a long migration from a statement timeout is the same one leaving an operator no bound on boot. A timeout is probably the wrong instrument; attribution is the useful part.
19. [A PATCH between `ReconcileOnStartup`'s read and its advance loses its recomputed `next_run_at`](docs/backlog/idea-2026-09-10-reconcile-patch-clobber-window.md) (idea, low) - bounded at one fire and narrowed from O(N) to O(page) by the paging slice. **The item must not prescribe a fence**: a fenced non-match skips the advance, and a row left overdue produces exactly the spurious fire never-catch-up exists to prevent.
20. [A validation sweep resumed after the listener would stamp stale verdicts over fresh clears](docs/backlog/idea-2026-09-10-resumed-validation-pass-can-clobber-a-fresh-clear.md) (idea, medium) - the remainder the deadline slice created has nowhere to go, because `AdvanceScheduledJob` clears `last_error` while touching none of the three columns `RecordScheduledJobFailure` fences on. Same caution as the sibling: do not prescribe the fence.

## MCP server fidelity (mcp)

1. [MCP live task-log streaming via resources or streaming tool calls](docs/backlog/idea-2026-05-09-mcp-live-task-log-streaming.md) (idea) - **no longer backend-blocked**: `GET /v1/events?task_id=` shipped 2026-08-09, so a live source exists and `relayclient.StreamEvents` already consumes it (its 64 KiB scanner limit was raised for exactly these frames). What remains is the item's own open question - whether stdio transport can surface live logs without polling - and the SPA has now proven a consuming client end to end, so the frame handling is a known quantity.
2. [Wire relay_wait_for_job to SSE job events with a poll fallback](docs/backlog/idea-2026-06-25-mcp-wait-sse-subscribe.md) (idea, low) - subscribe to `GET /v1/events?job_id=` with a post-subscribe terminal re-check, keeping the adaptive poll as fallback; near-instant completion, but the adaptive poll already captured most of the win.
3. [relay://workers/{id} resource template](docs/backlog/idea-2026-06-25-mcp-workers-resource-template.md) (idea, low) - parity follow-up to the jobs/tasks templates; `readEntityByID` is reusable and `GET /v1/workers/{id}` exists.
4. [MCP advertises `since_seq` as inclusive and the SQL is exclusive](docs/backlog/bug-2026-08-26-mcp-since-seq-is-advertised-inclusive-and-is-exclusive.md) (bug, medium) - **new 2026-08-26.** The tool's own jsonschema tells a model the cursor is inclusive; `GetTaskLogsPage` is `WHERE id > $2`. A model following the advertised contract re-requests or skips a row at every page boundary. Found while confirming the MCP client was the one of four that handled the logs envelope correctly - it passes the envelope through untouched, so this is a documentation defect on the same endpoint, not a decode one. A passthrough client is structurally immune to the decode class and not to this one.
5. [The MCP tool schema does not advertise the job-spec bounds](docs/backlog/idea-2026-08-28-mcp-tool-schema-does-not-advertise-the-job-spec-bounds.md) (idea, low) - **new 2026-08-28.** `jobspec.TaskSpec` carries no `jsonschema` tags, so `relay_submit_job` and `relay_create_schedule` advertise `retries` and `timeout_seconds` as unbounded integers and a model learns the new caps only by spending a tool call and being refused. The repo's own convention is the opposite - `internal/mcp/wait.go` spells its 300s cap into the description for exactly this reason. Nothing is broken (the refusal is well-formed, carries the range, and `ErrorIsTransient` classifies it permanent), which is why it is `idea`. One thing to settle first: `jobspec` is shared by all four ingest paths, so a tag there is a tag every consumer carries, and a mirror would need a better argument than tidiness against the single-pipeline invariant.
6. [MCP labels the 1 KB `last_error` excerpt untrusted and leaves the ~1 MiB `job_spec` beside it unlabelled](docs/backlog/bug-2026-08-29-mcp-labels-the-last-error-excerpt-but-not-the-job-spec.md) (bug, medium) - **new 2026-08-29.** The defect is not a missing label, it is that the labelling **teaches the wrong rule**: a model reading a loud untrusted-input warning on one key learns the warning marks the untrusted parts, and `job_spec` - the megabyte the excerpt was derived FROM - is served raw in the same object. `readJSON` sets no `DisallowUnknownFields` and `handleCreateScheduledJob` stores the raw bytes, so an unknown key carries arbitrary prose to an admin's model with no warning at all. The count bounds do not reach it: they bound counts, this is bytes.

## Client libraries & CLI correctness (clients)

The shipped `relay` CLI, the Python SDK, `internal/relayclient` and the MCP server's client
behaviour - as distinct from the handlers they talk to. New theme as of 2026-08-27: twelve
items had accumulated with no home, split across api-auth and dev-tooling. The recurring
shape here is a client that disagrees with the handler while its own tests agree with its own
fixtures, which is how two endpoints stayed broken for three and a half months.

1. [CLI and MCP interpolate ids into request paths unescaped](docs/backlog/bug-2026-08-26-cli-and-mcp-interpolate-ids-into-request-paths-unescaped.md) (bug, medium) - **new 2026-08-26**, and the Python SDK is a third language on it. The item's own asked-for measurement is now IN it, against httpx 0.28.1, and it cuts both ways: same-host path traversal is CONFIRMED (`../../v1/users` reaches `/v1/users` with the bearer attached, and escapes a reverse-proxy sub-path), while SSRF and header injection are REFUTED (an absolute URL becomes a path segment, CR/LF/NUL raise before a socket opens). A path-shape defect, not a credential-exfiltration one - read the severity accordingly. One of the nine Python sites was escaped in the envelope slice because that slice rewrote the line and its Go upstream escapes.
2. [`internal/relayclient` bounds no response body and sets no client timeout](docs/backlog/bug-2026-08-26-relayclient-has-no-response-bound-and-no-client-timeout.md) (bug, medium) - **new 2026-08-26**, now carrying Python numbers too. Measured on the SDK: no wall-clock bound (a request completed in 14.3 s under a 0.5 s read timeout, because httpx's timeout is per socket read), no byte bound (89 KiB on the wire materialised as 31 MB, 343x), and 0.5-1 KB retained per record. **The obvious remedy does not exist** - httpx has no total-time and no response-size setting, so `Client(timeout=)` bounds neither axis; that mistake was made and corrected in the SDK's own README. The right shape is one `_read_json` chokepoint, which the item below also needs.
3. [Python SDK decode failures escape the `RelayError` hierarchy](docs/backlog/bug-2026-08-27-python-sdk-exceptions-escape-the-relayerror-hierarchy.md) (bug, medium) - **new 2026-08-27.** `json.JSONDecodeError`, `ValueError`, `KeyError` and `pydantic.ValidationError` all escape at roughly thirteen `response.json()` sites, so `except RelayError` does not cover the SDK. The realistic trigger is the same family this slice came from: a proxy or ingress returning an HTML error page with a 200. The README no longer claims otherwise; this is the code half, and it wants the same `_read_json` chokepoint as the item above.
4. [`relay logs` treats a chunk as a line and writes content raw](docs/backlog/bug-2026-08-26-relay-logs-treats-a-chunk-as-a-line-and-forges-lines.md) (bug, low) - **new 2026-08-26**, deliberately out of the envelope slice's scope and disclosed there. A low-privilege log-forgery surface rather than a cosmetic one: task output is whatever a subprocess wrote, printed with `%s`. The Python SDK is now in the same position and moved from zero exposure to full - `task_logs()` returned nothing at all for three and a half months and now returns the whole log - so settle the rendering question for both clients at once.
5. [The CLI command constructors are unpinned](docs/backlog/bug-2026-08-26-cli-command-constructor-wiring-is-unpinned.md) (bug, low) - **new 2026-08-26.** No test constructs `LogsCommand` or `SubmitCommand`, so the `os.Stdout, os.Stderr` argument pair can be transposed with the whole package green. Same-typed adjacent arguments transpose silently, and `admin_output_test.go`'s `captureStdStreams` is the precedent whose own doc comment states the reason.
6. [Six byte-identical production copies of the UUID render format](docs/backlog/idea-2026-08-26-six-copies-of-the-uuid-render-format.md) (idea, low) - **new 2026-08-26.** Drift in the CLI direction is caught by a fixture literal; drift in the SERVER direction is caught by nothing, because `internal/api.uuidStr` is unexported. Related to the `follow_job` item above, which is the same format question in a third language.
7. [The `to_spec_dict` mappers need an arity check](docs/backlog/idea-2026-08-27-hand-written-to-spec-dict-mappers-need-an-arity-check.md) (idea, low) - **new 2026-08-27.** `Job.to_spec_dict()` hand-writes four keys out of sixteen model fields and `Task.to_spec_dict()` does the same. Correct today - every omitted field is response-only - but the blind spot WIDENED from six fields to twelve in the same slice that filed this, and no test pins the partition, so a future authoring field is silently dropped from the request body with the suite green.
8. [SDK copies of server vocabularies are invisible to the lockstep guards](docs/backlog/idea-2026-08-27-sdk-copies-of-server-vocabularies-are-unregistered.md) (idea, medium) - **new 2026-08-27.** `jobs_status_vocabulary_lockstep_test.go` enumerates six sites and says "Revisit ALL OF THEM"; the Python SDK holds a seventh (`_TERMINAL_JOB_STATUSES`, consumed by `wait()`, with the identical fail-open consequence the guard already describes for its registered twin), plus an `EventType` copy and a README snippet that mints an eighth in every user's codebase. Same family as item 3 in dev-tooling: a guard that cannot see its producer.
9. [The CLI and SPA label `last_error` as coming from the stored `job_spec`, but a class of it comes from `cron_expr` and `timezone`](docs/backlog/bug-2026-08-29-cli-and-spa-label-last-error-as-coming-from-the-job-spec.md) (bug, low) - **new 2026-08-29.** `ParseSchedule` echoes `cron_expr`/`timezone` and is wrapped as `parse cron:` into `last_error`, so for that whole class both labels name a column the text never touched. The count-bounds slice corrected the same claim in the eight places it was PROSE, including the MCP `provenance` string a model reads; these two are OUTPUT, pinned by three tests across two subsystems, so that prose-only round stopped and recorded the discrepancy in the comments instead.
10. [`get_tasks` reads a raw response body, so a null body raises TypeError outside RelayError](docs/backlog/bug-2026-08-29-python-sdk-get-tasks-reads-a-raw-response-body.md) (bug, medium) - **new 2026-08-29**, the residual the fetch-all slice deliberately left. That slice routed `_fetch_all` and `_get_page` through `Page[model]` and then enumerated every decoder rather than claiming the class was shut: twelve `response.json()` sites, ten of which hand the body to a model whole, two of which hand-pick, and only this one unguarded (`errors.py`'s `_extract_message` has `isinstance` checks at every step and a `response.text` fallback). Not live against a correct server - `handleListTasks` uses `make([]taskResponse, len(tasks))` - which is exactly the provenance argument this family exists to reject. Wants the same `_read_json` chokepoint as the `RelayError`-hierarchy and `relayclient` response-bound items above.
11. [`printTaskLogs`' completeness claim counts rows written, not distinct seqs](docs/backlog/bug-2026-08-29-print-task-logs-completeness-claim-counts-rows-written.md) (bug, medium) - **new 2026-08-29**, a port that outlived its original. `task_logs` was refuted for counting `len(out)` during the envelope slice and now counts distinct seqs; the Go code it was ported FROM still uses `logProgress.rows`. A server re-serving a page behind an advancing cursor drives that count to `total` while half the log was never sent, and the operator is told the log is complete - with both the cursor and `total` chosen by the same actor. `printTaskLogs` streams and cannot build a set, but it does not need one: seqs increase monotonically along a correct walk, so an O(1) high-water counter is exact, and would also let Python retire a 35-70 MB transient set.
12. [A pagination error on the revoked-workers fallback reads as "no worker found"](docs/backlog/bug-2026-08-29-pagination-error-on-the-revoked-workers-fallback-reads-as-no-worker-found.md) (bug, low) - **new 2026-08-29**, a consequence the fetch-all slice CREATED and recorded rather than fixed. `resolveWorkerIDIn`'s soft-error branch was written for a 403 on the admin-only revoked list; now that `FetchAllPages` can refuse to page, it swallows that refusal too and `relay workers delete <hostname>` prints `no worker found with hostname "..."` - a wrong diagnosis rather than a terse one. Two review lenses judged leaving it defensible (one command, a refusal rather than a wrong action, strictly better than the pre-slice hang). Narrowing means deciding which error classes stay soft, which is its own judgement with its own tests.
13. [`relayclient.PageEnvelope` reads a dropped `next_cursor` as drained](docs/backlog/bug-2026-08-29-go-pageenvelope-reads-a-dropped-next-cursor-as-drained.md) (bug, medium) - **new 2026-08-29**, the Go peer of the item that just closed, declined in writing at that slice's spec gate. Same latent defect; `encoding/json` has no required-field mechanism, so the remedy is a `*string` plus an explicit nil check plus a decision about what `internal/mcp`'s five `PageEnvelope[map[string]any]` decode sites do with an absent key. Six `FetchAllPages` call sites, one of them the implicit `resolveWorkerIDIn` walk that four `relay workers` subcommands reach with `userLimit=0`, where `--limit` cannot bound them.
14. [`scheduledJobResponse`'s field order is an unpinned input to a redaction claim](docs/backlog/idea-2026-08-29-scheduledjobresponse-field-order-is-an-unpinned-redaction-input.md) (idea, low) - **new 2026-08-29.** The Python README now states how much of a paged body an escaping `pydantic.ValidationError` can put in a log. Part of that bound is a fact about a Go struct's declaration order, and nothing pins it. Closes either by pinning the order in `internal/api` or by dropping the specific-field claim - and the second is worth weighing, since this slice spent four verify rounds on prose in one language making checkable claims about another's source.
15. [`follow_job("")` and `relay logs ""` open the broadcast subscription](docs/backlog/bug-2026-08-30-empty-job-id-opens-the-broadcast-subscription.md) (bug, medium) - **new 2026-08-30.** An empty `?job_id=` is indistinguishable from omitting the parameter, and the broker reads that as broadcast-to-all - so a method contracted to stream ONE job's events streams every job's. Pre-existing in both clients; this slice is the first place `""`-means-broadcast is written down client-side, which is what makes it findable now.
16. [`parseUUID` formats the whole rejected input into an error nobody reads](docs/backlog/idea-2026-08-30-parseuuid-formats-the-whole-rejected-input-into-a-discarded-error.md) (idea, low) - **new 2026-08-30.** `Scan`'s reject arm formats the entire input into an error string and `parseUUID` wraps it again with `%q`; on a 1 MiB `?job_id=` that measured ~29x `Scan`'s own cost, and the error is then discarded unread. Authenticated-only and not a new asymptotic class. The obvious length pre-check is declined on the record: it would restate pgx's grammar in a second place, and the existing acceptance table is all 32/36-byte rows, so a pgx that widened `Scan` would be silently narrowed with every test green.
17. [The Python SDK interpolates ids into request paths unescaped at most sites](docs/backlog/bug-2026-08-30-python-sdk-interpolates-ids-into-paths-unescaped.md) (bug, medium) - **new 2026-08-30**, the Python surface of the class item 1 tracks for the CLI and MCP; work them together. `task_logs_page` escapes with `quote(..., safe="")` and states why; the other id-bearing paths are bare f-strings, so the one escaped site reads as if the class is handled SDK-wide. Filed separately because the fixes are per-codebase and the sibling item's file is currently git-binary-classified.
18. [The CLI reconcile assumes cancel publishes no task frames, and nothing pins that](docs/backlog/idea-2026-08-30-cancel-path-task-frame-absence-is-unpinned.md) (idea) - **new 2026-08-30**, filed by the comment retrofit: the struck census of publish sites was the only record of the property `reconcileFinalSnapshot` exists for. A task-frame publish added to the cancel path would make the reconcile double-print or go dead, all packages green.
19. [ScheduledJob's absent-means-healthy omission has no Go-side pin](docs/backlog/idea-2026-08-30-scheduledjob-absent-means-healthy-has-no-pin.md) (idea) - **new 2026-08-30**, the inverse of `TestPageEnvelope_AllThreeKeysArePresentOnAZeroValuePage`: that guard pins presence of keys a client requires, this one must pin ABSENCE of keys the SDK reads meaning into. Weigh together with item 14, which already questions cross-language claims about this same response struct.
20. [The CLI, Python SDK and MCP server do not use the task-log tail mode, and MCP passes prev_seq through as always 0](docs/backlog/idea-2026-09-02-tail-mode-for-cli-sdk-and-mcp.md) (idea, low) - **new 2026-09-02.** Only the SPA uses `order=desc`/`before_seq`; `relay logs` and the SDK still walk forward from `since_seq`, and the MCP tool's verbatim map passthrough now hands an LLM a permanent `prev_seq: 0` (its description says so as of #174). This is the passthrough-consumer shape item 8 already describes, on a new field.
21. [Expose q, mine, since and until on the CLI, MCP and Python SDK job lists](docs/backlog/feature-2026-09-03-expose-jobs-filters-in-cli-mcp-and-python-sdk.md) (feature, low) - the four #178 filters exist on the wire and in the SPA only; one item so the three clients move together
22. [The Python SDK's `_CLIENT_TEMPLATE_RE` has drifted from Go's `clientTmplRe`](docs/backlog/bug-2026-09-04-python-client-template-regex-has-drifted-from-go.md) (bug, low) - **new this refresh**, found incidentally while checking the SDK for an unrelated question during the exclusion design. Go is `^[A-Za-z0-9_.][A-Za-z0-9_.-]*$`, tightened to refuse a leading hyphen because `CreateStreamClient` places the value immediately after `-t`; the SDK is still `^[A-Za-z0-9_.-]+$`. The server is the validator of record so this is not a hole, but the SDK accepts a spec the server refuses, which defeats the point of having the pattern client-side. Exactly this theme's recurring shape. Any guard must live in a lane that runs on the commits that can break it: `python.yml` filters `paths: python/**`, so a guard placed there cannot fire on the Go commit that moves `clientTmplRe`.
23. [Nothing keeps Go's `jobspec.SyncEntry` and the Python SDK's `Sync` in step](docs/backlog/idea-2026-09-04-nothing-guards-the-go-python-job-spec-type-pair.md) (idea, medium) - **new this refresh.** Both carry three fields today and nothing asserts it; the exclusion slice added the third to each by hand. The obvious guard cannot work, which is the interesting part: `python.yml` filters on `paths: python/**`, so a guard in the Python suite would not run on the Go commit that breaks the pair - per CLAUDE.md it has to sit on the Go side and read `models.py` as data. The technique exists one type over: `TestSourceSpecToProto_SyncEntryArityMatches` pins the Go-to-proto half by reflecting over both structs and comparing `NumField`, in the default lane. Deliberately not merged with the SDK-vocabularies item: that one is about value vocabularies and registration lines, this is about a struct's field set and an arity assertion, and they should settle the shared question - how a Go-side guard reads Python - once.
24. [The Python SDK's `Sync` docstring enumerates the server-side sibling rules and is now one short](docs/backlog/idea-2026-09-10-python-sync-docstring-enumeration-is-short.md) (idea, low) - the enumeration was deleted rather than extended when the bound shipped, so nothing is false today. Recorded mainly so the next person does not re-add the census or copy the number.
25. [`_path_starts_with_slashes` raises "stream is required" for a bad path, and duplicates its own branch](docs/backlog/bug-2026-09-10-python-path-validator-raises-the-stream-message.md) (bug, low) - local-only, so the consequence is a misleading client-side error rather than a wrong acceptance. The duplicated branch carries a second, unreachable message, which is the tell that an edit did not finish.

## Web SPA correctness & feature build-out (web-frontend)

Ordered against the `design_handoff_relay_holo/` Holo design direction (the picked full-UI design):
unblocked FE-ready build-out and cheap groundwork first; backend-blocked panels/filters next;
cross-cutting hardening last. Several items need a small new backend endpoint first (flagged inline);
those backend enablers are tracked in the Backend API & server enablers (api-backend) theme below.

**Whole-app hi-fi relayout: complete** (see Recently shipped). Every shipped page/surface is on
the shared Holo primitives; the items below are net-new features and enhancements, not styling
work. New pages here (Admin, Profile, Schedule detail) get built directly on the primitives.

**Admin console: complete** - all five tabs shipped (Users, Invites, Agent enrolls, Reservations,
Server/overview), the Invites tab closing it on 2026-08-13. New pages here (per-worker panels, any
future tab) build directly on the primitives.

**The web SPA batch (2026-09-02): complete.** The ten items that led this section shipped as PRs #169
to #174 (see Recently shipped): the collapsed header nav, the pager chain, the focus pair, task-log
tail paging, the worker current-tasks panel and the Jobs Lanes view. The fourteen items filed from
its reviews sit at the end of this section under their own heading.

   depends on [job-detail-page-row-click](docs/backlog/closed/idea-2026-06-05-job-detail-page-row-click.md) (merged)
   blocks [failed-24h-stat](docs/backlog/closed/idea-2026-06-05-failed-24h-stat.md)
   depends on [schedules-stats-endpoint](docs/backlog/closed/idea-2026-06-05-schedules-stats-endpoint.md)

The four below were filed 2026-08-09 out of the table-primitive work, and kept out of it deliberately -
that PR was gated on proving visual and behavioral neutrality, so any intentional behavior change had
to be its own deliberate step. They are cheap now that the semantics live in one place.


The three below were filed 2026-08-09 from the dialog hardening. Two are constraints the new shell
relies on but does not yet enforce; the third keeps a rejected route from being re-litigated.


Filed 2026-08-12 from the profile-dropdown stacking fix. Belongs with [body-level-portal-inert-marking](docs/backlog/closed/idea-2026-08-09-body-level-portal-inert-marking.md) above - the two are the
paint-order and the inert halves of the same "a new overlay appears" problem.

   do together with [body-level-portal-inert-marking](docs/backlog/closed/idea-2026-08-09-body-level-portal-inert-marking.md); read [native-dialog-element-reconsideration](docs/backlog/closed/idea-2026-08-09-native-dialog-element-reconsideration.md) first

Filed 2026-08-12 out of the schedule detail page - the three halves it scoped out rather than
fabricate, the shared-primitive debt it deliberately took on, and one app-wide security nit it
found and fixed only on its own surface.

1. [API clients interpolate ids into request paths without encodeURIComponent](docs/backlog/bug-2026-08-12-unencoded-path-interpolation-api-clients.md) (bug, low) - `fetch` normalizes dot segments, so a crafted `:id` retargets the verb at an unrelated endpoint - `/schedules/..%2Fjobs%2F<uuid>` turns a Delete button into `DELETE /v1/jobs/<uuid>`. **15 sites across four files** (`jobs/api.ts`, `workers/api.ts`, `admin/users/api.ts`, `admin/reservations/api.ts`); `schedules/api.ts` was fixed when the hole was found. Low because it reaches only endpoints the caller can already call - with one row flagged for re-rating, since `workers/api.ts:154` interpolates a `Workspace.short_id` the server stores verbatim off the gRPC wire, so an *enrolled agent* chooses a string that lands in an *admin's* browser POST path.
2. [Schedule detail shows one next fire; the hi-fi previews five](docs/backlog/idea-2026-08-12-schedule-next-fires-preview.md) (idea, low) - `web/package.json` has no cron parser. The constraint is not the dependency, it is agreement: any JS preview must match `robfig/cron/v3` including `@every`/`@hourly` and IANA zone resolution, and a preview that silently disagrees is worse than the one honest server-supplied value shipped today. A server-side `next_fires` field is the alternative worth weighing first.
3. [A schedule's job spec is read-only on the detail page](docs/backlog/idea-2026-08-12-schedule-job-spec-editor.md) (idea, low) - `PATCH` accepts `job_spec`; the panel renders it as read-only JSON. Any editor must defer semantics to the server (single job-spec pipeline invariant), the same way the New Job form does.
4. [Audit the form-error surfaces that bypass Field](docs/backlog/idea-2026-08-13-field-error-wiring-audit.md) (idea, low) - **new 2026-08-13.** `Field` gained `role="alert"` plus `aria-describedby` during the Profile work, and moving all ten of its source consumers onto that behavior changed **zero** of their 95 tests - because none had ever asserted it. That is the finding: the gap was invisible to the suite. Eleven error surfaces across ten files render errors *outside* `Field` (page-level banners, dialog errors) and are still unannounced. Wants an audit plus a sweep test that fails when a new error surface ships without a live region, in the same spirit as the DialogShell sweep-test item.
5. [The detail-page loading / not-found / error triad is duplicated verbatim in three pages](docs/backlog/idea-2026-08-12-detail-page-state-triad-primitive.md) (idea, low) - `WorkerDetailPage.tsx:30-55`, `JobDetailPage.tsx:57-78` and now `ScheduleDetailPage.tsx`. The project's rule is extract before the third consumer; the third shipped deliberately, because extraction has to migrate two shipped pages behind a byte-identical-test refactor gate. That gate is the item.
6. [JSON responses carry no Cache-Control: no-store](docs/backlog/idea-2026-08-13-no-store-on-json-responses.md) (idea, low) - **new 2026-08-13.** No JSON response sets `no-store`, so an `Authorization`-bearing body can sit in the browser back-forward cache; the newly shipped session list is the most pointed example. `writeJSON` is a single chokepoint, which is what makes this cheap - and the item states its own rejection case, so it can be killed cheaply too.
7. [Sign-out in one tab leaves other tabs rendering authenticated](docs/backlog/idea-2026-08-13-sign-out-in-one-tab-leaves-other-tabs-authenticated.md) (idea, low) - **new 2026-08-13**, observed while fixing the cross-generation 401. Tab 1 signs out everywhere; tab 2's in-flight request 401s carrying the old token while shared localStorage is already null, so the identity fence correctly rejects it and tab 2 keeps rendering an authenticated shell. It self-heals on tab 2's next request, which carries no token and passes the fence by equality. The item states plainly that the fence is behaving correctly and must NOT be relaxed to fix this - a cross-tab signal (storage event) is the shape, not a weaker guard.
8. [The web suite goes flaky under high vitest concurrency](docs/backlog/idea-2026-08-13-web-suite-waitfor-flakiness-under-concurrency.md) (idea, low) - **new 2026-08-13**, filed as a measurement to preserve rather than a diagnosed defect. Two review lanes saw `waitFor` timeouts across unmodified files (including `PasswordTab.auth.test.tsx` and `CreateUserForm.test.tsx`) when several vitest processes competed for CPU; ~90 targeted runs at 4-20x contention could not reproduce the specific failure. Step 1 is a both-ways run with pinned `maxThreads`. The item writes down its own unfalsifiability risk, since a flake nobody can reproduce is easy to close for the wrong reason.
9. [Table minWidth magnitude is unchecked](docs/backlog/idea-2026-08-14-table-minwidth-magnitude-is-unchecked.md) (idea, low) - **new 2026-08-14**, filed from the overflow slice. `Table.minWidth` is now a REQUIRED prop, so `tsc` guarantees every consumer passes one - but nothing checks it is large enough. A literal below the sum of a consumer's fixed tracks silently reproduces the header/row desynchronization the primitive exists to prevent, and every test still passes. The precondition is also subtler than the comment first stated: free space is non-negative only if `minWidth` exceeds the fixed tracks PLUS the `fr` cells' content minimums, which holds today only because every `fr` cell carries `truncate`/`min-w-0`. Recommends a dev-only runtime assertion inside `Table` over a third source scan.
10. [The Table scroll wrapper can clip a row popover](docs/backlog/idea-2026-08-14-table-scroll-wrapper-clips-a-row-popover.md) (idea, low) - **new 2026-08-14.** `overflow-x: auto` computes `overflow-y` to `auto`, so the new wrapper is a scroll container on BOTH axes. Nothing inside any of the ten consumers is clipped today - that was audited row by row - but the audit is a point-in-time claim with no test behind it, and `UsersTable`'s 270px three-button action column is the visible future trigger. A non-portalled popover in a row cell would be clipped, and no test would notice.
11. [Embedded SPA assets ship with no cache headers, and index.html has no no-cache](docs/backlog/idea-2026-08-23-spa-assets-no-cache-headers.md) (idea, low) - **new 2026-08-23.** `embed.FS` zero mod-time means no `ETag`/`Last-Modified`, so the hashed bundle and eight fonts re-download every load, while `index.html` (which must not be cached) has no `no-cache` either. The tracked no-store item covers JSON only; decide the two together so the policies do not contradict. Asymmetric fix: immutable for hashed assets, `no-cache` for the shell.
12. [Four small SPA accessibility gaps no item covers](docs/backlog/idea-2026-08-23-four-spa-a11y-gaps.md) (idea, low) - **new 2026-08-23**, filed as one sweep item: `MetricChart`'s `aria-label` omits the current value and trend (worker telemetry sighted-only); `JobDetailPage`'s Spec/Log `role="tablist"` has no tabpanel pairing or arrow-key model; `WorkerActions`' Edit toggle lacks `aria-expanded` (the one disclosure that did not inherit UserMenu's fix); `SortControl`'s disabled-sort hint is `title`-only. Correcting the advertisement (as #124 did) is a legitimate resolution for the tablist.
13. [Slice 2 of the browser harness - run a real relay-agent so job and task lifecycle surfaces are reachable](docs/backlog/idea-2026-08-24-e2e-harness-slice-2-agent-in-harness.md) (idea, medium) - **new 2026-08-24**, and the condition on which the parent item was closed. Slice 1 covers only what is reachable with no worker connected, so `/workers/:id` is never visited, no job executes, no task reaches `running`, and the SSE task-log stream is never opened in a browser. `/workers/:id` is the page the 2026-08-13 retro flagged as having under 15px of headroom, so it is the highest-value single surface this unlocks. The hard part is determinism, not wiring: every assertion must wait on an observed state transition rather than a duration, or slice 1's zero-flake record goes.
14. [The layout overflow gate cannot distinguish fits from clipped-behind-a-scroller](docs/backlog/idea-2026-08-24-layout-overflow-gate-cannot-see-inner-scrollers.md) (idea, medium) - **new 2026-08-24**, demonstrated by mutation with all 38 tests green. Replacing `flex-wrap` on `AdminTabs` with the width-constrained scroller form clips five pills behind an unlabelled scroller and the suite does not notice. Deliberately NOT a duplicate of the header-nav bug: that is one shipped instance, this is the property of the gate that lets a second one ship. Note the refinement - `overflow-x-auto` alone does not hide the signal, because `self-start` sizes to content; only the width-constrained form absorbs it.
15. [Four reachable SPA surfaces are absent from the harness's surface list](docs/backlog/idea-2026-08-24-four-surfaces-absent-from-the-e2e-surface-list.md) (idea, low) - **new 2026-08-24.** `/register`, both non-identity profile tabs, and the task detail page. The two profile tabs are the cheap unambiguous work; `/register` is a real decision, since the one test server pins `RELAY_ALLOW_SELF_REGISTER=false` deliberately so it runs the production-default posture.
16. [A single log line has no length cap in the SPA](docs/backlog/bug-2026-08-25-spa-log-line-has-no-length-cap.md) (bug, medium) - **new 2026-08-25**, surfaced by measurement while the CRLF slice checked its new `collapseCR` for quadratic exposure. `MAX_LINES` caps how many lines are RETAINED; nothing caps how long ONE line may be, and `appendEntries` grows `partials[stream].text` until a newline arrives. Both `collapseCR` and `stripAnsi` then re-scan the whole partial once per accepted SSE frame, so a 3.3 MB partial cost seconds per call in the measurements. Note the two principals differ: whoever submits the job controls the bytes, and the operator who opens the page pays. Pre-existing and distinct from the server-side volume cap - this one is client memory and main-thread time. The existing drop-marker machinery is the precedent for making the truncation visible rather than silent.

The fourteen below were filed 2026-09-02 from the web SPA batch's six Phase 4 reviews and spec-level
scope cuts. Two are shipped user-facing bugs (the first two); the rest are the guards and follow-ups
those reviews argued for. Intake order, not priority.

17. [PublicOnlyRoute renders the auth screens while auth status is still loading](docs/backlog/bug-2026-09-02-publiconlyroute-renders-auth-screens-during-loading.md) (bug, low) - **new 2026-09-02**, the one web bug the batch sharpened: an authenticated direct visit to `/auth` or `/register` mounts the form, `autoFocus` moves focus into it, then the redirect unmounts it and focus lands on `<body>` on `/jobs`. One guard in `PublicOnlyRoute` during `loading`, the mirror of `ProtectedRoute`.
18. [TaskDag's horizontal scroller has no tab stop, role or accessible name](docs/backlog/bug-2026-09-02-taskdag-scroller-has-no-tab-stop-or-name.md) (bug, low) - **new 2026-09-02.** The DAG panel is an `overflow-x-auto` `GlassPanel` around an SVG with no focusable descendant, on `/jobs/:id`, which the harness visits at 320. Found by refuting a README claim that the only remaining scrollers were `Table`'s; `LogView`, `TaskLogPage` and two `ScheduleDetailPage` panes are the rest of the audit.
19. [The collapsed header nav dismisses on mousedown and no lane can see a touch](docs/backlog/idea-2026-09-02-collapsed-nav-touch-dismiss-has-no-lane.md) (idea, low) - **new 2026-09-02.** The panel exists for phones and the outside-dismiss route is a document `mousedown` listener copied from `UserMenu`; no Playwright project emulates touch. `pointerdown` is the likely fix, but changing it without a lane that can see the difference is an unverifiable fix, so the lane comes first.
20. [The app has no route-change focus or announcement policy](docs/backlog/idea-2026-09-02-route-change-focus-and-announcement-policy.md) (idea, low) - **new 2026-09-02**, the route the focus pair deliberately did not take alone: after sign-in the user lands on `/jobs` with focus on `<body>`, and no transition is announced. Decide once, apply in the router layer, and the auth screens' `autoFocus` folds into it.
21. [The range half of every paginated footer's toLocaleString is unpinned](docs/backlog/idea-2026-09-02-footer-range-tolocalestring-halves-unpinned.md) (idea, low) - **new 2026-09-02**, mutation-proven: no footer test ranges past row 999, so replacing the range half with plain interpolation survives the whole suite; only `total` is guarded. Also the standing locale question, which the pager chain decided one way and this item keeps open.
22. [No web surface test renders the server's unsupported-sort 400](docs/backlog/idea-2026-09-02-no-web-test-pins-an-unsupported-sort-400.md) (idea, low) - **new 2026-09-02.** `toggleSort` is now constrained at compile time and every list request uses `URLSearchParams`, so the server allow-list 400 is the backstop for a future loosening, and nothing in `web/` renders that state.
23. [Retrofit the file:line citations under web/src now that web/CLAUDE.md forbids them](docs/backlog/idea-2026-09-02-line-citation-retrofit-under-web-src.md) (idea, low) - **new 2026-09-02.** The rule landed with three fixes; a shape search matches roughly 342 lines across 107 files (lines, not distinct citations, including cross-language pointers). Promote the rule to the root CLAUDE.md, and consider a guard in `responsive.guard.test.ts`'s walk.
24. [Task-log row virtualization so the retained-line cap can rise](docs/backlog/idea-2026-09-02-task-log-row-virtualization.md) (idea, low) - **new 2026-09-02**, carved out of the closed tail item. Only worth doing with the browser harness as its lane, since jsdom performs no layout; tail-open made the cap stop being the binding constraint, which is why it waited.
25. [A failed Load earlier fetch is swallowed with no user signal, and the un-abortable fetch has no timeout](docs/backlog/idea-2026-09-02-load-earlier-failure-is-silent-and-unbounded.md) (idea, low) - **new 2026-09-02.** A 500 makes the click flash and do nothing, indistinguishable from an empty page; by design the fetch carries no `AbortSignal`, so a hung socket has no ceiling. The drop-marker machinery is the precedent for visible failure.
26. [An MSW /logs fixture that omits prev_seq fails open](docs/backlog/idea-2026-09-02-shared-task-log-fixture-helper.md) (idea, low) - **new 2026-09-02.** Every `/logs` literal now carries `prev_seq` by hand sweep; a future one that omits it leaves Load earlier enabled with nothing red. The remedy is a shared hand-written helper emitting all four keys, never the production type.
27. [No lane can render a populated worker detail page](docs/backlog/idea-2026-09-02-measure-the-populated-worker-detail-panels.md) (idea, low) - **new 2026-09-02.** The current-tasks panel added a table to a page flagged at under 15px of headroom, on an arithmetic argument only. Depends on the agent-in-harness slice, or a seeded `dispatched` row in the fixtures as the cheaper route.
   depends on [e2e-harness-slice-2-agent-in-harness](docs/backlog/idea-2026-08-24-e2e-harness-slice-2-agent-in-harness.md), or a cheaper seeded-row alternative
28. [The Jobs Lanes row always scrolls, even at 1280, where the hi-fi uses fluid columns](docs/backlog/idea-2026-09-02-jobs-lanes-fluid-columns-at-desktop.md) (idea, low) - **new 2026-09-02.** Five 280px lanes are wider than the content box at every measured width, so the fifth lane is clipped mid-word by construction; the hi-fi's `HoloLanes` is a fluid grid at desktop. A `lg:` five-column grid above the scroller.
29. [A user-adjustable cards-per-lane cap for the Jobs Lanes view](docs/backlog/idea-2026-09-02-cards-per-lane-stepper.md) (idea, low) - **new 2026-09-02.** Deferred at spec time on a five-fold row-volume argument; if wanted, the cap joins each lane's query key and is persisted beside the view choice.
30. [Perforce source builder in the New Job form builder, so a task with a source is authorable without JSON](docs/backlog/feature-2026-09-03-perforce-source-builder-in-the-new-job-builder.md) (feature, low) - the half of the job builder cut from #185: a Perforce source block the builder can type, against the same import-refusal rules; the README source table needs its `client_template` row first
31. [Selecting a task row is silent to assistive technology, and a non-name cell click does not move focus](docs/backlog/bug-2026-09-03-task-selection-is-silent-to-assistive-technology.md) (bug, low) - aria-current on an unfocused row is not announced and a non-name cell click leaves focus behind; the tasks-table slice kept it out of scope
32. [The Jobs timeline's truncation banner is not announced to assistive technology](docs/backlog/bug-2026-09-03-timeline-truncation-banner-not-announced.md) (bug, low) - the three-page walk's banner is plain text with no live region; deferred out of #184's fix rounds
33. [WorkersTable's fr-sized name and labels cells lack min-w-0, violating Table.tsx's stated precondition](docs/backlog/bug-2026-09-03-workerstable-fr-cells-lack-min-w-0.md) (bug, low) - the fr-sized name and labels cells violate Table.tsx's own stated precondition; pre-existing, found by the table lane
34. [One app-level focus-visible rule instead of per-control outlines](docs/backlog/feature-2026-09-03-shared-focus-visible-token.md) (feature, low) - three lanes each drew their own focus ring; one token in tokens.css and a base rule cover the controls nobody touched
35. [A browser e2e spec for the dialog shell: focus trap, focus restore and Escape scoping in chromium and webkit](docs/backlog/feature-2026-09-03-e2e-dialog-spec-in-both-engines.md) (feature, low) - half of the native-dialog item's reopen trigger; trap, restore and Escape scoping under a real focus manager
36. [HoloShell.tsx and its test spell two layering utilities in comments, which Tailwind v4 emits as orphan rules](docs/backlog/bug-2026-09-03-holoshell-comments-emit-orphan-layer-rules.md) (bug, low) - two class-shaped tokens in comments ship as ownerless CSS rules; the dialog guard strips comments and cannot see them
37. [Adopt the shared useDebouncedPagingGuard on SchedulesPage](docs/backlog/idea-2026-09-03-adopt-usedebouncedpagingguard-on-schedulespage.md) (idea, low) - SF and JF fixed the same debounce-window race twice in one day; the schedules page should call the hook JF extracted
38. [The job-detail split commits the mid-drag position on pointercancel instead of reverting](docs/backlog/bug-2026-09-03-pointercancel-commits-the-mid-drag-split-position.md) (bug, low) - an interrupted drag persists its mid-gesture position; the comment calls it deliberate and nobody decided it
39. [The Jobs timeline filters on created time, so a long job active inside the window but created before it is not drawn](docs/backlog/idea-2026-09-03-timeline-created-versus-active-window.md) (idea, low) - the timeline walks created_at, so a long render active inside the window but created before it is not drawn; needs an endpoint change or a merged running query
40. [ScheduleRunsPanel is the fourth panel-title and table-label duplication](docs/backlog/idea-2026-09-03-schedulerunspanel-title-label-duplication.md) (idea, low) - the fourth panel-title and table-label duplication, outside the lane that fixed three
41. [Two comment-policy leftovers from the web batch: UsersTab's debounce comment and a superlative in SchedulesTable's test](docs/backlog/bug-2026-09-03-two-comment-policy-leftovers-from-the-web-batch.md) (bug, low) - a debounce comment that says real timers and a superlative in a schedules test; each noticed by a lane that did not own the file
42. [Decide deliberately whether to adopt vitest 4's wider fakeTimers.toFake default](docs/backlog/idea-2026-09-03-evaluate-vitest-4-faketimers-default.md) (idea, low) - the vitest 2 toFake list was pinned to keep #177 behaviour-neutral; that is a decision deferred, not made

## Backend API & server enablers (api-backend)

Backend endpoints and server-side work that unblock the web SPA (and, for SSE, the MCP live-log idea).
Filed from the prior gaps pass; schedule each ahead of the UI it gates.

1. [The boot admits and dispatches agent work for the whole pre-listener window while HTTP is down](docs/backlog/idea-2026-09-10-grpc-admits-work-before-the-http-listener.md) (idea, medium) - every pre-listener item says "before the server accepts a request", and that is true of HTTP and false of gRPC. A readiness probe on `:8080` fails while `:9090` accepts, the passes contend with registrations for one pool, and a cross-replica PATCH is reachable in that window. Recording it once is cheaper than correcting each item.
2. [Revoke an unused agent-enrollment token](docs/backlog/feature-2026-06-26-agent-enrollment-revocation.md) (feature, medium) - `DELETE /v1/agent-enrollments/{id}` so an admin can revoke a leaked, unconsumed enrollment token (only a TTL sweep exists today). **Now the shipped enrollments tab's only missing capability** - it deliberately ships no revoke control rather than a dead button - so this is worth scheduling near it. Carries an integration requirement: revoking must not disturb an already-enrolled worker.
3. [Job-detail timing enrichment](docs/backlog/feature-2026-07-01-job-detail-timing-enrichment.md) (feature, low) - `GET /v1/jobs/{id}` omits `started_at`/`finished_at`, so the relaid-out job-detail header cannot show STARTED / elapsed / duration; populate the same enrichment the list already derives. Fills omitted job-detail-relayout fields.
4. [Per-task timing](docs/backlog/feature-2026-07-01-per-task-timing.md) (feature, low) - the task response carries no per-task `started_at`/`finished_at`/duration, so the job-detail tasks table has no DUR column; expose it from the task lifecycle. Fills omitted job-detail-relayout fields.
5. [Every single-job response reports total_tasks 0 and done_tasks 0](docs/backlog/bug-2026-08-13-single-job-responses-report-zero-total-tasks.md) (bug, low) - **new 2026-08-13**, filed from the job-retry review and scoped wider than proposed: `applyJobEnrichment` is called only by the list-row converters, so **five** handlers (create, detail, cancel, retry, scheduled run-now) serialize `total_tasks: 0` as a positive assertion that the job has no tasks. The retry 200 is the sharpest instance - it can carry `tasks_retried: 3` and `total_tasks: 0` in one object. Wrong in the other direction too: `web/src/jobs/api.ts` declares the fields optional and comments that the detail endpoint does not return them, which is false. `*int32` + `omitempty` beats populating five sites. A trap for the job-retry-action FE slice, which must not seed its job cache from the retry response.
6. [Decide whether owner_email and peers should carry omitempty](docs/backlog/idea-2026-08-14-owner-email-omitempty-contract.md) (idea, low) - **new 2026-08-14**, deferred deliberately during the owner_email fix rather than ridden along with it. An absent key is honest where an empty string is not, but adding `omitempty` is a **breaking change for every existing consumer of the list endpoints**, which today always receive the key, and it forces the shipped TS type optional in the same commit. Reachable in production, not theoretical: `fillOwnerEmails` degrades on a store error by logging and leaving the field empty. Must be decided **together with** [single-job-responses](docs/backlog/bug-2026-08-13-single-job-responses-report-zero-total-tasks.md), which proposes `*int32` + `omitempty` for the same reason - or one should absorb the other. The house rule is the deliverable, not the field.
7. [GET /v1/server/info - admin-only allowlist build and config facts](docs/backlog/feature-2026-08-09-server-info-allowlist-endpoint.md) (feature, low) - filed 2026-08-09 when the Server/overview tab shipped without the hi-fi's VERSION/BUILD/DB/UPTIME strip because nothing backs it. Must be a hand-written allowlist, never a redacted `os.Environ()` dump: `RELAY_DATABASE_URL`, `RELAY_BOOTSTRAP_PASSWORD`, `RELAY_ALLOW_AUTO_ENROLL` and both rate limits are excluded by construction, and acceptance is key-set **equality** against the allowlist constant, since a deny-list-shaped test fails open on the next variable added.
8. [`rawJSON` passes a stored JSON `null` through where `rawObject` normalises it](docs/backlog/bug-2026-08-27-api-rawjson-passes-null-where-rawobject-normalises.md) (bug, medium) - **new 2026-08-27**, found by a live integration lane and by nothing else. Two helpers differ by an undocumented judgment call: `rawObject` normalises `null` at two sites, `rawJSON` passes it through at five. `Job.labels` and `Reservation.selector` are confirmed reachable on a live wire; the other three are not, by inspection. Fixing it server-side retires the class for every client at once - the Python SDK has already defended itself client-side, and the SPA crashed on this once, which is why `rawObject` exists at all. `rawJSON`'s empty-bytes fallback also returns an object for ARRAY-typed columns; settle both halves together. **Amended 2026-08-30:** now also the durable home of the `_empty_on_null` null-path record (which fields arrive null on a live wire vs. defence in depth) that the Python docstring gave up under the comment policy.
9. [An explicit `"job_spec": null` bypasses the required-field guard and misreports the failure](docs/backlog/bug-2026-08-27-scheduled-jobs-null-job-spec-bypasses-required-guard.md) (bug, medium) - **new 2026-08-27**, found incidentally against a running server. `len(req.JobSpec) == 0` is false for the four-byte literal `null`, `json.Unmarshal` no-ops on JSON null, and `ValidateJobSpec` then returns `"name is required"` - which reads as the top-level schedule name failing when the name was supplied and correct. Two defects stacked, and the misleading message is the one that costs debugging time; `jobspec.Validate` is shared by REST, CLI, MCP and schedrunner, so the misattribution is not local to this handler.

10. [run-now is step 1 of the documented remedy ladder and is a no-op on the signal it points at](docs/backlog/bug-2026-08-28-run-now-neither-clears-nor-records-the-failure.md) (bug, medium) - **new 2026-08-28**, and the sharpest of the three the schedule-failure slice disclosed rather than absorbed. `handleRunScheduledJobNow` validates, creates the job, commits, and never touches `scheduled_jobs` - so it neither clears `last_error` on success nor records it on failure, while README, the SPA panel and the CLI all name it as the FIRST thing to try on a failing schedule. The asymmetry is what makes it worth fixing rather than documenting: a successful run-now performs exactly the event `AdvanceScheduledJob`'s own comment cites to justify clearing ("the only event that proves the stored spec both validates and inserts"), so the evidence is already in hand and is discarded. Bites hardest on a spec repaired by any route other than PATCH - a binary upgrade that relaxes a rule, or a direct-SQL repair - where the chip stays up until the next scheduled fire, a month on `@monthly`. The 400 path is a genuine open question rather than an oversight: recording there would let anyone with run-now access write the field at will, which is a different trust story from the schedrunner writing it. Decide it; do not assume it.
11. [Every list handler's limit+1 over-fetch and buildPage routing is pinned by nothing](docs/backlog/idea-2026-08-30-list-overfetch-and-buildpage-routing-is-unpinned.md) (idea) - **new 2026-08-30**, filed by the comment retrofit. Both SDK page-walk caps document "reaching the cap on a LIST means the server is misbehaving", which rests on a universal split across two surfaces: the `+ 1` in every list query's `LIMIT` and `buildPage`'s hasMore trim. A future list handler that pages without the over-fetch silently breaks the clients' conclusion; the remedy is a structural guard over both surfaces.

12. [Reject env keys containing `=` or NUL in `jobspec.Validate`](docs/backlog/feature-2026-09-01-validate-env-keys-in-the-job-spec-pipeline.md) (feature, medium) - **new 2026-09-01.** `Runner.Run` skips any task env key containing `=`, because such a key is a different string to the reserved-name predicate and the same variable to `os/exec`. That guard is a security control and stays; the gap is that nothing on the ingestion side knows the rule, so `jobspec.TaskSpec.Env` accepts the key, it is stored, dispatched, and then **silently discarded on the agent** with no log line and nothing the submitter can observe. A NUL key is worse than silent: `Cmd.Start` fails before `os.StartProcess` and the runner turns any Start failure into `TASK_STATUS_FAILED` with an EMPTY `ErrorMessage`, so one bad byte fails the whole task with zero diagnosis. Sibling of [scheduled-jobs-null-job-spec](docs/backlog/bug-2026-08-27-scheduled-jobs-null-job-spec-bypasses-required-guard.md) - both are `jobspec.Validate` accepting a shape it should name. **Carries a retroactivity hazard the item states rather than leaves to be found:** `schedrunner` re-validates a stored scheduled job's spec at fire time, so tightening this breaks a stored spec on a cron at 03:00 rather than in the response to the request that introduced it. Decide the stored-data handling before enforcing.
13. [Per-worker activity aggregate (jobs or tasks today, failures, average duration)](docs/backlog/feature-2026-09-01-worker-activity-aggregate.md) (feature, low) - **new 2026-09-01**, the half of the worker-detail activity item that did not ship: a per-worker 24h aggregate needs a partial index on terminal tasks plus a migration, a windowed statement, and a product decision (relay assigns tasks, not jobs, to workers, so the hi-fi's "Jobs today" label is wrong as written). The `Jobs today` KPI stays a placeholder pointing here.
14. [Per-worker task history (terminal tasks for a worker)](docs/backlog/feature-2026-09-01-per-worker-task-history.md) (feature, low) - **new 2026-09-01.** `GET /v1/workers/{id}/tasks` returns only the active partition; widening it was refused at spec time because the only per-worker index is partial over `('dispatched','running')`. Shares item 12's index; extend the route with an allow-listed `?status=` rather than adding a second one.
15. [A server-side task-log export that streams a full log as text](docs/backlog/idea-2026-09-02-task-log-export-endpoint.md) (idea, low) - **new 2026-09-02**, carved out of the closed tail item. Log content is raw subprocess output, so this must not be more permissive than the paged read; and a byte-exact export is foreclosed, since the agent normalises CR LF before a chunk is sent.
16. [jobs.go carries thirteen partial hand-written store.Job literals](docs/backlog/idea-2026-09-02-jobs-go-hand-written-store-job-copies.md) (idea, low) - **new 2026-09-02**; the worker-tasks slice avoided a fourteenth by using two statements instead of a JOIN, which sidestepped the class without closing it. Every copy drops any column `jobs` gains; a `NumField` arity guard or `sqlc.embed` closes them as a set. Go sibling of the clients theme's `to_spec_dict` arity item.
17. [A list cursor carries no filter fingerprint, so a cursor minted under one filter is accepted under another](docs/backlog/idea-2026-09-03-cursor-carries-no-filter-fingerprint.md) (idea, medium) - a cursor is validated against the sort only, so one minted under a filter is accepted under another; the SPA guards it client-side and nobody else is protected
18. [GET /v1/jobs?scheduled_job_id= ignores a ?status= given alongside it](docs/backlog/bug-2026-09-03-scheduled-job-id-branch-ignores-status.md) (bug, medium) - the two parameters are accepted together and one is silently dropped; a 400 or a threaded predicate, either with a test
19. [The NUL-byte and malformed-query rejection covers parsePage endpoints only](docs/backlog/bug-2026-09-03-nul-and-malformed-query-guard-covers-parsepage-only.md) (bug, medium) - five handlers still read `r.URL.Query()` directly and two pass a value toward the database, the path that 500ed on `%00` before #178
20. [Run now never advances a schedule's last_job_id, so LAST JOB points at the previous scheduled fire](docs/backlog/bug-2026-09-03-run-now-does-not-advance-last-job-id.md) (bug, medium) - the LAST JOB cell #182 shipped points at the previous scheduled fire after a manual run
21. [The /stats endpoints ignore query strings, and the scheduled-jobs stats read two snapshots](docs/backlog/idea-2026-09-03-stats-endpoints-ignore-query-strings.md) (idea, low) - a filter passed to a stats endpoint is ignored and the answer looks filtered; two snapshots behind one strip
22. [List-filter remainder - multi-value status and label filters on jobs and workers, substring q on users](docs/backlog/idea-2026-09-03-list-filters-remainder-status-labels-users-q.md) (idea, low) - the unshipped part of the closed list-filters umbrella: multi-value status, label filters, users `?q=`
23. [`GET /v1/scheduled-jobs/stats`' `failing` key is documented "Not windowed" and now under-reports for a second reason](docs/backlog/idea-2026-09-10-scheduled-jobs-stats-failing-is-not-windowed.md) (idea, low) - a live `count(*)`, so accurate about the table and not about the world. The sweep only ever ADDS records, so a set `last_error` stays trustworthy and only its ABSENCE lost meaning.
## Development tooling & agent team (dev-tooling)

The project's own build/verify pipeline, as distinct from relay's runtime. New theme as of
2026-08-09; the dev-tooling items presently filed under web-frontend (the vite/vitest upgrade,
the Playwright harness) stay there for now rather than churning two sections.

The theme's first item (the reviewer agent's unreachable review skills) closed 2026-08-09; see
Recently shipped. The dev-tooling items currently filed under web-frontend (the vite/vitest
upgrade, the Playwright harness) belong here whenever either is next touched.

1. [CLAUDE.md's canonical commands are all `make`, and `make` is not runnable on the dev machine](docs/backlog/idea-2026-09-10-make-is-not-on-path-and-msys-git-corrupts-status.md) (idea, medium) - `quick win`; hit independently by three agents in one batch. The obvious workaround is worse than the problem: putting msys64 on PATH brings an msys `git` that reports every tracked file as modified under `core.autocrlf`.
2. [ClaimTaskForWorker fixtures bind a NULL assigned_at](docs/backlog/idea-2026-08-20-claimtaskforworker-fixtures-bind-a-null-assigned-at.md) (idea, low) - **new 2026-08-20.** Migration 000021 added `AssignedAt` to `ClaimTaskForWorkerParams`, and a keyed Go literal without the new field still compiles and binds SQL NULL. The one production call site sets it and is pinned by its own test; the test fixtures largely do not, so they build `dispatched` rows with `assigned_at IS NULL` - a shape production can no longer produce and one the watchdog's absolute arm can never match. No live consequence today; the cost is a future watchdog test written from one of those fixtures that passes while asserting nothing. **The fix is the fixture, not the field** - do not churn the literals.

3. [Generalize the env-to-field wiring guard](docs/backlog/idea-2026-08-14-generalize-the-env-to-field-wiring-guard.md) (idea, low) - **new 2026-08-14.** The trailing-window slice added a `go/ast` structural guard asserting `cmd/relay-server/main.go` actually assigns the parsed env value into `Handler.TrailingLogWindow` - proven necessary by mutation, since deleting the wiring block left **all 20 packages green** and the env var silently dead. `agentHandler.Metrics` and `AllowAutoEnroll` have the identical untested seam, as do `RELAY_TELEMETRY_WINDOW` and `RELAY_WORKER_GRACE_WINDOW`. One generalized guard beats four copies, and the shipped guard's own comment already points here - not filing would leave a source comment aimed at nothing. Note the project's extract-before-the-third-consumer rule just fired on the cursor-pager item; this is the same rule, one layer down.
4. [51 default-lane fixture bodies encode through the CLI's own response structs](docs/backlog/idea-2026-08-27-cli-default-lane-fixtures-encode-through-their-own-decoder.md) (idea, medium) - **new 2026-08-27**, the residual half of the item closed this refresh. The real-server lane catches server-shape drift and sits BESIDE the fast fixtures rather than replacing them, so the fixtures' own vacuity is untouched: a body marshalled from `PageEnvelope[workerResp]` or `Encode(jobResp{...})` agrees with the decoder by construction, on both the envelope keys and the item fields, and can never detect drift in either direction. **The count is the interesting part.** Four attempts produced 19, 29, 42 and finally 51, because every wrong one grepped for `Encode(<cliType>{` and `logs_test.go`'s `fakeJobSnapshotServer` routes nine call sites' literals through a single `Encode(bodies[i])` no such grep can see. 23 of the 51 are in that one file - the file CLAUDE.md's routing rule holds up as the pattern to copy. Prefer a structural guard that caps the number over a mass rewrite; treat a parser guard as the expensive fallback this repo has measured it to be.
5. [The CLI lane's shared-service mode leaks `relaytest_` databases on abnormal exit](docs/backlog/bug-2026-08-27-cli-lane-leaks-test-databases-on-abnormal-exit.md) (bug, low) - **new 2026-08-27.** `t.Cleanup` is the only thing anywhere that drops the per-test database, so Ctrl-C, a `cancel-in-progress`, or a `timeout-minutes` kill strands one. Container mode has no equivalent gap because Ryuk reaps it. Two things lift it above a shrug: the mode is advertised for local use against the long-lived `relay-postgres` container, so leaks accumulate across sessions with no documented cleanup; and a stranded database is not empty - it holds an `is_admin` user with a cost-4 bcrypt hash of a hardcoded password and a token row with `ExpiresAt` NULL. **The obvious fix is wrong as stated**: a startup sweep of `relaytest_%` would break two concurrent runs against one server, so this needs design rather than a one-liner, and "document a manual cleanup and accept the leak" is a legitimate outcome.
6. [The CLI lane never crosses a list page boundary](docs/backlog/idea-2026-08-27-cli-lane-never-crosses-a-list-page-boundary.md) (idea, low) - **new 2026-08-27**, and it is what stops the new lane from covering the Go half of the `_fetch_all` defect leading Now. The lane exercises the LOG cursor across a real 201-row boundary; every list in it holds one to three rows against a 200-row `PageRequestLimit`, so `FetchAllPages` returns on page one every time and renaming `page[T].NextCursor` survives the whole suite. One test that seeds >200 rows closes it. Worth checking in the same sitting: `--json` on any list path encodes a bare array because `FetchAllPages` unwraps the envelope, so `next_cursor` and `total` never reach `relay list --json` at all.

7. [internal/api's counter payload guards are proven against fixtures, not producers](docs/backlog/idea-2026-08-24-counter-payload-guards-check-fixtures-not-producers.md) (idea, medium) - **new 2026-08-24**, and MEASURED rather than reasoned. Every `counterPayloadAllowList` predicate and both payload walks run against fakes whose values are literals in `internal/api/server_counters_test.go`; mutating `internal/scheduler`'s producer to emit `"build-agent-07.corp.example
10.0.0.7"` as every key left `internal/api` green. `internal/api` structurally cannot fix this - the producing packages import it. Slice 4 closed it for `watchdog` only, in `cmd/relay-server`. **Not the same gap as item 3**: fixing the CI lane makes integration-tagged guards run, and still leaves these predicates checking fixtures.
8. [The watchdog sweep counters are never driven by real ListOverdueAssignedTasks rows](docs/backlog/idea-2026-08-24-watchdog-counters-never-driven-by-real-rows.md) (idea, low) - **new 2026-08-24.** Three real-Postgres watchdog sweeps already exist in `internal/worker/handler_watchdog_e2e_integration_test.go` and assert nothing about `CounterSnapshot()`. No live defect - the scan requires `worker_id IS NOT NULL` and `uuidStr` renders canonically - but the expensive fixture is already built and the assertion is a few lines. Same family as item 1: a fixture agreeing with itself rather than with the SQL.
9. [The enrollment refusal counters exist but no operator can read them](docs/backlog/idea-2026-08-25-publish-enrollment-refusal-counters.md) (idea, medium) - **new 2026-08-25**, de-scoped at plan time rather than missed: `internal/api/server_counters.go` is 574 lines and its test 1355, so a sixth section is comparable to the guards the slice existed to ship. Refusals are deliberately never logged, so a refused enrollment currently produces **zero** server-side output. Two things must ride along or the payload ships a wrong contract: the section is `enrollment`, not `auto_enroll` (`credential_live_total` fires only on the token path, and the Go type was renamed for exactly this), and the `fleet_at_ceiling` aliasing - at capacity every refusal is recorded there, including claimed-hostname retries, so `hostname_claimed_total` goes to zero precisely when an operator is triaging. That was observed empirically, not reasoned about.
10. [web-ci.yml runs the last Node-20 action majors and pins an unverified Node version](docs/backlog/idea-2026-08-24-web-ci-node-20-actions-and-unverified-node-version.md) (idea, medium) - **new 2026-08-24**, from the deprecation annotation on the workflow's first green run. Three of its four actions are a major behind their siblings in the same repo, and `node-version: 22` type-checks against an `@types/node` several majors ahead of the runtime. Filed rather than fixed deliberately: a wrong guess at a replacement major breaks CI for everyone, and the run was green. **Do not hardcode a target major from memory** when acting on it.

11. [Long -count=N integration runs trip the 15m timeout inside Docker teardown](docs/backlog/bug-2026-08-26-integration-lane-times-out-on-docker-teardown.md) (bug, low) - **new 2026-08-26.** The stack is entirely in `go-winio` during testcontainers' `ContainerRemove`, after several hundred ephemeral containers in an hour. Worth an item only because of how it RENDERS: a bare `panic:`/`FAIL` banner with no test name, indistinguishable from a real failure once piped through `tail` - which is exactly what happened and cost a round before a baseline run and four full-capture runs settled it.
12. [The mutation harness does not assert where a mutation landed](docs/backlog/idea-2026-08-26-mutation-harness-does-not-assert-where-it-landed.md) (idea, low) - **new 2026-08-26, amended 2026-09-01.** Mutation testing is load-bearing here - it is how several guards were shown decorative and several "unkillable" claims refuted - and there is no committed harness, only a per-slice script. Two FALSE SURVIVALS in one session: the script patched the first matching occurrence, which sat in a different function, and a narrowing `-run` filter hid the kill. A false survival is the dangerous direction: it reads as "not covered" and invites a test that was never needed. **Ten recorded instances now, and the tenth has a mechanism the other nine do not.** All nine are anchors that never matched - CRLF against a `sed`, a mangled backslash, a `$` that will not match before `\r\n`, a unique string in the wrong function. The tenth MATCHED when written and went stale afterwards: a `git stash` round-trip mid-battery re-materialised the working copy as CRLF, and three mutants reported surviving against a guard written specifically to kill them. That is not covered by the Proposal's "assert the mutation landed there", which is stated as a property of the edit and has to be a property of the edit **at the moment the test runs** - `git stash`, `git checkout`, a formatter and a second agent in a shared worktree all invalidate a check performed at batch start. Acceptance now carries that timing property. Each of the ten was a different script making a different mistake, and the CRLF fixes never propagated because there was nothing to propagate them into, which is the item's own argument for a committed harness.

13. [Four worker subcommands cannot resolve a revoked worker by hostname](docs/backlog/idea-2026-08-26-should-worker-subcommands-resolve-revoked-hostnames.md) (idea, low) - **new 2026-08-26**, and an open question rather than a defect. `delete` needs to find a revoked worker by hostname - that is the point of the command - but `resolveWorkerID` has six call sites, so giving it that fallback would extend the capability to `disable`, `enable`, `workspaces` and `evict-workspace` too. The slice used a delete-local resolver instead: the spec had pre-registered the trigger ("fall back to a delete-local resolver if a test pins it") and it fired twice, and two existing fixtures are byte-identical to `origin/main` again as a result. Decide per command, not in one sweep - `workspaces` is read-only and plausibly useful, `evict-workspace` acts on an agent that is by definition not connected.

14. [Restructure the Invariants section of CLAUDE.md rule-first](docs/backlog/idea-2026-08-25-restructure-claude-md-invariants-rule-first.md) (idea, none) - **new 2026-08-25**, from a context audit rather than a code review. The Invariants section is 11,509 chars (~2.9k est tokens) and **57% of CLAUDE.md**; the Epoch fence bullet alone is 6,885 chars, braiding five distinguishable rules (fence-or-end-or-predicate; currency-not-identity; identity-not-honesty; allow-lists-not-deny-lists; the `AppendTaskLog`/`ListOverdueAssignedTasks` carve-outs) into one paragraph with six dated incident narratives inline. Rule-first sub-bullets - statement, one-line rationale, named counter-example, pointer to the retro holding the story - target ~7k chars and ~1.1k est tokens off every session with zero rules lost. **Must run as its own reviewed slice, and the acceptance criteria say so**: wrong prose about correct code is this project’s dominant defect class and these paragraphs are what the review lenses treat as ground truth, so the rewrite needs an independent rule-for-rule comparison against the original. Same family as the CLAUDE.md prose findings that dominate recent retros, one layer up: this one is about the document’s shape rather than any single claim in it.
15. [README describes task_logs as captured stdout/stderr without qualifying that CRLF is normalised](docs/backlog/idea-2026-08-25-readme-task-logs-no-longer-byte-exact.md) (idea, low) - **new 2026-08-25.** The agent now normalises CRLF to LF before a chunk is sent, so stored bytes are no longer a byte-exact copy of subprocess output. Nothing in README is FALSE - no passage claims byte-exactness, which is exactly why the slice's spec scoped README out and the engineer respected that boundary - but a reviewer flagged it as a GAP rather than a defect, since "captured" reads as verbatim to someone deciding whether an export can be. One qualifying clause; resist expanding it, because the full argument already lives in the spec and the export item is already annotated.
16. [mypy silently ignores `python_version = "3.9"`, so the SDK's type-check floor is unenforced](docs/backlog/bug-2026-08-27-mypy-python-version-floor-is-silently-not-enforced.md) (bug, low) - **new 2026-08-27.** mypy 2.x rejects the value, prints a diagnostic, and exits 0, so CI's lint job is green while type-checking at 3.13. `requires-python`, the classifiers and the pytest matrix all still claim 3.9. Nothing is broken today - the suite was run on a real 3.9.5 interpreter, and ruff's `target-version = "py39"` IS honoured - so the loss is type-level only. Worth an item because a gate that passes while emitting a diagnostic about its own configuration reports success in exactly the case it exists to catch.

17. [Go CI runs only on ubuntu-latest and never cross-compiles, so no Windows-only file is ever built](docs/backlog/idea-2026-09-01-go-ci-never-compiles-or-runs-windows-code.md) (idea, medium) - **new 2026-09-01**, and in Next. Both `go-ci.yml` jobs are `ubuntu-latest`; `grep -rn GOOS .github/ Makefile` returns nothing; `release.yml` is the Python SDK's PyPI publish and builds no Go at all. So build constraints exclude `cmd/relay-agent/free_disk_windows.go`, `internal/agent/credentials_acl_windows.go` and `internal/agent/proctree_windows.go` - plus the two `*_windows_test.go` files - from every automated lane, and a compile error in any of them is invisible. Relay agents run on Windows render nodes, and `bug-2026-06-20-agent-proctree-windows-race` (closed) was a Windows-only race in exactly this code. Surfaced by the identity-vars slice, whose case-folding rule is RED on Windows and green on Linux when its Windows clause is dropped, and green on Windows and RED on Linux when the platform check is; that slice's `goos` parameter seam is the right local fix and does not generalise, because a seam cannot parameterise a build-constrained file calling the Windows job-object API. Two halves priced separately in the item: a cross-compile step (seconds, measured green at HEAD) and a `windows-latest` test job (real minutes, default lane only, since the integration lane needs Docker). **Deliberately not merged with item 3** - that is a build TAG the command does not pass, this is a PLATFORM the runner does not have.
18. [The active-task index predicate guard only reads quoted literals](docs/backlog/idea-2026-09-04-index-predicate-guard-is-blind-to-conjuncts.md) (idea, low) - **new this refresh**, same family as items 1 and 8: a guard that agrees with itself rather than with the thing it guards. The test reads `idx_tasks_worker_active`'s predicate back off `pg_get_expr` and extracts it with a quoted-literal regex, so an added CONJUNCT contributes nothing and leaves it green. Measured rather than assumed: the conjunct was applied against a live Postgres and all four current consumers still used the index, because Postgres derives `NOT NULL` from a strict `=` and every one of them carries such a predicate - so this is a blindness, not a live regression, and it would bite the first consumer that carries none. The hard-coded expectation is correct and must stay; the gap is the extraction.
19. [Replace README's hand-maintained log-site and key counts with a test-backed census](docs/backlog/idea-2026-09-03-replace-the-readme-log-site-count-with-a-guard.md) (idea, low) - **new this refresh.** The numbers are correct at HEAD (nine `lim.allow(logKey{...})` sites, nine kinds) and the surrounding argument is arithmetic, so the next kind falsifies a paragraph rather than a digit. Adding one kind this session falsified six counts across four files, and two review rounds were spent finding them all. A guard asserting the site count equals `kindCount - 1` lets README cite a named test instead of a number.
20. [`go-ci.yml`'s `Race unit tests` comment carries the change history current policy forbids](docs/backlog/idea-2026-09-04-go-ci-race-step-comment-carries-change-history.md) (idea, low) - **new this refresh.** It records what a previous version of that comment claimed, why it was wrong, and the date it was fixed. The content is accurate and git is its home. The surviving useful claim is present-tense: the step is byte-identical to `make test-race` and the two must stay in step. Worth doing as a sweep of the file rather than alone - but the three job-level timeout comments reason about a real hazard (a job timeout equal to a Go `-timeout` makes a panic and a job kill indistinguishable) and should be left.
21. [`InventoryRowRejections` exists but no operator can read it](docs/backlog/idea-2026-09-10-publish-inventory-row-rejection-counter.md) (idea, medium) - filed because the comment deferring to it named no findable item. Sibling of the enrollment-refusal counters, deferred the same way, and the two should settle on one answer about sections. The accessor's warning - find the agent, never raise the bound - has to survive into wherever the number is read.

## Recently shipped

Items closed since the previous refresh, one line each. Earlier entries are not kept here: the
durable record is each item's own `## Resolution` note in `docs/backlog/closed/`, the per-item
retros in `docs/retros/`, and `git log --follow ROADMAP.md`.

- [A job author controls how many p4 client specs and workspace directories every agent creates](docs/backlog/closed/idea-2026-09-04-a-job-author-controls-how-many-p4-clients-each-agent-creates.md) - closed 2026-09-10, [#211](https://github.com/chadmv/relay/pull/211); a per-(stream, agent) ceiling over exclusion-derived keys, evicting inline because on a default agent the sweeper reclaims nothing, after measuring against real p4d that a client spec for a nonexistent stream cannot be persisted.
- [A wall-clock deadline on the boot sweep](docs/backlog/closed/feature-2026-09-04-wall-clock-deadline-on-the-boot-sweep.md) - closed 2026-09-10, [#210](https://github.com/chadmv/relay/pull/210); `RELAY_STARTUP_VALIDATION_DEADLINE` at 45s, with the counts and the truncation flag in one value so no caller can print a partial count as a total.
- [Bound the agent-supplied text columns `worker_workspaces` indexes](docs/backlog/closed/idea-2026-09-04-worker-workspaces-source-key-is-unbounded-in-a-primary-key.md) - closed 2026-09-10, [#209](https://github.com/chadmv/relay/pull/209); four columns bounded in bytes behind one constructor, refusing rather than transforming because the value is an identity, with `CHECK ... NOT VALID` so planted data cannot deny the control's own deployment.
- [`applyInventory` freezes a worker's whole inventory on one unparseable `last_used_at`](docs/backlog/closed/bug-2026-08-23-applyinventory-null-timestamp-freezes-inventory.md) - closed 2026-09-10, [#209](https://github.com/chadmv/relay/pull/209), as a consequence of the slice above rather than as its own scope; the parse alone was never the check, since a zero time formats as the year-one instant and parses cleanly.
- [`ReconcileOnStartup` lists every overdue schedule with no LIMIT, also before the listener](docs/backlog/closed/bug-2026-09-04-reconcileonstartup-lists-every-overdue-schedule-unbounded.md) - closed 2026-09-10, [#208](https://github.com/chadmv/relay/pull/208); keyset-paged on `id` and narrowed to four columns, with termination depending on the cursor alone rather than on the loop body succeeding at anything.
- [`source.sync` has no entry-count bound](docs/backlog/closed/idea-2026-09-04-source-sync-has-no-entry-count-bound.md) - closed 2026-09-10, [#207](https://github.com/chadmv/relay/pull/207); `maxSyncEntries = 512`, a per-spec concentration control rather than the aggregate reduction the first draft claimed, and explicitly not a bound on argv bytes.
- [`pgdsn`'s DSN user-arm guard is RED for any run as root](docs/backlog/closed/bug-2026-09-04-pgdsn-user-guard-is-red-for-any-run-as-root.md) - closed 2026-09-10, [#206](https://github.com/chadmv/relay/pull/206); the injected user is a const carrying a colon, which no OS account can be named, after review caught the first fix surviving a mutation the pre-fix version killed.

## Deferred

- [CIDR allowlist for auto-enroll defense-in-depth](docs/backlog/idea-2026-06-04-cidr-allowlist-auto-enroll.md) - explicitly a Non-Goal in the auto-enroll design; the accepted trust boundary is network reachability to the gRPC server.
- [Should the warm-preference scoring be configurable?](docs/backlog/idea-2026-04-25-warm-preference-scoring-configurable.md) - speculative tuning knob with no current need; CLAUDE.md discourages unrequested configurability.
- [Is last_used_at accurate enough for the sweeper's age policy?](docs/backlog/idea-2026-04-25-last-used-at-accuracy-sweeper.md) - an open question with no confirmed defect; revisit if a misjudged-age eviction is actually observed.
